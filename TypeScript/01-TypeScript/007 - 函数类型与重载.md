---
title: "007 - 函数类型与重载"
slug: "007-ts-functions"
category: "TypeScript"
tech_stack: "TypeScript"
created_at: "2026-04-26T17:02:44.436+08:00"
updated_at: "2026-04-29T10:02:46.386+08:00"
reading_time: 19
tags: []
---

# 034-函数类型与重载

> **难度：** 🟡 进阶 | **阅读时间：** 18分钟 | **前置知识：** 接口与类型别名

---

## 一、概念讲解

### 函数类型基础

在TypeScript中，函数的每个参数和返回值都可以（也应该）有类型。

```typescript
// Named function
function add(a: number, b: number): number {
  return a + b;
}

// Arrow function
const multiply = (a: number, b: number): number => a * b;

// Function type expression
type MathOp = (a: number, b: number) => number;
const divide: MathOp = (a, b) => a / b;
```

### 参数变体

```typescript
// Optional parameters (must come last)
function greet(name: string, title?: string): string {
  return title ? `${title} ${name}` : name;
}

// Default parameters
function createUser(name: string, role: string = 'viewer'): object {
  return { name, role };
}

// Rest parameters
function sum(...nums: number[]): number {
  return nums.reduce((a, b) => a + b, 0);
}
```

### 函数重载（Function Overloading）

TypeScript的函数重载通过**多个签名+一个实现**来实现：

```typescript
// Overload signatures
function format(value: string): string;
function format(value: number): string;
// Implementation
function format(value: string | number): string {
  if (typeof value === 'string') return value.trim();
  return value.toFixed(2);
}
```

---

## 二、思维导图

```
函数类型与重载
├── 函数类型定义
│   ├── 行内标注
│   ├── 函数类型表达式
│   └── 接口定义函数
├── 参数类型
│   ├── 必选参数
│   ├── 可选参数 (?:)
│   ├── 默认参数 (= val)
│   └── 剩余参数 (...args)
├── 返回值类型
│   ├── 显式返回
│   ├── void
│   ├── never
│   └── 类型推断
├── 函数重载
│   ├── 多个签名
│   ├── 实现签名
│   └── 使用场景
└── 高级用法
    ├── this类型
    ├── 断言函数
    └── 可调用/可构造
```

---

## 三、完整代码示例

```typescript
// ============================================
// Function Types and Overloads
// ============================================

// --- 1. Basic Function Types ---
function greet(name: string): string {
  return `Hello, ${name}!`;
}

// Function type alias
type Callback = (data: string) => void;
type AsyncFn = (url: string) => Promise<Response>;

// --- 2. Optional & Default Parameters ---
function search(
  query: string,
  limit: number = 10,
  offset?: number
): string[] {
  const results: string[] = [];
  // Simulate search results
  for (let i = 0; i < limit; i++) {
    results.push(`${query}-result-${i}`);
  }
  return results;
}

console.log(search('ts'));           // 10 results
console.log(search('ts', 5));        // 5 results
console.log(search('ts', 5, 10));    // 5 results with offset

// --- 3. Rest Parameters ---
function log(level: string, ...messages: string[]): void {
  console.log(`[${level}]`, messages.join(' '));
}

log('INFO', 'Server', 'started', 'on', 'port', '3000');

// --- 4. Function Overloading ---

// Example 1: Type-safe event handler
function on(event: 'click', handler: (e: MouseEvent) => void): void;
function on(event: 'input', handler: (e: InputEvent) => void): void;
function on(event: string, handler: (e: Event) => void): void {
  console.log(`Registered handler for ${event}`);
}

on('click', (e) => {
  console.log('Click at', e.clientX, e.clientY); // e is MouseEvent
});

// Example 2: Utility function with overloads
function pluck<T, K extends keyof T>(obj: T, key: K): T[K];
function pluck<T>(obj: T, keys: (keyof T)[]): unknown[];
function pluck<T>(obj: T, keyOrKeys: keyof T | (keyof T)[]): unknown {
  if (Array.isArray(keyOrKeys)) {
    return keyOrKeys.map(k => obj[k]);
  }
  return obj[keyOrKeys];
}

const user = { name: 'Alice', age: 30, email: 'a@b.com' };
const name = pluck(user, 'name');      // type: string
const vals = pluck(user, ['name', 'age']); // type: unknown[]

// --- 5. 'this' Parameter ---
interface DB {
  users: string[];
  addUser(this: DB, name: string): void;
}

const db: DB = {
  users: [],
  addUser(name: string) {
    this.users.push(name);
  }
};

db.addUser('Alice');
console.log(db.users); // ['Alice']

// --- 6. Assertion Functions ---
function assertDefined<T>(value: T | undefined | null, msg?: string): asserts value is T {
  if (value == null) throw new Error(msg ?? 'Value is null/undefined');
}

let maybeName: string | undefined = 'Alice';
assertDefined(maybeName);
console.log(maybeName.toUpperCase()); // TS knows it's string now

// --- 7. Predicate Functions ---
function isString(value: unknown): value is string {
  return typeof value === 'string';
}

const items: unknown[] = ['hello', 42, 'world', true];
const strings = items.filter(isString); // type: string[]
console.log(strings); // ['hello', 'world']

// --- 8. Constructor Type ---
type Constructor<T> = new (...args: any[]) => T;

function createInstance<T>(Ctor: Constructor<T>, ...args: any[]): T {
  return new Ctor(...args);
}

class Person {
  constructor(public name: string) {}
}

const p = createInstance(Person, 'Alice');
console.log(p.name); // "Alice"

console.log('All function type examples completed!');
```

---

## 四、执行预览

```bash
$ npx ts-node functions.ts
[ 'ts-result-0', 'ts-result-1', ... 'ts-result-9' ]
[INFO] Server started on port 3000
Registered handler for click
Click at 100 200
[ 'Alice' ]
ALICE
[ 'hello', 'world' ]
Alice
All function type examples completed!
```

---

## 五、注意事项

| 注意点 | 说明 |
|--------|------|
| 可选参数必须在必选参数后面 | `fn(a?: string, b: string)`会报错 |
| 重载的实现在内部是any式处理 | 实现签名对外不可见 |
| `void`返回值不阻止返回值 | 只是告诉调用者不要使用返回值 |
| 箭头函数不能重载 | 只有用function声明的函数支持重载 |
| `this`参数是编译时擦除的 | 只用于类型检查，不影响运行时 |

---

## 六、避坑指南

| ❌ 常见错误 | ✅ 正确做法 |
|-------------|-----------|
| 重载签名顺序随意 | **最具体的**签名放最前面 |
| 实现签名与调用签名不匹配 | 实现参数必须是所有重载的联合 |
| 用`any`做回调参数 | 用泛型或具体函数类型 |
| 忽略返回值推断 | 复杂函数显式标注返回值类型 |
| 在重载中返回不同类型 | 用联合返回值或泛型保持一致性 |

---

## 七、练习题

### 🟢 基础
1. 编写`clamp(value: number, min: number, max: number): number`函数。
2. 定义`Comparator<T>`函数类型，用于排序。

### 🟡 进阶
3. 实现重载函数`createElement`：传入string返回Text节点，传入对象返回Element。
4. 编写类型安全的`EventEmitter.on`方法，不同事件名对应不同回调类型。

### 🔴 挑战
5. 实现一个类型安全的`pipe`函数：`pipe(fn1, fn2, fn3)`，前一个函数的输出是后一个的输入。

---

## 八、知识点总结

```
函数类型与重载
├── 基础
│   ├── 参数类型标注
│   ├── 返回值类型
│   └── 类型推断
├── 参数变体
│   ├── 可选参数 (?:)
│   ├── 默认参数 (= val)
│   └── 剩余参数 (...args)
├── 函数类型表达式
│   ├── type Fn = (...) => ...
│   └── interface Fn { (...): ... }
├── 函数重载
│   ├── 签名列表
│   ├── 实现签名
│   └── 顺序规则
├── 特殊函数
│   ├── 断言函数 (asserts)
│   ├── 谓词函数 (is)
│   └── 构造签名 (new)
└── this参数
    ├── 显式this类型
    └── 回调中的this
```

---

## 九、举一反三

| 场景 | 函数类型用法 |
|------|------------|
| API层 | `type APIFn<T> = (params: Request) => Promise<Response<T>>` |
| 事件系统 | 重载实现不同事件的回调类型 |
| 管道/中间件 | 泛型函数链式调用 |
| 数据验证 | 谓词函数`value is T`做类型收窄 |
| 工厂模式 | `Constructor<T>`类型做依赖注入 |
| 条件检查 | 断言函数`asserts value is T` |

---

## 十、参考资料

- [TypeScript Handbook - Functions](https://www.typescriptlang.org/docs/handbook/2/functions.html)
- [TypeScript Handbook - Function Overloads](https://www.typescriptlang.org/docs/handbook/2/functions.html#function-overloads)

---

## 十一、代码演进

### v1 — 简单函数
```typescript
function processInput(input) {
  if (typeof input === 'string') return input.toUpperCase();
  if (typeof input === 'number') return input.toFixed(2);
  return String(input);
}
```

### v2 — 添加类型注解
```typescript
function processInput(input: unknown): string {
  if (typeof input === 'string') return input.toUpperCase();
  if (typeof input === 'number') return input.toFixed(2);
  return String(input);
}
```

### v3 — 函数重载+泛型
```typescript
function process(input: string): string;
function process(input: number): string;
function process(input: string | number): string {
  if (typeof input === 'string') return input.toUpperCase();
  return input.toFixed(2);
}

// Generic version with return type inference
function transform<T extends string | number>(input: T): string {
  return typeof input === 'string' ? input.toUpperCase() : input.toFixed(2);
}
```

---

**总结：** TypeScript函数类型是日常开发中最常用的特性。掌握参数变体、函数重载和谓词函数，你的代码将拥有编译时的安全保障。
