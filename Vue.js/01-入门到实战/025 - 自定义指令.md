---
title: "025 - 自定义指令"
slug: "025-custom-directives"
category: "Vue.js入门到实战"
tech_stack: "Vue.js"
created_at: "2026-04-26T17:45:39.912+08:00"
updated_at: "2026-04-29T10:02:46.701+08:00"
reading_time: 18
tags: ["Vue.js", "Vue 3"]
---

## 难度标注

> 🟡 **中级** — 需要 Vue 3 基础、了解指令概念

## 概念讲解

### 什么是自定义指令？

自定义指令（Custom Directives）允许你**封装底层 DOM 操作**，以声明式的方式应用到元素上。适用于需要直接操作 DOM 的场景。

### 指令钩子函数

| 钩子 | 触发时机 |
|------|----------|
| `created` | 元素创建后，属性/事件绑定前 |
| `beforeMount` | 元素挂载到 DOM 前 |
| `mounted` | 元素挂载后（最常用） |
| `beforeUpdate` | 父组件更新前 |
| `updated` | 父组件更新后 |
| `beforeUnmount` | 元素卸载前 |
| `unmounted` | 元素卸载后 |

### 指令与组件的选择

> 优先用组件，只在需要**复用底层 DOM 操作**时才用指令。

## 脑图

```
自定义指令
├── 钩子函数
│   ├── mounted — 最常用
│   ├── updated — 值变化时
│   └── unmounted — 清理
├── 注册方式
│   ├── app.directive() — 全局
│   └── directives: {} — 局部（setup 中 vXxx）
├── 实用指令
│   ├── v-focus — 自动聚焦
│   ├── v-permission — 权限控制
│   ├── v-debounce — 防抖点击
│   ├── v-lazy — 图片懒加载
│   └── v-copy — 一键复制
└── 简写
    └── (el, binding) => { ... }
       等同于 mounted + updated
```

## 完整代码

```javascript
// directives/index.js — Common custom directives

// Auto-focus directive
export const vFocus = {
  mounted(el) {
    el.focus()
  }
}

// Permission control directive
// Usage: v-permission="['admin', 'editor']"
export const vPermission = {
  mounted(el, binding) {
    const requiredRoles = binding.value
    const userRoles = JSON.parse(localStorage.getItem('roles') || '[]')
    const hasPermission = requiredRoles.some(role => userRoles.includes(role))

    if (!hasPermission) {
      // Remove element from DOM
      el.parentNode?.removeChild(el)
    }
  }
}

// Debounce click directive
// Usage: v-debounce="handleSubmit" or v-debounce:500="handleSubmit"
export const vDebounce = {
  mounted(el, binding) {
    const delay = binding.arg ? parseInt(binding.arg) : 300
    let timer = null

    el.addEventListener('click', () => {
      if (timer) clearTimeout(timer)
      timer = setTimeout(() => {
        binding.value()
      }, delay)
    })
  },

  unmounted(el) {
    // Clean up is handled by element removal
  }
}

// Copy to clipboard directive
// Usage: v-copy="'text to copy'"
export const vCopy = {
  mounted(el, binding) {
    el.style.cursor = 'pointer'
    el.addEventListener('click', () => {
      navigator.clipboard.writeText(binding.value)
        .then(() => {
          // Visual feedback
          const original = el.textContent
          el.textContent = 'Copied!'
          setTimeout(() => { el.textContent = original }, 1500)
        })
        .catch(() => {
          // Fallback for older browsers
          const textarea = document.createElement('textarea')
          textarea.value = binding.value
          document.body.appendChild(textarea)
          textarea.select()
          document.execCommand('copy')
          document.body.removeChild(textarea)
        })
    })
  }
}

// Image lazy loading directive
// Usage: v-lazy="imageUrl"
export const vLazy = {
  mounted(el, binding) {
    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            el.src = binding.value
            el.classList.add('loaded')
            observer.unobserve(el)
          }
        })
      },
      { rootMargin: '100px' }
    )
    observer.observe(el)
    el._lazyObserver = observer
  },

  unmounted(el) {
    el._lazyObserver?.disconnect()
  }
}

// Long press directive
// Usage: v-longpress="handleLongPress"
export const vLongpress = {
  mounted(el, binding) {
    let timer = null

    el.addEventListener('mousedown', () => {
      timer = setTimeout(() => binding.value(), 500)
    })

    el.addEventListener('mouseup', () => {
      if (timer) clearTimeout(timer)
    })

    el.addEventListener('mouseleave', () => {
      if (timer) clearTimeout(timer)
    })
  }
}
```

```javascript
// main.js — Register directives globally
import { createApp } from 'vue'
import App from './App.vue'

const app = createApp(App)

// Register globally
app.directive('focus', vFocus)
app.directive('permission', vPermission)
app.directive('debounce', vDebounce)
app.directive('copy', vCopy)
app.directive('lazy', vLazy)

app.mount('#app')
```

```vue
<!-- components/DirectiveDemo.vue — Using custom directives -->
<script setup>
import { ref } from 'vue'

const imageUrl = ref('https://picsum.photos/400/300')
const copyText = ref('Hello from v-copy!')

function handleClick() {
  console.log('Debounced click!')
}

function handleLongPress() {
  alert('Long pressed!')
}
</script>

<template>
  <div>
    <!-- Auto-focus -->
    <input v-focus placeholder="Auto-focused" />

    <!-- Permission control -->
    <button v-permission="['admin']">Admin Only Button</button>

    <!-- Debounce click -->
    <button v-debounce:500="handleClick">Debounced Button (500ms)</button>

    <!-- Copy text -->
    <span v-copy="copyText">Click to copy: {{ copyText }}</span>

    <!-- Lazy load image -->
    <img v-lazy="imageUrl" alt="Lazy loaded" style="width: 400px" />

    <!-- Long press -->
    <button v-longpress="handleLongPress">Hold me (500ms)</button>
  </div>
</template>
```

## 执行预览

```
页面加载:
  → v-focus: input 自动获得焦点

用户角色 ["user"]:
  → v-permission="['admin']": 按钮被移除

用户角色 ["admin"]:
  → v-permission="['admin']": 按钮正常显示

点击 debounced 按钮:
  → 500ms 内重复点击只执行最后一次

点击 copy 文本:
  → 文本复制到剪贴板
  → 显示 "Copied!" 1.5秒后恢复

滚动到图片位置:
  → IntersectionObserver 检测到可见
  → 设置 src 加载图片
```

## 注意事项

| 注意点 | 说明 |
|--------|------|
| 指令命名 | `vFocus` 对应 `v-focus`（自动转换） |
| 简写形式 | `(el, binding) => {}` 等同于 mounted + updated |
| 清理资源 | unmounted 中清理 observer、事件监听等 |
| binding.value | 指令绑定值，如 `v-xxx="expr"` 中的 expr |
| binding.arg | 指令参数，如 `v-xxx:500` 中的 "500" |
| binding.modifiers | 修饰符，如 `v-xxx.prevent` 中的 `{ prevent: true }` |

## 避坑指南

| ❌ 错误做法 | ✅ 正确做法 |
|------------|-----------|
| 指令中修改组件状态 | 指令只做 DOM 操作，状态管理用组件 |
| 不清理副作用 | unmounted 中 disconnect observer / removeEventListener |
| 用指令实现 v-if 功能 | 逻辑显示/隐藏用 v-if，指令做 DOM 操作 |
| 全局注册太多指令 | 常用的全局，不常用的局部注册 |
| 指令中硬编码数据 | 通过 binding.value 动态传入 |

## 练习题

### 🟢 基础
1. 实现 v-focus 指令，让元素自动聚焦
2. 实现 v-color 指令，动态设置元素背景色

### 🟡 进阶
3. 实现 v-debounce 指令，支持自定义延迟时间
4. 实现 v-lazy 图片懒加载指令，使用 IntersectionObserver

### 🔴 挑战
5. 实现 v-click-outside 指令，点击元素外部时触发回调

## 知识点总结

```
自定义指令
├── 钩子
│   ├── mounted(el, binding) — 最常用
│   ├── updated(el, binding) — 值更新
│   └── unmounted(el) — 清理
├── binding 对象
│   ├── value — 绑定值
│   ├── arg — 参数 (v-xxx:arg)
│   ├── modifiers — 修饰符 (v-xxx.mod)
│   └── oldValue — 旧值
├── 注册
│   ├── app.directive('xxx', {}) — 全局
│   └── const vXxx = {} — setup 局部
├── 实用指令
│   ├── v-focus / v-permission
│   ├── v-debounce / v-lazy
│   └── v-copy / v-longpress
└── 简写
    └── (el, binding) => { ... }
```

## 举一反三

| 场景 | 指令 | 技巧 |
|------|------|------|
| 自动聚焦 | v-focus | mounted + el.focus() |
| 权限隐藏 | v-permission | el.parentNode.removeChild() |
| 防抖点击 | v-debounce | setTimeout 延迟执行 |
| 图片懒加载 | v-lazy | IntersectionObserver |
| 一键复制 | v-copy | navigator.clipboard |
| 点击外部 | v-click-outside | contains(event.target) |

## 参考资料

- [Vue 3 官方文档 - Custom Directives](https://vuejs.org/guide/reusability/custom-directives.html)
- [Vue 3 API - Directive](https://vuejs.org/api/custom-directive.html)

## 代码演进

### v1 — 最简指令
```javascript
const vFocus = { mounted(el) { el.focus() } }
```

### v2 — 带参数和清理
```javascript
const vLazy = {
  mounted(el, binding) { observer.observe(el) },
  unmounted(el) { el._observer?.disconnect() }
}
```

### v3 — 完整方案（本文代码）
- 多种实用指令
- 全局/局部注册
- 完整清理逻辑
- binding 完整使用
