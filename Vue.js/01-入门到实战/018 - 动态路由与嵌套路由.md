---
title: "018 - 动态路由与嵌套路由"
slug: "018-dynamic-nested-routes"
category: "Vue.js入门到实战"
tech_stack: "Vue.js"
created_at: "2026-04-26T17:38:25.944+08:00"
updated_at: "2026-04-29T10:02:46.628+08:00"
reading_time: 22
tags: ["Vue.js", "Vue 3"]
---

# 056-动态路由与嵌套路由

> 📌 **难度标注**：⭐⭐⭐ 中级（需要理解 Vue Router 基础配置）

## 一、概念讲解

### 动态路由

动态路由通过在路径中使用**动态参数**（`:param`）匹配多个相似路由。例如 `/user/:id` 可以匹配 `/user/1`、`/user/42` 等。

关键特性：
- **参数获取**：通过 `useRoute().params` 访问
- **参数变化监听**：同组件间跳转时组件会复用，需要手动监听参数变化
- **可选参数**：`/user/:id?` 中 `?` 表示参数可选
- **正则约束**：`/user/:id(\d+)` 限制 id 只能是数字
- **通配符**：`/:pathMatch(.*)*` 匹配所有未命中的路径（404页面）

### 嵌套路由

嵌套路由允许在组件内部嵌套 `<router-view>`，实现页面布局的层级嵌套。例如后台管理系统中，侧边栏固定、内容区根据子路由变化。

### 命名视图

同一层级同时展示多个 `<router-view>`，通过 `name` 属性区分，适合复杂布局。

## 二、脑图

```
动态路由与嵌套路由
├── 动态路由
│   ├── 基础参数  /user/:id
│   ├── 多参数    /post/:category/:id
│   ├── 可选参数  /user/:id?
│   ├── 正则约束  /user/:id(\d+)
│   ├── 通配符    /:pathMatch(.*)*
│   └── 参数获取  useRoute().params
├── 嵌套路由
│   ├── children 配置
│   ├── 嵌套 <router-view>
│   ├── 默认子路由
│   └── 深层嵌套 (3+ levels)
├── 命名视图
│   ├── <router-view name="header">
│   ├── components 配置 (复数)
│   └── 嵌套命名视图
└── 编程式导航
    ├── router.push()
    ├── router.replace()
    └── router.go() / back() / forward()
```

## 三、完整代码

```javascript
// router/index.js
import { createRouter, createWebHistory } from 'vue-router'

const AdminLayout = () => import('../views/admin/Layout.vue')
const DashboardHome = () => import('../views/admin/Dashboard.vue')
const UserList = () => import('../views/admin/UserList.vue')
const UserDetail = () => import('../views/admin/UserDetail.vue')
const UserEdit = () => import('../views/admin/UserEdit.vue')
const PostDetail = () => import('../views/admin/PostDetail.vue')
const Settings = () => import('../views/admin/Settings.vue')
const NotFound = () => import('../views/NotFound.vue')

const routes = [
  {
    path: '/',
    component: () => import('../views/Home.vue')
  },
  {
    path: '/admin',
    component: AdminLayout,
    children: [
      {
        path: '',           // /admin
        name: 'AdminHome',
        component: DashboardHome
      },
      {
        path: 'users',      // /admin/users
        name: 'UserList',
        component: UserList
      },
      {
        path: 'users/:id',  // /admin/users/:id
        name: 'UserDetail',
        component: UserDetail,
        children: [
          {
            path: '',        // /admin/users/:id
            name: 'UserProfile',
            component: () => import('../views/admin/UserProfile.vue')
          },
          {
            path: 'edit',   // /admin/users/:id/edit
            name: 'UserEdit',
            component: UserEdit
          }
        ]
      },
      {
        path: 'posts/:category/:id', // Multiple dynamic params
        name: 'PostDetail',
        component: PostDetail
      },
      {
        path: 'settings',
        name: 'Settings',
        component: Settings
      }
    ]
  },
  {
    // Regex constraint: id must be numeric
    path: '/article/:id(\d+)',
    name: 'Article',
    component: () => import('../views/Article.vue')
  },
  {
    // Named views example
    path: '/blog/:id',
    name: 'BlogPost',
    components: {
      default: () => import('../views/BlogContent.vue'),
      sidebar: () => import('../views/BlogSidebar.vue'),
      footer: () => import('../views/BlogFooter.vue')
    }
  },
  {
    // Catch-all 404
    path: '/:pathMatch(.*)*',
    name: 'NotFound',
    component: NotFound
  }
]

const router = createRouter({
  history: createWebHistory(),
  routes
})

export default router
```

```vue
<!-- views/admin/Layout.vue -->
<template>
  <div class="admin-layout">
    <aside class="sidebar">
      <h3>管理后台</h3>
      <nav>
        <router-link :to="{ name: 'AdminHome' }">仪表盘</router-link>
        <router-link :to="{ name: 'UserList' }">用户管理</router-link>
        <router-link :to="{ name: 'Settings' }">设置</router-link>
      </nav>
    </aside>
    <main class="content">
      <router-view />
    </main>
  </div>
</template>

<style scoped>
.admin-layout { display: flex; min-height: 100vh; }
.sidebar { width: 220px; background: #2c3e50; color: #fff; padding: 20px; }
.sidebar nav { display: flex; flex-direction: column; gap: 8px; margin-top: 16px; }
.sidebar a { color: #ccc; text-decoration: none; padding: 8px; border-radius: 4px; }
.sidebar a.router-link-active { background: #42b883; color: #fff; }
.content { flex: 1; padding: 24px; }
</style>
```

```vue
<!-- views/admin/UserDetail.vue -->
<template>
  <div class="user-detail">
    <h2>用户详情 #{{ $route.params.id }}</h2>
    <nav class="sub-nav">
      <router-link :to="{ name: 'UserProfile' }">基本信息</router-link>
      <router-link :to="{ name: 'UserEdit' }">编辑</router-link>
    </nav>
    <router-view />
  </div>
</template>
```

```vue
<!-- views/admin/PostDetail.vue -->
<template>
  <div>
    <h2>文章详情</h2>
    <p>分类：{{ $route.params.category }}</p>
    <p>文章ID：{{ $route.params.id }}</p>
    <button @click="$router.back()">返回</button>
  </div>
</template>

<script setup>
import { useRoute } from 'vue-router'
import { watch } from 'vue'

const route = useRoute()

// Watch params for component reuse
watch(() => route.params, (newParams) => {
  console.log('Params changed:', newParams)
}, { immediate: true })
</script>
```

## 四、执行预览

```
URL结构示例：

/admin                     -> Layout + DashboardHome
/admin/users               -> Layout + UserList
/admin/users/42            -> Layout + UserDetail + UserProfile
/admin/users/42/edit       -> Layout + UserDetail + UserEdit
/admin/posts/vue/123       -> Layout + PostDetail (category=vue, id=123)
/article/999               -> Article (id must be numeric)
/article/abc               -> NotFound (regex fails)
/anything/else             -> NotFound (catch-all)
```

## 五、注意事项

| 场景 | 注意事项 | 推荐做法 |
|------|---------|---------|
| 组件复用 | /user/1->/user/2 组件不重建 | watch(() => route.params) |
| 404路由 | 必须放在路由配置最后 | 使用 /:pathMatch(.*)* |
| 嵌套层级 | 避免超过3层 | 过深时考虑扁平化 |
| 参数类型 | params 拿到的都是字符串 | 手动 parseInt() |
| 正则转义 | 字符串中需要双重转义 | 仔细测试正则 |
| 命名视图 | components 是复数 | components: { default: A, sidebar: B } |

## 六、避坑指南

### ❌ 坑1：组件复用时数据不更新

```vue
<!-- ❌ onMounted 只执行一次 -->
<script setup>
import { onMounted } from 'vue'
import { useRoute } from 'vue-router'
const route = useRoute()
onMounted(() => {
  fetchUser(route.params.id) // /user/1->/user/2 won't trigger
})
</script>

<!-- ✅ 使用 watch 监听参数变化 -->
<script setup>
import { watch } from 'vue'
import { useRoute } from 'vue-router'
const route = useRoute()
watch(() => route.params.id, (newId) => {
  fetchUser(newId)
}, { immediate: true })
</script>
```

### ❌ 坑2：子路由 path 加了前导斜杠

```javascript
// ❌ 绝对路径，不再是嵌套子路由
children: [
  { path: '/users', component: UserList } // matches /users, not /admin/users
]

// ✅ 相对路径
children: [
  { path: 'users', component: UserList } // matches /admin/users
]
```

### ❌ 坑3：编程式导航 params 被忽略

```javascript
// ❌ path + params 无效（Vue Router 4）
router.push({ path: '/user', params: { id: 1 } })

// ✅ 使用 name 传递 params
router.push({ name: 'UserDetail', params: { id: 1 } })
```

## 七、练习题

### 🟢 入门：用户资料路由
创建 `/profile/:username` 动态路由，显示对应用户名。用户名为空时跳转首页。

### 🟡 进阶：三级嵌套管理后台
实现 `/admin/users/:userId/posts/:postId` 四层路由，展示特定用户的文章。

### 🔴 挑战：命名视图布局
实现三栏布局：左侧导航（`<router-view name="nav">`）、中间内容、右侧边栏（`<router-view name="aside">`）。

## 八、知识点总结

```
动态路由与嵌套路由
├── 动态路由参数
│   ├── :param        -> 单个参数
│   ├── :param?       -> 可选参数
│   ├── :param(\d+)   -> 正则约束
│   └── :pathMatch(.*)* -> 通配符
├── 嵌套路由
│   ├── children 数组
│   ├── 父组件 <router-view>
│   └── 默认子路由 path: ''
├── 命名视图
│   ├── components (复数)
│   └── <router-view name="xxx">
└── 关键API
    ├── useRoute()  -> 当前路由信息
    ├── useRouter() -> 编程式导航
    └── watch(route.params) -> 参数变化监听
```

## 九、举一反三

| 技术 | 场景 | 实际应用 |
|------|------|---------|
| 动态路由 | RESTful URL | /api/post/:id |
| 嵌套路由 | 后台管理菜单 | 系统->用户->详情 |
| 命名视图 | 复杂布局 | CMS：头部+主体+侧边栏 |
| 正则约束 | 参数校验 | /article/:id(\d+) |
| 通配符404 | 兜底页面 | 未匹配URL显示404 |
| watch params | 组件复用刷新 | 列表点击不同项 |

## 十、参考资料

- [Vue Router - 动态路由匹配](https://router.vuejs.org/zh/guide/essentials/dynamic-matching.html)
- [Vue Router - 嵌套路由](https://router.vuejs.org/zh/guide/essentials/nested-routes.html)
- [Vue Router - 命名视图](https://router.vuejs.org/zh/guide/essentials/named-views.html)

## 十一、代码演进

### v1：基础动态路由
```javascript
const routes = [
  { path: '/user/:id', component: UserDetail }
]
```

### v2：嵌套+多参数
```javascript
const routes = [
  {
    path: '/admin',
    component: AdminLayout,
    children: [
      { path: 'users/:id', component: UserDetail },
      { path: 'posts/:category/:id', component: PostDetail }
    ]
  }
]
```

### v3：完整方案
```javascript
const routes = [
  {
    path: '/admin',
    component: AdminLayout,
    children: [
      { path: '', component: Dashboard },
      { path: 'users/:id(\d+)', component: UserDetail },
      { path: 'posts/:category/:id', component: PostDetail }
    ]
  },
  {
    path: '/blog/:id',
    components: { default: BlogContent, sidebar: BlogSidebar }
  },
  { path: '/:pathMatch(.*)*', component: NotFound }
]
```

> 💡 **总结**：动态路由和嵌套路由是构建复杂单页应用的基石。掌握参数获取、组件复用时的数据刷新、以及合理的路由层级设计，能让你构建出清晰、可维护的前端路由架构。
