---
title: "033 - 实战：博客系统前端"
slug: "033-blog-frontend"
category: "Vue.js入门到实战"
tech_stack: "Vue.js"
created_at: "2026-04-26T17:59:15.002+08:00"
updated_at: "2026-04-29T10:02:46.779+08:00"
reading_time: 26
tags: ["Vue.js", "Vue 3"]
---


## 难度标注
> **难度：🟡 中高级** | **前置知识：Vue Router、Pinia、API集成** | **预计学习时间：90 分钟**

## 概念讲解

### 博客系统前端架构

博客前端是一个经典的 CRUD 应用，但涉及的技术点非常全面：

- **内容展示**：Markdown 渲染、代码高亮、目录导航
- **数据筛选**：分类、标签、搜索、分页
- **状态管理**：文章缓存、用户状态、UI 状态
- **性能优化**：组件懒加载、图片懒加载、SEO

### 技术选型

| 模块 | 技术 | 理由 |
|------|------|------|
| 框架 | Vue 3 + TypeScript | 类型安全、Composition API |
| 路由 | Vue Router 4 | 官方方案，足够成熟 |
| 状态管理 | Pinia | 轻量、TypeScript 友好 |
| UI 组件 | Element Plus | 后台管理场景 |
| Markdown | markdown-it + highlight.js | 可扩展、高性能 |
| HTTP | Axios | 拦截器、取消请求 |
| 构建工具 | Vite | 开发体验极佳 |

## 脑图

```
博客前端系统
├── 页面
│   ├── 首页 (文章列表)
│   ├── 文章详情
│   ├── 分类页
│   ├── 标签页
│   ├── 搜索页
│   └── 关于页
├── 功能模块
│   ├── Markdown 渲染
│   ├── 代码高亮
│   ├── 目录导航 (TOC)
│   ├── 文章搜索
│   ├── 分页加载
│   └── 响应式布局
├── 状态管理
│   ├── 文章缓存
│   ├── 分类/标签列表
│   └── UI 状态
└── 性能优化
    ├── 路由懒加载
    ├── 图片懒加载
    └── Markdown 预渲染
```

## 完整代码

### 路由配置

```ts
// src/router/index.ts
import { createRouter, createWebHistory } from 'vue-router'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    {
      path: '/',
      component: () => import('@/layouts/BlogLayout.vue'),
      children: [
        { path: '', name: 'home', component: () => import('@/views/Home.vue') },
        { path: 'post/:slug', name: 'post', component: () => import('@/views/PostDetail.vue') },
        { path: 'category/:slug', name: 'category', component: () => import('@/views/Category.vue') },
        { path: 'tag/:slug', name: 'tag', component: () => import('@/views/Tag.vue') },
        { path: 'search', name: 'search', component: () => import('@/views/Search.vue') },
        { path: 'about', name: 'about', component: () => import('@/views/About.vue') }
      ]
    }
  ],
  scrollBehavior() {
    return { top: 0 }
  }
})

export default router
```

### 文章 Store

```ts
// src/stores/post.ts
import { defineStore } from 'pinia'
import { ref } from 'vue'
import { postApi, type Post, type PostListParams } from '@/api/post'

export const usePostStore = defineStore('post', () => {
  // Cache fetched posts by slug
  const cache = ref<Map<string, Post>>(new Map())
  const currentPost = ref<Post | null>(null)

  async function fetchList(params: PostListParams) {
    const res = await postApi.getList(params)
    return res
  }

  async function fetchBySlug(slug: string) {
    // Check cache first
    if (cache.value.has(slug)) {
      currentPost.value = cache.value.get(slug)!
      return currentPost.value
    }
    const post = await postApi.getBySlug(slug)
    cache.value.set(slug, post)
    currentPost.value = post
    return post
  }

  function clearCache() {
    cache.value.clear()
  }

  return { currentPost, fetchList, fetchBySlug, clearCache }
})
```

### Markdown 渲染工具

```ts
// src/utils/markdown.ts
import MarkdownIt from 'markdown-it'
import hljs from 'highlight.js'
import toc from 'markdown-it-table-of-contents'

const md = new MarkdownIt({
  html: true,
  linkify: true,
  // Custom code highlighting
  highlight(str: string, lang: string) {
    if (lang && hljs.getLanguage(lang)) {
      try {
        return `<pre class="hljs"><code>${hljs.highlight(str, { language: lang }).value}</code></pre>`
      } catch (_) { /* fallback */ }
    }
    return `<pre class="hljs"><code>${md.utils.escapeHtml(str)}</code></pre>`
  }
})

// Add heading IDs for TOC
md.use(toc)

// Render markdown to HTML + extract headings for TOC
export function renderMarkdown(content: string) {
  const html = md.render(content)
  const headings = extractHeadings(html)
  return { html, headings }
}

// Extract h2/h3 headings for sidebar navigation
function extractHeadings(html: string) {
  const regex = /<h([2-3])[^>]*id="([^"]*)"[^>]*>(.*?)<\/h\1>/g
  const headings: { level: number; id: string; text: string }[] = []
  let match
  while ((match = regex.exec(html)) !== null) {
    headings.push({
      level: parseInt(match[1]),
      id: match[2],
      text: match[3].replace(/<[^>]*>/g, '') // strip HTML tags
    })
  }
  return headings
}
```

### 文章详情页

```vue
<!-- src/views/PostDetail.vue -->
<template>
  <div class="post-detail" v-loading="loading">
    <article v-if="post" class="post-content">
      <header class="post-header">
        <h1 class="post-title">{{ post.title }}</h1>
        <div class="post-meta">
          <span>{{ formatDate(post.createdAt) }}</span>
          <span>·</span>
          <router-link :to="`/category/${post.category.slug}`">
            {{ post.category.name }}
          </router-link>
          <span>·</span>
          <span>{{ post.readingTime }} min read</span>
        </div>
        <div class="post-tags">
          <router-link
            v-for="tag in post.tags"
            :key="tag.slug"
            :to="`/tag/${tag.slug}`"
            class="tag"
          >
            #{{ tag.name }}
          </router-link>
        </div>
      </header>

      <!-- Markdown rendered content -->
      <div class="post-body markdown-body" v-html="renderedHtml" />

      <footer class="post-footer">
        <h3>Related Posts</h3>
        <div class="related-posts">
          <router-link
            v-for="related in relatedPosts"
            :key="related.slug"
            :to="`/post/${related.slug}`"
            class="related-card"
          >
            <h4>{{ related.title }}</h4>
            <p>{{ related.excerpt }}</p>
          </router-link>
        </div>
      </footer>
    </article>

    <!-- TOC Sidebar -->
    <aside v-if="headings.length" class="toc-sidebar">
      <h3>Table of Contents</h3>
      <nav>
        <a
          v-for="h in headings"
          :key="h.id"
          :href="`#${h.id}`"
          :class="['toc-link', `toc-h${h.level}`, { active: activeHeading === h.id }]"
        >
          {{ h.text }}
        </a>
      </nav>
    </aside>
  </div>
</template>

<script setup lang="ts">
import { ref, computed, watch, onMounted, onUnmounted } from 'vue'
import { useRoute } from 'vue-router'
import { usePostStore } from '@/stores/post'
import { renderMarkdown } from '@/utils/markdown'
import { postApi } from '@/api/post'

const route = useRoute()
const postStore = usePostStore()

const loading = ref(false)
const relatedPosts = ref<any[]>([])
const activeHeading = ref('')

// Fetch post data
const post = computed(() => postStore.currentPost)

// Render markdown
const renderedHtml = computed(() => {
  if (!post.value?.content) return ''
  return renderMarkdown(post.value.content).html
})

const headings = computed(() => {
  if (!post.value?.content) return []
  return renderMarkdown(post.value.content).headings
})

// Load post when slug changes
async function loadPost(slug: string) {
  loading.value = true
  try {
    await postStore.fetchBySlug(slug)
    // Load related posts
    const related = await postApi.getRelated(slug)
    relatedPosts.value = related
  } finally {
    loading.value = false
  }
}

watch(() => route.params.slug, (slug) => {
  if (slug) loadPost(slug as string)
}, { immediate: true })

// Track active heading on scroll
function handleScroll() {
  for (const h of headings.value) {
    const el = document.getElementById(h.id)
    if (el && el.getBoundingClientRect().top > 100) break
    activeHeading.value = h.id
  }
}

onMounted(() => window.addEventListener('scroll', handleScroll))
onUnmounted(() => window.removeEventListener('scroll', handleScroll))

function formatDate(date: string) {
  return new Date(date).toLocaleDateString('en-US', {
    year: 'numeric', month: 'long', day: 'numeric'
  })
}
</script>

<style scoped>
.post-detail {
  display: flex;
  gap: 2rem;
  max-width: 1100px;
  margin: 0 auto;
  padding: 2rem;
}
.post-content { flex: 1; min-width: 0; }
.toc-sidebar {
  width: 240px;
  position: sticky;
  top: 2rem;
  align-self: flex-start;
}
.toc-link { display: block; padding: 4px 0; color: #666; text-decoration: none; }
.toc-h3 { padding-left: 1rem; }
.toc-link.active { color: #409eff; font-weight: 600; }
</style>
```

## 执行预览

```
首页加载
├── GET /api/posts?page=1&pageSize=10
├── 渲染文章卡片列表
└── 侧边栏：分类列表 + 标签云

点击文章 → /post/vue-3-composition-api
├── GET /api/posts/vue-3-composition-api
├── Markdown → HTML 渲染
├── 代码高亮生效
├── TOC 侧边栏生成
└── 滚动时高亮当前标题

搜索 "vue"
├── GET /api/posts?keyword=vue
└── 展示搜索结果列表
```

## 注意事项

| 事项 | 说明 | 建议 |
|------|------|------|
| XSS 安全 | v-html 有注入风险 | 后端对 Markdown 内容做 sanitize |
| SEO | SPA 不利 SEO | 考虑 SSR 或预渲染 |
| 图片优化 | 博客图片可能很大 | 懒加载 + WebP 格式 |
| 代码高亮 | highlight.js 包较大 | 按需引入语言包 |
| TOC 定位 | 标题可能动态变化 | 用 IntersectionObserver |
| 缓存策略 | 文章内容不常变 | Pinia 缓存 + localStorage |

## 避坑指南

- ❌ 每次进入文章都重新请求 → ✅ Pinia 缓存已加载的文章
- ❌ `v-html` 渲染未过滤的内容 → ✅ 后端 sanitize 或使用 DOMPurify
- ❌ 引入全部 highlight.js 语言 → ✅ 只引入需要的语言包
- ❌ TOC 用 `scroll` 事件监听 → ✅ IntersectionObserver 性能更好
- ❌ 文章图片不处理 → ✅ 懒加载 + 点击放大

## 练习题

### 🟢 基础
1. 实现博客首页文章列表，支持分页
2. 实现文章详情页，Markdown 渲染并显示

### 🟡 进阶
3. 实现 TOC 侧边栏，滚动时高亮当前章节
4. 实现搜索功能，支持关键词搜索文章

### 🔴 挑战
5. 实现文章缓存策略：Pinia + localStorage 持久化
6. 实现暗黑模式切换，博客主题跟随系统偏好

## 知识点总结

```
博客前端知识树
├── 页面结构
│   ├── 布局组件 (Header/Footer/Sidebar)
│   ├── 路由配置 (嵌套路由)
│   └── 响应式适配
├── 内容渲染
│   ├── Markdown-it 配置
│   ├── 代码高亮 (highlight.js)
│   ├── TOC 生成
│   └── v-html 安全
├── 数据管理
│   ├── Pinia Store (文章缓存)
│   ├── API 模块化
│   └── 分页/筛选
└── 性能优化
    ├── 路由懒加载
    ├── 图片懒加载
    └── 组件按需引入
```

## 举一反三

| 功能 | 实现方式 | 关键点 |
|------|----------|--------|
| 文章列表 | 分页 API + 卡片组件 | usePagination composable |
| 文章详情 | Markdown 渲染 + TOC | markdown-it + heading extraction |
| 分类筛选 | 路由参数 + API 过滤 | /category/:slug |
| 标签云 | 统计标签文章数 | 字体大小映射 |
| 搜索 | 关键词 + 防抖 | debounce + API keyword |
| 阅读时间 | 字数 / 阅读速度 | 约 200 字/分钟 |
| 相关推荐 | 同分类/标签文章 | API 按相似度排序 |

## 参考资料

- [markdown-it 官方文档](https://github.com/markdown-it/markdown-it)
- [highlight.js](https://highlightjs.org/)
- [Vue Router 4](https://router.vuejs.org/)

## 代码演进

### v1 — 基础列表 + 详情
```vue
<!-- Simple list + detail -->
<template v-for="post in posts">
  <router-link :to="`/post/${post.slug}`">{{ post.title }}</router-link>
</template>
```

### v2 — Markdown + 分页
```ts
// Added markdown rendering + pagination composable
const { data, loading, changePage } = usePagination(postApi.getList)
```

### v3 — 完整博客系统
```ts
// Cache + TOC + search + responsive + dark mode
const postStore = usePostStore() // Pinia cache
const { html, headings } = renderMarkdown(post.content) // TOC
const { results, search } = useSearch(postApi.search) // Search
// Dark mode via CSS variables + usePreferredDark()
```
