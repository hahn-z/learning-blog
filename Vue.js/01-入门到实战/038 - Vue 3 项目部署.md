---
title: "038 - Vue 3 项目部署"
slug: "038-deployment"
category: "Vue.js入门到实战"
tech_stack: "Vue.js"
created_at: "2026-04-26T17:58:50.444+08:00"
updated_at: "2026-04-29T10:02:46.83+08:00"
reading_time: 22
tags: ["Vue.js", "Vue 3"]
---


## 难度标注
> **难度：🟡 中级** | **前置知识：Vue 3、命令行基础、Linux 基础** | **预计学习时间：50 分钟**

## 概念讲解

### Vue 3 部署架构

Vue SPA 应用的部署本质上是：**将构建产物（HTML/CSS/JS）放到 Web 服务器上**。

```
开发环境 → npm run build → dist/ (静态文件) → Web 服务器 (Nginx/CDN)
```

### 部署方案对比

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| Nginx 直接部署 | 简单、性能好 | 手动操作 | 小型项目 |
| Docker | 标准化、可复制 | 需要 Docker 知识 | 团队协作 |
| Vercel / Netlify | 零配置、自动部署 | 定制性有限 | 个人/小项目 |
| OSS + CDN | 全球加速 | 配置复杂 | 高流量项目 |
| Kubernetes | 弹性伸缩 | 复杂度高 | 大型企业 |

## 脑图

```
Vue 3 部署
├── 构建优化
│   ├── 代码分割 (dynamic import)
│   ├── Tree Shaking
│   ├── 压缩 (Terser)
│   ├── Gzip/Brotli
│   └── 资源哈希
├── Web 服务器
│   ├── Nginx 配置
│   ├── SPA 路由回退
│   ├── HTTPS / SSL
│   └── 缓存策略
├── 容器化
│   ├── Dockerfile (多阶段构建)
│   ├── Docker Compose
│   └── 镜像优化
├── CI/CD
│   ├── GitHub Actions
│   ├── 自动构建 + 部署
│   └── 环境变量注入
└── 监控
    ├── 错误上报 (Sentry)
    ├── 性能监控
    └── 日志收集
```

## 完整代码

### Vite 构建配置

```ts
// vite.config.ts
import { defineConfig, loadEnv } from 'vite'
import vue from '@vitejs/plugin-vue'
import { resolve } from 'path'

export default defineConfig(({ mode }) => {
  const env = loadEnv(mode, process.cwd())

  return {
    plugins: [vue()],
    resolve: {
      alias: { '@': resolve(__dirname, 'src') }
    },
    build: {
      // Output directory
      outDir: 'dist',
      // Enable source map for production debugging
      sourcemap: false,
      // Chunk size warning limit
      chunkSizeWarningLimit: 1000,
      // Rollup options for manual chunk splitting
      rollupOptions: {
        output: {
          // Manual chunks to avoid large bundles
          manualChunks: {
            'vue-vendor': ['vue', 'vue-router', 'pinia'],
            'element-plus': ['element-plus'],
            'echarts': ['echarts'],
            'utils': ['axios', 'lodash-es']
          },
          // Asset file naming with content hash
          chunkFileNames: 'js/[name]-[hash].js',
          entryFileNames: 'js/[name]-[hash].js',
          assetFileNames: '[ext]/[name]-[hash].[ext]'
        }
      },
      // Minification
      minify: 'terser',
      terserOptions: {
        compress: {
          drop_console: true,  // Remove console.log
          drop_debugger: true
        }
      }
    },
    // Proxy for development API
    server: {
      proxy: {
        '/api': {
          target: env.VITE_API_BASE_URL,
          changeOrigin: true
        }
      }
    }
  }
})
```

### 环境变量管理

```bash
# .env (all environments)
VITE_APP_TITLE=My App

# .env.development
VITE_API_BASE_URL=http://localhost:8080
VITE_WS_URL=ws://localhost:8080/ws

# .env.staging
VITE_API_BASE_URL=https://staging-api.example.com
VITE_WS_URL=wss://staging-api.example.com/ws

# .env.production
VITE_API_BASE_URL=https://api.example.com
VITE_WS_URL=wss://api.example.com/ws
```

### Nginx 配置

```nginx
# /etc/nginx/conf.d/vue-app.conf
server {
    listen 80;
    server_name example.com;
    # Redirect HTTP to HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name example.com;

    # SSL certificates
    ssl_certificate /etc/letsencrypt/live/example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/example.com/privkey.pem;

    # Static files root
    root /var/www/vue-app/dist;
    index index.html;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml;
    gzip_min_length 1000;

    # SPA fallback: all routes → index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Static assets: aggressive caching (hashed filenames)
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    # API proxy
    location /api/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # WebSocket proxy
    location /ws {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Security headers
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Disable caching for index.html (always get latest)
    location = /index.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate";
        try_files $uri =404;
    }
}
```

### Docker 多阶段构建

```dockerfile
# Dockerfile

# Stage 1: Build
FROM node:20-alpine AS builder
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
ARG VITE_API_BASE_URL
ENV VITE_API_BASE_URL=$VITE_API_BASE_URL
RUN npm run build

# Stage 2: Serve (Nginx)
FROM nginx:alpine
# Copy custom nginx config
COPY nginx.conf /etc/nginx/conf.d/default.conf
# Copy built assets from builder stage
COPY --from=builder /app/dist /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

```yaml
# docker-compose.yml
version: '3.8'
services:
  vue-app:
    build:
      context: .
      args:
        VITE_API_BASE_URL: https://api.example.com
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./ssl:/etc/nginx/ssl:ro
    restart: unless-stopped
```

### GitHub Actions CI/CD

```yaml
# .github/workflows/deploy.yml
name: Deploy Vue App

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci
      - run: npm run lint
      - run: npm run test
      - run: npm run build
        env:
          VITE_API_BASE_URL: ${{ secrets.API_BASE_URL }}

      - name: Deploy to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: "dist/*"
          target: "/var/www/vue-app/dist"

      - name: Reload Nginx
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: sudo systemctl reload nginx
```

## 执行预览

```bash
# Local build
$ npm run build
vite v5.4.0 building for production...
✓ 89 modules transformed.
dist/index.html                   0.46 kB │ gzip: 0.30 kB
dist/js/vue-vendor-a1b2c3.js     142.30 kB │ gzip: 45.67 kB
dist/js/element-plus-d4e5f6.js   234.50 kB │ gzip: 78.90 kB
dist/js/index-g7h8i9.js           56.20 kB │ gzip: 18.34 kB
dist/css/index-j0k1l2.css          12.30 kB │ gzip: 3.45 kB
✓ built in 3.2s

# Docker build and run
$ docker build -t vue-app .
$ docker run -p 80:80 vue-app

# Visit https://example.com → loads SPA
```

## 注意事项

| 事项 | 说明 | 建议 |
|------|------|------|
| SPA 路由 | 刷新 404 | Nginx `try_files $uri /index.html` |
| 缓存策略 | index.html 不缓存，JS/CSS 长缓存 | 哈希文件名 + 1年缓存 |
| 环境变量 | VITE_ 前缀才暴露 | 不要放敏感信息在 VITE_ 变量 |
| HTTPS | 生产必须 | Let's Encrypt 免费证书 |
| Gzip | 减小传输体积 | Nginx 开启 gzip 或预生成 .gz |
| Source Map | 生产环境关闭 | 防止源码泄露 |

## 避坑指南

- ❌ 部署后刷新页面 404 → ✅ Nginx 配置 SPA fallback
- ❌ 更新后用户看到旧版本 → ✅ index.html 不缓存 + 文件名哈希
- ❌ API 请求跨域报错 → ✅ Nginx 代理 `/api/` 或后端配 CORS
- ❌ 构建产物太大 → ✅ 手动分包 + Tree Shaking + 按需引入
- ❌ 敏感信息放 VITE_ 变量 → ✅ 只放公开配置，密钥放后端
- ❌ 不做 Gzip 压缩 → ✅ Nginx 开启 gzip 或 vite-plugin-compression

## 练习题

### 🟢 基础
1. 配置 Vite 构建参数，实现代码分割和压缩
2. 编写 Nginx 配置文件，支持 SPA 路由和静态资源缓存

### 🟡 进阶
3. 编写 Dockerfile 多阶段构建，镜像大小 < 50MB
4. 配置 GitHub Actions，push 到 main 自动部署

### 🔴 挑战
5. 实现蓝绿部署：两个版本交替上线，零停机
6. 配置 CDN 加速：静态资源上传 OSS + CDN 域名

## 知识点总结

```
部署知识树
├── 构建
│   ├── vite build
│   ├── 代码分割
│   ├── Tree Shaking
│   └── 压缩优化
├── 服务器
│   ├── Nginx 配置
│   ├── SPA Fallback
│   ├── 缓存策略
│   ├── HTTPS/SSL
│   └── Gzip 压缩
├── 容器化
│   ├── Dockerfile
│   ├── 多阶段构建
│   └── Docker Compose
├── CI/CD
│   ├── GitHub Actions
│   ├── 自动构建
│   └── 自动部署
└── 监控
    ├── 错误上报
    └── 性能监控
```

## 举一反三

| 场景 | 方案 | 关键配置 |
|------|------|----------|
| VPS 部署 | Nginx + PM2 | try_files + proxy_pass |
| Docker 部署 | 多阶段构建 | nginx:alpine 镜像 |
| 云平台 | Vercel/Netlify | 零配置，Git push 即部署 |
| OSS + CDN | 静态资源上云 | 哈希文件名 + 长缓存 |
| 多环境 | .env.staging/.env.production | VITE_ 变量 + CI 注入 |

## 参考资料

- [Vite 构建优化](https://vitejs.dev/guide/build.html)
- [Nginx 官方文档](https://nginx.org/en/docs/)
- [GitHub Actions 文档](https://docs.github.com/en/actions)

## 代码演进

### v1 — 手动部署
```bash
npm run build
scp -r dist/* user@server:/var/www/html/
ssh user@server "sudo systemctl reload nginx"
```

### v2 — Docker 部署
```bash
docker build -t vue-app .
docker run -d -p 80:80 vue-app
```

### v3 — CI/CD 自动化
```yaml
# Push to main → auto build + test + deploy
# Docker multi-stage build for minimal image
# Nginx with optimized caching + Gzip + HTTPS
```
