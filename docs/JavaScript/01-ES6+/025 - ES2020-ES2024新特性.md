---
title: "025 - ES2020-ES2024新特性"
slug: "025-js-new-features"
category: "JavaScript ES6+"
tech_stack: "JavaScript"
created_at: "2026-04-26T14:21:20.076+08:00"
updated_at: "2026-04-29T10:02:46.308+08:00"
reading_time: 26
tags: []
---

# ES2020-ES2024新特性

> **难度：** ⭐⭐⭐ 中等 | **适合：** 想跟上ECMAScript演进的开发者
> **阅读时间：** 约25分钟 | **代码量：** 200+ 行

---

## 一、概念讲解

ECMAScript从2015年开始每年发布一个新版本。本文聚焦 **ES2020~ES2024** 的实用新特性，按版本梳理。

### 各版本核心特性一览

| 版本 | 核心特性 | 实用性 |
|------|---------|--------|
| ES2020 | Optional Chaining, Nullish Coalescing, BigInt, dynamic import, Promise.allSettled | ⭐⭐⭐⭐⭐ |
| ES2021 | Logical Assignment, Numeric Separators, Promise.any, WeakRef, AggregateError | ⭐⭐⭐⭐ |
| ES2022 | Top-level await, at(), Object.hasOwn, Error Cause, class fields | ⭐⭐⭐⭐⭐ |
| ES2023 | Array group/groupToSorted, Change Array by Copy, Hashbang Grammar | ⭐⭐⭐ |
| ES2024 | Grouping / Well-Formed Unicode / Promise.withResolvers | ⭐⭐⭐ |

---

## 二、脑图（ASCII）

```
            ES2020-ES2024新特性
                    │
     ┌──────────────┼──────────────┐
     │              │              │
  语法糖         异步增强        数据类型
     │              │              │
  ┌──┼──┐      ┌───┼───┐     ┌───┼───┐
  │  │  │      │   │   │     │   │   │
 opt null log  top  P.  P.  Big at() Obj
 ion  ish  ical lev any all Int     .has
 chn  coal ass  awa it   Set   Own
 ain  esc  ign  it   led
```

---

## 三、完整代码示例

### 3.1 ES2020: Optional Chaining & Nullish Coalescing

```javascript
// Optional Chaining (?.) - safe property access
const user = { address: { street: 'Main St' } };

// Before ES2020
const zip1 = user && user.address && user.address.zip;

// After ES2020
const zip2 = user?.address?.zip; // undefined, no error
const name = user?.profile?.name?.length ?? 0; // 0

// Works with methods and arrays
const result = data?.items?.[0]?.getName?.() ?? 'default';

// Nullish Coalescing (??) - only null/undefined trigger fallback
const count = 0 ?? 10;   // 0 (not 10!)
const name = '' ?? 'N/A'; // '' (not 'N/A!')
const val = null ?? 10;   // 10
const val2 = undefined ?? 10; // 10

// Practical: config with defaults
function createConfig(options) {
  return {
    port: options?.port ?? 3000,
    host: options?.host ?? 'localhost',
    debug: options?.debug ?? false,
    timeout: options?.timeout ?? 5000,
  };
}
```

### 3.2 ES2020: BigInt & Dynamic Import

```javascript
// BigInt: arbitrary precision integers
const big = 9007199254740993n; // n suffix
const big2 = BigInt('9007199254740993');
console.log(big === big2); // true
console.log(big + 1n);     // 9007199254740994n

// BigInt cannot mix with Number
// big + 1; // TypeError! Must convert:
Number(big); // loses precision for large values

// Dynamic import: load modules on demand
async function loadEditor() {
  const { Editor } = await import('./editor.js');
  return new Editor(document.getElementById('editor'));
}

// Conditional loading
if (supportsWebGL()) {
  const { render3D } = await import('./webgl-renderer.js');
  render3D(scene);
} else {
  const { render2D } = await import('./canvas-renderer.js');
  render2D(scene);
}
```

### 3.3 ES2021: Logical Assignment & Promise.any

```javascript
// Logical Assignment Operators
let config = { debug: false, retries: 3 };

config.debug ||= true;       // ||= : assign if falsy
config.retries &&= 0;        // &&= : assign if truthy
config.name ??= 'default';   // ??= : assign if nullish

// Promise.any: resolve with first fulfilled promise
async function fetchFastest(mirrors) {
  try {
    const response = await Promise.any(
      mirrors.map(url => fetch(url).then(r => {
        if (!r.ok) throw new Error(`${r.status}`);
        return r;
      }))
    );
    return response;
  } catch (aggregateError) {
    // All promises rejected
    console.error('All mirrors failed:');
    aggregateError.errors.forEach((e, i) => console.error(`  Mirror ${i}:`, e.message));
    throw aggregateError;
  }
}

// Numeric Separators: improve readability
const budget = 1_000_000;     // 1000000
const bytes = 0xFF_FF_FF_FF;  // 4294967295
const binary = 0b1010_0001;   // 161
```

### 3.4 ES2022: Top-level await, Object.hasOwn, Error Cause

```javascript
// Top-level await (in ES modules)
// config.mjs
const response = await fetch('/api/config');
export const config = await response.json();

// Object.hasOwn: safer than hasOwnProperty
const obj = Object.create(null); // no prototype
obj.name = 'test';
// obj.hasOwnProperty('name'); // TypeError! No prototype
Object.hasOwn(obj, 'name'); // true - works everywhere

// Error Cause: chain errors with context
try {
  await connectDatabase();
} catch (originalError) {
  throw new Error('Failed to initialize database', {
    cause: originalError, // Link to original error
  });
}

// Access the chain
// err.message → 'Failed to initialize database'
// err.cause.message → 'ECONNREFUSED'
// err.cause.cause?.message → deeper errors...

// Array.prototype.at(): negative indexing
const arr = [10, 20, 30, 40, 50];
arr.at(0);    // 10 (first)
arr.at(-1);   // 50 (last)
arr.at(-2);   // 40 (second to last)

// Class Fields (public & private)
class Timer {
  #startTime = null;       // Private field
  #running = false;        // Private field
  interval = 1000;         // Public field

  get isRunning() { return this.#running; }

  start() {
    this.#startTime = Date.now();
    this.#running = true;
  }

  elapsed() {
    if (!this.#running) return 0;
    return Date.now() - this.#startTime;
  }
}
```

### 3.5 ES2023-2024: Array Grouping & Promise.withResolvers

```javascript
// ES2023: Array.prototype.group()
const products = [
  { name: 'Apple', category: 'fruit' },
  { name: 'Carrot', category: 'vegetable' },
  { name: 'Banana', category: 'fruit' },
];

const grouped = Object.groupBy(products, p => p.category);
// { fruit: [{name:'Apple',...}, {name:'Banana',...}], vegetable: [{name:'Carrot',...}] }

// ES2023: Change Array by Copy (non-mutating methods)
const original = [3, 1, 4, 1, 5];
const sorted = original.toSorted();    // [1, 1, 3, 4, 5] - original unchanged
const reversed = original.toReversed(); // [5, 1, 4, 1, 3]
const spliced = original.toSpliced(1, 2, 9); // [3, 9, 1, 5]
const withNew = original.with(0, 99);  // [99, 1, 4, 1, 5]

// ES2024: Promise.withResolvers
function createDeferred() {
  const { promise, resolve, reject } = Promise.withResolvers();
  return { promise, resolve, reject };
}

// Usage: resolve/reject from outside the executor
const { promise, resolve } = Promise.withResolvers();
setTimeout(() => resolve('done!'), 1000);
const result = await promise; // 'done!' after 1s

// ES2024: Well-Formed Unicode Strings
'\uD800'.isWellFormed(); // false (lone surrogate)
'hello'.isWellFormed();  // true
'\uD800'.toWellFormed(); // '\uFFFD' (replacement char)
```

---

## 四、执行预览

```
ES2020-ES2024 Features Demo
============================

ES2020:
  user?.address?.zip → undefined (no crash)
  0 ?? 10 → 0 (nullish coalescing preserves 0)
  1_000_000n + 1n → 1000001n (BigInt)

ES2021:
  config.debug ||= true → true (was false)
  Promise.any([fetch(url1), fetch(url2)]) → first success

ES2022:
  arr.at(-1) → last element
  Object.hasOwn(obj, 'key') → true
  new Error('msg', { cause: original })

ES2023-2024:
  Object.groupBy(items, fn) → grouped object
  arr.toSorted() → sorted copy
  Promise.withResolvers() → { promise, resolve, reject }
```

---

## 五、注意事项

| 特性 | 注意点 | 建议 |
|------|--------|------|
| Optional Chaining | 不能用于赋值左侧 | `obj?.foo = 1` ❌ |
| Nullish Coalescing | 运算符优先级低于 `||` | 必要时加括号 |
| BigInt | 不能与Number混算 | 显式转换 `BigInt(n)` 或 `Number(b)` |
| Top-level await | 仅在ES模块中可用 | `<script type="module">` |
| Private字段 | `#` 是语法的一部分 | 运行时无法动态访问 |
| `Promise.any` | 全部失败返回 `AggregateError` | 需处理 `errors` 数组 |

---

## 六、避坑指南

| ❌ 错误做法 | ✅ 正确做法 | 原因 |
|------------|-----------|------|
| `a?.b = c` | `if (a) a.b = c` | Optional chaining不可赋值 |
| `value ?? default` 当 `value` 是 `0` 或 `''` | 用 `??` 就是对的，`||` 才会误判 | `??` 只对 null/undefined 生效 |
| `BigInt(number)` 超过安全整数 | 用字符串 `BigInt('9007...')` | 超过 Number.MAX_SAFE_INTEGER 精度丢失 |
| `Promise.any` 不处理全失败 | 用 try/catch 处理 AggregateError | 否则未捕获异常 |
| 滥用 top-level await 阻塞模块 | 只在确实需要同步初始化时使用 | 阻塞整个模块图 |
| `private` 关键字（TS） | JS用 `#` 前缀 | JS原生私有字段是 `#` |

---

## 七、练习题

### 🟢 基础
1. 用 Optional Chaining 重写 `user && user.profile && user.profile.avatar`
2. 用 `??` 给函数参数设置默认值，确保 `0` 和 `''` 不被覆盖
3. 使用 `BigInt` 计算 `2n ** 100n`

### 🟡 进阶
4. 实现一个带超时的 fetch，用 `Promise.any` 竞速 fetch 和 setTimeout
5. 用 `Object.groupBy` 将学生按班级分组
6. 使用 `Promise.withResolvers` 实现一个一次性事件监听器

### 🔴 挑战
7. 用 Private Fields + WeakRef 实现一个自动清理的缓存类
8. 用 top-level await + dynamic import 实现按环境加载不同配置

---

## 八、知识点总结

```
ES2020-ES2024新特性
├── ES2020
│   ├── Optional Chaining (?.)
│   ├── Nullish Coalescing (??)
│   ├── BigInt
│   ├── Dynamic Import
│   └── Promise.allSettled
├── ES2021
│   ├── Logical Assignment (||=, &&=, ??=)
│   ├── Numeric Separators (1_000)
│   ├── Promise.any + AggregateError
│   └── WeakRef + FinalizationRegistry
├── ES2022
│   ├── Top-level await
│   ├── Array.prototype.at()
│   ├── Object.hasOwn()
│   ├── Error Cause
│   └── Class Fields (#private)
├── ES2023
│   ├── Object.groupBy / Map.groupBy
│   ├── toSorted/toReversed/toSpliced/with
│   └── Hashbang Grammar
└── ES2024
    ├── Promise.withResolvers
    ├── String.isWellFormed/toWellFormed
    └── RegExp v flag
```

---

## 九、举一反三

| ES新特性 | 类似概念（其他语言） | 对比 |
|---------|-------------------|------|
| Optional Chaining | C# `?.` / Kotlin `?.` | 语法和语义几乎一致 |
| Nullish Coalescing | C# `??` / Python `or` | JS区分nullish和falsy |
| BigInt | Python int (任意精度) | Python默认支持，JS需后缀 |
| Pattern Matching (proposal) | Rust/Swift match | JS还在提案阶段 |
| Private Fields `#` | Java `private` / Python `_` | JS是运行时强制，Python是约定 |

---

## 十、参考资料

- [ECMAScript Proposals](https://github.com/tc39/proposals)
- [MDN - JavaScript Reference](https://developer.mozilla.org/zh-CN/docs/Web/JavaScript/Reference)
- [ES Compatibility Table](https://compat-table.github.io/compat-table/es2016plus/)
- [V8 - ES Features](https://v8.dev/features)

---

## 十一、代码演进

### v1: ES2020前的安全访问

```javascript
// Verbose null checks
const zip = user && user.address && user.address.zip;
const name = (user && user.profile && user.profile.name) || 'Anonymous';
```

### v2: ES2020简化

```javascript
// Optional chaining + nullish coalescing
const zip = user?.address?.zip;
const name = user?.profile?.name ?? 'Anonymous';
```

### v3: ES2022+ 完整现代化

```javascript
// Private fields, error cause, top-level await
class UserService {
  #cache = new Map();

  async getUser(id) {
    try {
      return await this.#fetchUser(id);
    } catch (e) {
      throw new AppError('User fetch failed', { cause: e });
    }
  }
}

// Top-level await in module
export const config = await loadConfig();
```

---

## 十二、总结

ES2020-ES2024带来了大量**提升代码质量和开发体验**的特性。Optional Chaining和Nullish Coalescing是日常用得最多的；Private Fields让封装更正规；Top-level await简化异步模块；Array grouping和immutable方法让数据处理更优雅。保持学习新特性，但也要注意**浏览器兼容性**——必要时用Babel转译。
