---
title: "033-多服务协作实战"
slug: "033-kratos-multi-service"
category: "Kratos框架"
tech_stack: "Golang"
created_at: "2026-04-25T19:31:43.52+08:00"
updated_at: "2026-04-29T10:02:45.538+08:00"
reading_time: 40
tags: []
---

# 033-多服务协作实战

> 难度：⭐⭐⭐⭐⭐（高级实战篇）
> 适用：全栈工程师、微服务开发者、系统架构师
> 前置知识：Kratos完整基础、gRPC、Docker、数据库基础

---

## 一、概念讲解

### 1.1 微服务协作的挑战

在真实电商场景中，一个下单操作涉及**用户服务、商品服务、订单服务、库存服务、支付服务**的协同。核心挑战：

| 挑战 | 说明 |
|------|------|
| 服务间通信 | HTTP vs gRPC，同步 vs 异步 |
| 数据一致性 | 跨服务的数据如何保持一致 |
| 服务发现 | 服务实例动态变化，如何互相找到 |
| 链路追踪 | 一次请求跨多个服务，如何追踪 |
| 故障隔离 | 一个服务挂了，不影响整体 |

### 1.2 分布式事务方案

| 方案 | 适用场景 | 复杂度 | 一致性 |
|------|---------|--------|--------|
| 2PC (两阶段提交) | 数据库层面 | 中 | 强一致 |
| TCC (Try-Confirm-Cancel) | 资源预留型业务 | 高 | 最终一致 |
| Saga (编排式) | 长事务、异步流程 | 中 | 最终一致 |
| 本地消息表 | 简单可靠场景 | 低 | 最终一致 |

### 1.3 项目架构

```
┌────────────────────────────────────────────┐
│                  Client                     │
└───────────────────┬────────────────────────┘
                    │
              ┌─────▼─────┐
              │ API Gateway│ (APISIX/Kong)
              └─────┬─────┘
       ┌────────────┼────────────┐
       v            v            v
 ┌──────────┐ ┌──────────┐ ┌──────────┐
 │  User    │ │  Order   │ │ Product  │
 │ Service  │ │ Service  │ │ Service  │
 │ :8001    │ │ :8002    │ │ :8003    │
 │ gRPC:9001│ │ gRPC:9002│ │ gRPC:9003│
 └──────────┘ └──────────┘ └──────────┘
       │            │            │
       v            v            v
  [PostgreSQL] [PostgreSQL] [MySQL]
                    │
              ┌─────▼─────┐
              │   Kafka    │ (异步事件)
              └───────────┘
```

---

## 二、脑图

```
多服务协作实战
├── 服务设计
│   ├── 用户服务 (User)
│   │   ├── 注册/登录
│   │   ├── 用户信息
│   │   └── 地址管理
│   ├── 商品服务 (Product)
│   │   ├── 商品CRUD
│   │   ├── 库存管理
│   │   └── 分类管理
│   └── 订单服务 (Order)
│       ├── 创建订单
│       ├── 订单状态
│       └── 订单查询
├── 服务通信
│   ├── gRPC (服务间同步调用)
│   ├── HTTP (对外API)
│   └── Kafka (异步事件)
├── 数据一致性
│   ├── 本地消息表
│   ├── Saga编排
│   └── 补偿事务
└── 基础设施
    ├── 服务发现 (Consul/etcd)
    ├── 链路追踪 (OpenTelemetry)
    ├── Docker Compose
    └── 数据库迁移
```

---

## 三、完整Go代码

### 3.1 项目结构

```
ecommerce/
├── api/                    # Protobuf definitions
│   ├── user/v1/
│   ├── order/v1/
│   └── product/v1/
├── cmd/
│   ├── user/main.go
│   ├── order/main.go
│   └── product/main.go
├── internal/
│   ├── user/              # User service
│   │   ├── service.go
│   │   ├── repository.go
│   │   └── biz/
│   ├── order/             # Order service
│   │   ├── service.go
│   │   ├── repository.go
│   │   └── saga.go
│   └── product/           # Product service
│       ├── service.go
│       └── repository.go
├── pkg/
│   ├── event/             # Event publishing
│   └── middleware/         # Shared middleware
└── docker-compose.yml
```

### 3.2 Protobuf定义

```protobuf
// api/order/v1/order.proto
syntax = "proto3";
package order.v1;

option go_package = "ecommerce/api/order/v1";

import "google/api/annotations.proto";
import "google/protobuf/empty.proto";

service OrderService {
  rpc CreateOrder(CreateOrderRequest) returns (CreateOrderReply) {
    option (google.api.http) = {post: "/v1/orders"};
  }
  rpc GetOrder(GetOrderRequest) returns (OrderReply) {
    option (google.api.http) = {get: "/v1/orders/{id}"};
  }
  rpc ListOrders(ListOrdersRequest) returns (ListOrdersReply) {
    option (google.api.http) = {get: "/v1/orders"};
  }
  rpc CancelOrder(CancelOrderRequest) returns (google.protobuf.Empty) {
    option (google.api.http) = {put: "/v1/orders/{id}/cancel"};
  }
}

message CreateOrderRequest {
  string user_id = 1;
  repeated OrderItem items = 2;
}

message OrderItem {
  string product_id = 1;
  int32 quantity = 2;
}

message CreateOrderReply {
  string id = 1;
  string status = 2;
}

message GetOrderRequest { string id = 1; }

message OrderReply {
  string id = 1;
  string user_id = 2;
  string status = 3;
  repeated OrderItemReply items = 4;
  float total_amount = 5;
  string created_at = 6;
}

message OrderItemReply {
  string product_id = 1;
  string product_name = 2;
  int32 quantity = 3;
  float price = 4;
}

message ListOrdersRequest {
  string user_id = 1;
  int32 page = 2;
  int32 page_size = 3;
}

message ListOrdersReply {
  repeated OrderReply orders = 1;
  int32 total = 2;
}

message CancelOrderRequest { string id = 1; }
```

### 3.3 订单服务实现（含多服务协作）

```go
// internal/order/service.go - Order service with multi-service collaboration
package order

import (
	"context"
	"fmt"
	"log"
	"time"

	pb "ecommerce/api/order/v1"
	pbProduct "ecommerce/api/product/v1"
	pbUser "ecommerce/api/user/v1"

	"github.com/go-kratos/kratos/v2/middleware/recovery"
	"google.golang.org/grpc"
)

// OrderServiceImpl implements OrderService
type OrderServiceImpl struct {
	pb.UnimplementedOrderServiceServer

	repo          OrderRepository
	productClient pbProduct.ProductServiceClient
	userClient    pbUser.UserServiceClient
	eventPublisher EventPublisher
}

// NewOrderService creates a new order service
func NewOrderService(
	repo OrderRepository,
	productConn *grpc.ClientConn,
	userConn *grpc.ClientConn,
	publisher EventPublisher,
) *OrderServiceImpl {
	return &OrderServiceImpl{
		repo:           repo,
		productClient:  pbProduct.NewProductServiceClient(productConn),
		userClient:     pbUser.NewUserServiceClient(userConn),
		eventPublisher: publisher,
	}
}

// CreateOrder handles order creation with multi-service collaboration
// Flow: validate user → check stock → create order → deduct stock → emit event
func (s *OrderServiceImpl) CreateOrder(ctx context.Context, req *pb.CreateOrderRequest) (*pb.CreateOrderReply, error) {
	// Step 1: Validate user exists
	user, err := s.userClient.GetUser(ctx, &pbUser.GetUserRequest{Id: req.UserId})
	if err != nil {
		return nil, fmt.Errorf("user validation failed: %w", err)
	}
	log.Printf("User validated: %s (%s)", user.Name, req.UserId)

	// Step 2: Check product availability and get prices
	productIDs := make([]string, 0, len(req.Items))
	for _, item := range req.Items {
		productIDs = append(productIDs, item.ProductId)
	}
	products, err := s.productClient.BatchGetProducts(ctx, &pbProduct.BatchGetRequest{Ids: productIDs})
	if err != nil {
		return nil, fmt.Errorf("product query failed: %w", err)
	}

	// Validate stock availability
	for _, item := range req.Items {
		product := findProduct(products.Products, item.ProductId)
		if product == nil {
			return nil, fmt.Errorf("product %s not found", item.ProductId)
		}
		if product.Stock < item.Quantity {
			return nil, fmt.Errorf("product %s insufficient stock: have %d, need %d",
				item.ProductId, product.Stock, item.Quantity)
		}
	}

	// Step 3: Calculate total and create order in DB
	order := &Order{
		UserID:    req.UserId,
		Status:    "pending",
		Items:     make([]OrderItem, 0, len(req.Items)),
		CreatedAt: time.Now(),
	}
	var total float64
	for _, item := range req.Items {
		product := findProduct(products.Products, item.ProductId)
		order.Items = append(order.Items, OrderItem{
			ProductID:   item.ProductId,
			ProductName: product.Name,
			Quantity:    item.Quantity,
			Price:       product.Price,
		})
		total += product.Price * float64(item.Quantity)
	}
	order.TotalAmount = total

	if err := s.repo.Create(ctx, order); err != nil {
		return nil, fmt.Errorf("create order failed: %w", err)
	}

	// Step 4: Deduct stock (synchronous call with compensation on failure)
	for _, item := range req.Items {
		_, err := s.productClient.DeductStock(ctx, &pbProduct.DeductStockRequest{
			ProductId: item.ProductId,
			Quantity:  item.Quantity,
		})
		if err != nil {
			// Compensation: cancel the order
			s.repo.UpdateStatus(ctx, order.ID, "cancelled")
			return nil, fmt.Errorf("stock deduction failed for %s: %w", item.ProductId, err)
		}
	}

	// Step 5: Update order status and emit event
	s.repo.UpdateStatus(ctx, order.ID, "confirmed")
	s.eventPublisher.Publish(ctx, "order.created", map[string]interface{}{
		"order_id":  order.ID,
		"user_id":   req.UserId,
		"total":     total,
		"item_count": len(req.Items),
	})

	log.Printf("Order created: id=%s user=%s total=%.2f", order.ID, user.Name, total)

	return &pb.CreateOrderReply{
		Id:     order.ID,
		Status: "confirmed",
	}, nil
}

// GetOrder returns order details
func (s *OrderServiceImpl) GetOrder(ctx context.Context, req *pb.GetOrderRequest) (*pb.OrderReply, error) {
	order, err := s.repo.GetByID(ctx, req.Id)
	if err != nil {
		return nil, fmt.Errorf("order not found: %w", err)
	}
	return orderToReply(order), nil
}

// CancelOrder cancels an order and restores stock
func (s *OrderServiceImpl) CancelOrder(ctx context.Context, req *pb.CancelOrderRequest) (*pb.Empty, error) {
	order, err := s.repo.GetByID(ctx, req.Id)
	if err != nil {
		return nil, fmt.Errorf("order not found: %w", err)
	}
	if order.Status == "cancelled" {
		return nil, fmt.Errorf("order already cancelled")
	}

	// Restore stock for each item
	for _, item := range order.Items {
		_, err := s.productClient.RestoreStock(ctx, &pbProduct.RestoreStockRequest{
			ProductId: item.ProductID,
			Quantity:  item.Quantity,
		})
		if err != nil {
			log.Printf("Warning: stock restore failed for %s: %v", item.ProductID, err)
			// Continue anyway - can be reconciled later
		}
	}

	// Update order status
	if err := s.repo.UpdateStatus(ctx, req.Id, "cancelled"); err != nil {
		return nil, err
	}

	// Emit cancellation event
	s.eventPublisher.Publish(ctx, "order.cancelled", map[string]interface{}{
		"order_id": req.Id,
	})

	return &pb.Empty{}, nil
}

func findProduct(products []*pbProduct.ProductReply, id string) *pbProduct.ProductReply {
	for _, p := range products {
		if p.Id == id {
			return p
		}
	}
	return nil
}
```

### 3.4 Docker Compose配置

```yaml
# docker-compose.yml
version: '3.8'
services:
  # User Service
  user-service:
    build: ./cmd/user
    ports:
      - "8001:8000"
      - "9001:9000"
    environment:
      - DB_HOST=user-db
      - DB_PORT=5432
      - DB_NAME=users
      - CONSUL_ADDR=consul:8500
    depends_on:
      - user-db
      - consul

  # Order Service
  order-service:
    build: ./cmd/order
    ports:
      - "8002:8000"
      - "9002:9000"
    environment:
      - DB_HOST=order-db
      - DB_PORT=5432
      - DB_NAME=orders
      - CONSUL_ADDR=consul:8500
      - KAFKA_BROKERS=kafka:9092
      - PRODUCT_SERVICE_ADDR=product-service:9000
      - USER_SERVICE_ADDR=user-service:9000
    depends_on:
      - order-db
      - consul
      - kafka

  # Product Service
  product-service:
    build: ./cmd/product
    ports:
      - "8003:8000"
      - "9003:9000"
    environment:
      - DB_HOST=product-db
      - DB_PORT=3306
      - DB_NAME=products
      - CONSUL_ADDR=consul:8500
    depends_on:
      - product-db
      - consul

  # Databases
  user-db:
    image: postgres:15
    environment:
      POSTGRES_DB: users
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
    volumes:
      - user-db-data:/var/lib/postgresql/data

  order-db:
    image: postgres:15
    environment:
      POSTGRES_DB: orders
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: secret
    volumes:
      - order-db-data:/var/lib/postgresql/data

  product-db:
    image: mysql:8
    environment:
      MYSQL_DATABASE: products
      MYSQL_ROOT_PASSWORD: secret

  # Infrastructure
  consul:
    image: consul:1.15
    ports:
      - "8500:8500"

  kafka:
    image: bitnami/kafka:3.5
    ports:
      - "9092:9092"
    environment:
      - KAFKA_CFG_NODE_ID=1
      - KAFKA_CFG_PROCESS_ROLES=broker,controller
      - KAFKA_CFG_CONTROLLER_QUORUM_VOTERS=1@kafka:9093
      - ALLOW_PLAINTEXT_LISTENER=yes

volumes:
  user-db-data:
  order-db-data:
```

---

## 四、执行预览

```bash
# Start all services
$ docker-compose up -d
Creating network "ecommerce_default"
Creating ecommerce_consul_1 ... done
Creating ecommerce_kafka_1  ... done
Creating ecommerce_user-db_1 ... done
Creating ecommerce_product-db_1 ... done
Creating ecommerce_order-db_1 ... done
Creating ecommerce_user-service_1 ... done
Creating ecommerce_product-service_1 ... done
Creating ecommerce_order-service_1 ... done

# Verify services are registered in Consul
$ curl http://localhost:8500/v1/agent/services | jq 'keys'
["order-service", "product-service", "user-service"]

# Create a product
$ curl -X POST http://localhost:8003/v1/products -d '{
  "name": "Go编程圣经",
  "price": 99.9,
  "stock": 100
}'
{"id": "prod-1", "name": "Go编程圣经", "price": 99.9, "stock": 100}

# Create an order (multi-service collaboration)
$ curl -X POST http://localhost:8002/v1/orders -d '{
  "user_id": "user-1",
  "items": [
    {"product_id": "prod-1", "quantity": 2}
  ]
}'
{"id": "order-001", "status": "confirmed"}

# Check stock was deducted
$ curl http://localhost:8003/v1/products/prod-1
{"id": "prod-1", "stock": 98}  # Was 100, now 98

# Cancel the order (stock restored)
$ curl -X PUT http://localhost:8002/v1/orders/order-001/cancel
{}

# Check stock restored
$ curl http://localhost:8003/v1/products/prod-1
{"id": "prod-1", "stock": 100}  # Restored to 100
```

---

## 五、注意事项

| 注意点 | 说明 | 影响 |
|--------|------|------|
| 超时设置 | 每个gRPC调用都要设超时 | 防止级联阻塞 |
| 重试策略 | 幂等操作可重试，非幂等要谨慎 | 重复扣款风险 |
| 数据库隔离 | 每个服务独立数据库 | 数据一致性边界 |
| 服务发现 | 动态地址需注册中心 | 服务上下线感知 |
| 日志关联 | 全链路TraceID透传 | 排障效率 |
| 库存扣减 | 先扣后创建 vs 先创建后扣 | 并发安全策略 |
| 优雅停机 | 服务关闭时完成进行中的请求 | 数据完整性 |

---

## 六、避坑指南

| ❌ 错误做法 | ✅ 正确做法 |
|------------|-----------|
| 所有服务共享一个数据库 | 每个服务独立数据库，通过API通信 |
| 同步调用链过长（A→B→C→D） | 限制调用链深度，异步解耦长链路 |
| 不做超时控制 | 每层设置合理超时，使用context传递 |
| 忽略部分失败 | 库存扣减失败要回滚已成功的操作 |
| 服务间直接传DB对象 | 通过Protobuf定义清晰的API契约 |
| 不做服务发现 | 用Consul/etcd注册发现，不硬编码地址 |
| 单体思维设计API | 按领域拆分，每个服务有独立生命周期 |

---

## 七、练习题

### 🟢 初级

1. **服务拆分**：一个电商系统，你会如何划分微服务边界？依据是什么？
2. **Docker部署**：使用docker-compose启动上面三个服务，验证互相连通。
3. **gRPC调用**：写一个简单的gRPC客户端，调用商品服务的ListProducts接口。

### 🟡 中级

4. **创建订单**：完整实现CreateOrder接口，含用户验证、库存检查、价格计算。
5. **服务发现**：集成Consul，实现服务自动注册和发现。
6. **链路追踪**：集成OpenTelemetry，实现跨服务的请求追踪。

### 🔴 高级

7. **分布式事务**：实现本地消息表方案，保证订单创建和库存扣解的最终一致性。
8. **并发安全**：实现乐观锁库存扣减，处理高并发下的超卖问题。
9. **完整系统**：添加支付服务和通知服务，实现完整的电商下单流程。

---

## 八、知识点总结

```
多服务协作知识点
├── 服务设计
│   ├── 服务边界划分（按领域）
│   ├── 独立数据库（数据隔离）
│   └── API契约（Protobuf）
├── 服务通信
│   ├── 同步：gRPC（服务间）
│   ├── 对外：HTTP（客户端）
│   └── 异步：Kafka（事件）
├── 数据一致性
│   ├── 本地消息表
│   ├── Saga模式
│   └── 补偿事务
├── 基础设施
│   ├── 服务发现（Consul）
│   ├── 链路追踪（OpenTelemetry）
│   └── 容器编排（Docker Compose）
└── 最佳实践
    ├── 超时控制
    ├── 重试策略
    ├── 优雅停机
    └── 可观测性
```

---

## 九、举一反三

| 场景 | 服务划分 | 通信方式 | 关键挑战 |
|------|---------|---------|---------|
| 电商 | 用户/商品/订单/支付/库存 | gRPC+Kafka | 分布式事务 |
| 社交 | 用户/动态/评论/消息/推荐 | gRPC+消息队列 | Feed流聚合 |
| 外卖 | 用户/商家/菜品/订单/配送 | gRPC+WebSocket | 实时状态推送 |
| 在线教育 | 用户/课程/订单/直播/作业 | gRPC+流式 | 高并发直播 |
| SaaS | 租户/用户/权限/计费/通知 | gRPC+事件 | 多租户隔离 |

---

## 十、参考资料

1. [Kratos官方文档](https://go-kratos.dev/)
2. [微服务设计模式 - Microservices.io](https://microservices.io/patterns/)
3. [Consul服务发现](https://developer.hashicorp.com/consul/docs)
4. [OpenTelemetry Go](https://opentelemetry.io/docs/instrumentation/go/)
5. [Docker Compose文档](https://docs.docker.com/compose/)

---

## 十一、代码演进

### v1：单体应用
```go
// v1: Everything in one service, one database
func CreateOrder(db *sql.DB, req OrderReq) error {
    user := queryUser(db, req.UserID)
    product := queryProduct(db, req.ProductID)
    // Direct DB transaction for everything
    tx := db.Begin()
    tx.Exec("INSERT INTO orders ...")
    tx.Exec("UPDATE products SET stock = stock - ?", req.Quantity)
    tx.Commit()
}
```

### v2：微服务拆分 + 同步调用
```go
// v2: Separate services, synchronous gRPC calls
func (s *OrderService) CreateOrder(ctx context.Context, req *CreateReq) (*Order, error) {
    user := s.userClient.GetUser(ctx, &GetUserReq{Id: req.UserID})
    product := s.productClient.GetProduct(ctx, &GetProductReq{Id: req.ProductID})
    // Cross-service calls, but no transaction guarantees
    s.productClient.DeductStock(ctx, &DeductReq{...})
    s.repo.Create(ctx, order)
}
```

### v3：微服务 + 事件驱动 + 补偿事务
```go
// v3: Event-driven with saga compensation
func (s *OrderService) CreateOrder(ctx context.Context, req *CreateReq) (*Order, error) {
    // Create order in pending state
    order := s.repo.CreatePending(ctx, req)
    
    // Publish event for async processing
    s.eventBus.Publish(ctx, OrderCreatedEvent{OrderID: order.ID, Items: req.Items})
    
    // Saga coordinator handles: deduct stock → confirm → notify
    // With compensation: cancel → restore stock → notify cancellation
    return order, nil
}
```

**演进路线**：单体事务 → 微服务同步调用 → 事件驱动 + Saga补偿
