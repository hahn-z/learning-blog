---
title: "016 - 服务健康检查与自动摘除"
slug: "016-health-check"
category: "Hyperf微服务实战"
tech_stack: "Hyperf"
created_at: "2026-05-01T21:52:08.829+08:00"
updated_at: "2026-05-01T22:17:01.057+08:00"
reading_time: 7
tags: []
---

# 016 - 服务健康检查与自动摘除

> **难度：** ⭐⭐ | **前置知识：** 服务注册（014）、服务发现（015）| **预估用时：** 40 分钟

## 概念讲解

**一句话定义：** 健康检查是定期向服务实例发送探测请求，验证其是否正常响应，不健康的实例自动从注册中心摘除，避免流量打到故障节点。

**现实类比：** 就像医生定期体检——医生给你做检查（健康检查），结果正常就标记健康（healthy=true），有问题就建议休息（摘除）。同事发现你"病了"后就不会给你派活了。

**技术场景：** 用户服务实例进程崩溃、数据库连接失败或响应超时时，Nacos 通过健康检查标记为不健康，其他服务（订单服务）调用时自动跳过该实例，继续使用健康实例。

## 实时脑图

```
Nacos Server
    │
    │ heartbeat (every 5s)
    ▼
┌─────────────────────────────────────────────────┐
│              Health Check Engine                 │
│                                                  │
│  user-service instances:                         │
│  ├── 10.0.1.11:9501  healthy ✓  last beat: 1s   │
│  ├── 10.0.1.12:9501  healthy ✓  last beat: 3s   │
│  ├── 10.0.1.13:9501  healthy ✗  timeout > 15s   │ ← auto-deregister
│  └── 10.0.1.14:9501  healthy ✗  manual offline  │
└─────────────────────────────────────────────────┘
         │
         │ Discovery query
         ▼
Order Service
         │
         │ Only gets healthy instances
         ▼
┌─────────────────────────────────────────────┐
│ Load balancer picks:                        │
│  ┌──────────────────────┐                  │
│  │ 10.0.1.11:9501  ✓    │                  │
│  │ 10.0.1.12:9501  ✓    │                  │
│  └──────────────────────┘                  │
│  (10.0.1.13 and .14 excluded)               │
└─────────────────────────────────────────────┘
```

## 完整代码

### 健康检查控制器 — HealthController.php

```php
<?php

declare(strict_types=1);

namespace App\Controller;

use Hyperf\DbConnection\Db;
use Hyperf\Di\Annotation\Inject;
use Hyperf\HttpServer\Annotation\Controller;
use Hyperf\HttpServer\Annotation\GetMapping;
use Hyperf\Redis\Redis;
use Psr\Log\LoggerInterface;

#[Controller(prefix: '/health')]
class HealthController
{
    #[Inject]
    private LoggerInterface $logger;

    #[Inject]
    private Redis $redis;

    /**
     * ✅ Simple liveness check — process is running
     */
    #[GetMapping(path: '/live')]
    public function liveness(): array
    {
        return [
            'status' => 'UP',
            'timestamp' => time(),
            'service' => 'user-service',
        ];
    }

    /**
     * ✅ Readiness check — dependencies are ready
     */
    #[GetMapping(path: '/ready')]
    public function readiness(): array
    {
        $checks = [
            'database' => $this->checkDatabase(),
            'redis' => $this->checkRedis(),
            'nacos' => $this->checkNacos(),
        ];

        $allHealthy = !in_array(false, $checks, true);

        return [
            'status' => $allHealthy ? 'UP' : 'DOWN',
            'checks' => $checks,
            'timestamp' => time(),
        ];
    }

    /**
     * ✅ Detailed health check for monitoring
     */
    #[GetMapping(path: '/detail')]
    public function detail(): array
    {
        return [
            'status' => 'UP',
            'timestamp' => time(),
            'service' => 'user-service',
            'version' => env('APP_VERSION', '1.0.0'),
            'checks' => [
                'database' => $this->checkDatabase(),
                'redis' => $this->checkRedis(),
                'nacos' => $this->checkNacos(),
                'disk' => $this->checkDisk(),
            ],
            'metrics' => [
                'connections' => $this->getConnectionCount(),
                'memory_mb' => round(memory_get_usage(true) / 1024 / 1024, 2),
            ],
        ];
    }

    private function checkDatabase(): array
    {
        try {
            Db::select('SELECT 1');
            return ['status' => 'UP', 'latency_ms' => 5];
        } catch (\Throwable $e) {
            $this->logger->error('Database health check failed', ['error' => $e->getMessage()]);
            return ['status' => 'DOWN', 'error' => $e->getMessage()];
        }
    }

    private function checkRedis(): array
    {
        try {
            $start = microtime(true);
            $this->redis->ping();
            $latency = round((microtime(true) - $start) * 1000, 2);

            return ['status' => 'UP', 'latency_ms' => $latency];
        } catch (\Throwable $e) {
            $this->logger->error('Redis health check failed', ['error' => $e->getMessage()]);
            return ['status' => 'DOWN', 'error' => $e->getMessage()];
        }
    }

    private function checkNacos(): array
    {
        $nacosHost = env('NACOS_HOST', '127.0.0.1');
        $nacosPort = env('NACOS_PORT', 8848);
        $url = "http://{$nacosHost}:{$nacosPort}/nacos/v1/console/health/readiness";

        $start = microtime(true);
        $response = @file_get_contents($url);
        $latency = round((microtime(true) - $start) * 1000, 2);

        if ($response === 'UP') {
            return ['status' => 'UP', 'latency_ms' => $latency];
        }

        return ['status' => 'DOWN', 'error' => 'Nacos not ready'];
    }

    private function checkDisk(): array
    {
        $free = disk_free_space('/');
        $total = disk_total_space('/');
        $usagePercent = round((1 - $free / $total) * 100, 2);

        $status = $usagePercent < 90 ? 'UP' : 'DOWN';

        return [
            'status' => $status,
            'usage_percent' => $usagePercent,
            'free_gb' => round($free / 1024 / 1024 / 1024, 2),
        ];
    }

    private function getConnectionCount(): int
    {
        // Return active DB connections or coroutine count
        return 42; // Mock value
    }
}
```

### Nacos 健康检查配置 — config/autoload/services.php

```php
<?php
// ✅ Nacos heartbeat and health check configuration

declare(strict_types=1);

return [
    'enable' => [
        'discovery' => true,
        'register' => true,
    ],
    'drivers' => [
        'nacos' => [
            'host' => env('NACOS_HOST', '127.0.0.1'),
            'port' => (int) env('NACOS_PORT', 8848),
            'username' => env('NACOS_USERNAME', 'nacos'),
            'password' => env('NACOS_PASSWORD', 'nacos'),
            'group_name' => env('NACOS_SERVICE_GROUP', 'DEFAULT_GROUP'),
            'namespace_id' => env('NACOS_NAMESPACE', ''),

            // 🟡 Heartbeat configuration
            'heartbeat' => [
                'interval' => 5000,  // ms
                'timeout' => 15000,  // ms (Nacos considers unhealthy if no heartbeat for 15s)
                'ttl' => 20000,      // ms (TTL for ephemeral instances)
            ],

            // 🟢 Custom health check endpoint
            'health_check' => [
                'url' => '/health/live',
                'interval' => 10000,
            ],
        ],
    ],
];
```

### 自定义健康检查事件监听器 — HealthCheckListener.php

```php
<?php

declare(strict_types=1);

namespace App\Listener;

use Hyperf\Event\Annotation\Listener;
use Hyperf\Event\Contract\ListenerInterface;
use Hyperf\Framework\Event\BeforeServerStart;
use Psr\Log\LoggerInterface;

#[Listener]
class HealthCheckListener implements ListenerInterface
{
    public function __construct(
        private LoggerInterface $logger
    ) {}

    public function listen(): array
    {
        return [BeforeServerStart::class];
    }

    public function process(object $event): void
    {
        $this->logger->info('Starting health check pre-flight...');

        // 🟡 Verify all dependencies are healthy before starting
        $checks = [
            'database' => $this->checkDatabase(),
            'redis' => $this->checkRedis(),
            'nacos' => $this->checkNacos(),
        ];

        $allHealthy = !in_array(false, $checks, true);

        if (!$allHealthy) {
            $this->logger->error('Pre-flight health check failed', ['checks' => $checks]);
            // throw new \RuntimeException('Dependencies not ready');
        }

        $this->logger->info('Pre-flight health check passed', ['checks' => $checks]);
    }

    private function checkDatabase(): bool
    {
        try {
            \Hyperf\DbConnection\Db::select('SELECT 1');
            return true;
        } catch (\Throwable $e) {
            return false;
        }
    }

    private function checkRedis(): bool
    {
        try {
            $this->container->get(\Hyperf\Redis\Redis::class)->ping();
            return true;
        } catch (\Throwable $e) {
            return false;
        }
    }

    private function checkNacos(): bool
    {
        $url = 'http://127.0.0.1:8848/nacos/v1/console/health/readiness';
        return @file_get_contents($url) === 'UP';
    }
}
```

### K8s 健康检查配置 — k8s-healthcheck.yaml

```yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      containers:
        - name: user-service
          image: user-service:1.0.0
          ports:
            - containerPort: 9501

          # ✅ Liveness probe — restart if unhealthy
          livenessProbe:
            httpGet:
              path: /health/live
              port: 9501
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3

          # ✅ Readiness probe — stop traffic if not ready
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 9501
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 2
```

### 健康检查测试脚本 — test-healthcheck.php

```php
<?php
// ✅ Test health check endpoints

declare(strict_types=1);

$baseUrl = 'http://127.0.0.1:9501';

echo "🧪 Health Check Test\n\n";

// 1. Liveness check
echo "1️⃣ Liveness Check (/health/live):\n";
$response = @file_get_contents("{$baseUrl}/health/live");
echo json_encode(json_decode($response) ?: [], JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES) . "\n\n";

// 2. Readiness check
echo "2️⃣ Readiness Check (/health/ready):\n";
$response = @file_get_contents("{$baseUrl}/health/ready");
$data = json_decode($response, true);

if (isset($data['checks'])) {
    foreach ($data['checks'] as $name => $check) {
        $status = $check['status'] === 'UP' ? '✅' : '❌';
        echo "   {$status} {$name}: {$check['status']}";
        if (isset($check['latency_ms'])) {
            echo " ({$check['latency_ms']}ms)";
        }
        echo "\n";
    }
}

echo "\n3️⃣ Detailed Check (/health/detail):\n";
$response = @file_get_contents("{$baseUrl}/health/detail");
echo json_encode(json_decode($response) ?: [], JSON_PRETTY_PRINT | JSON_UNESCAPED_SLASHES) . "\n";

echo "\n✅ Test complete!\n";
```

## 执行预览

```bash
# 启动用户服务
$ php bin/hyperf.php start

# 测试健康检查
$ php test-healthcheck.php
🧪 Health Check Test

1️⃣ Liveness Check (/health/live):
{
    "status": "UP",
    "timestamp": 1714567890,
    "service": "user-service"
}

2️⃣ Readiness Check (/health/ready):
   ✅ database: UP (5ms)
   ✅ redis: UP (2ms)
   ✅ nacos: UP (8ms)

3️⃣ Detailed Check (/health/detail):
{
    "status": "UP",
    "timestamp": 1714567890,
    "checks": {
        "database": {"status": "UP", "latency_ms": 5},
        "redis": {"status": "UP", "latency_ms": 2},
        "nacos": {"status": "UP", "latency_ms": 8},
        "disk": {"status": "UP", "usage_percent": 45.2}
    },
    "metrics": {
        "connections": 42,
        "memory_mb": 64.5
    }
}

✅ Test complete!

# 在 Nacos 控制台查看实例健康状态
# 所有实例显示为 healthy ✓
```

## 注意事项

| 约束 | 说明 | 违反后果 |
|------|------|----------|
| 🔴 liveness 和 readiness 区分 | liveness 重启容器，readiness 停流量 | 混用导致误重启或流量泄露 |
| 🟡 检查间隔 | 不要太频繁（5-10s），也不要太长（>30s） | 频繁检查增加负载，太长延迟发现故障 |
| 🟢 依赖检查超时 | 每个检查要有超时（1-3s） | 单个依赖挂起导致整个检查卡死 |
| 🔴 优雅降级 | 次要依赖失败不影响整体状态 | 小问题导致整个服务不可用 |
| 🟡 日志级别 | 健康检查失败要记录警告或错误 | 无法排查故障原因 |

## 避坑指南

| 错误 | 现象 | 正确做法 |
|------|------|----------|
| liveness 响应慢 | 容器被频繁重启 | liveness 只检查进程，不查依赖 |
| readiness 不检查 DB | 流量打到 DB 失败的实例 | readiness 要检查所有关键依赖 |
| 检查超时太长 | 探测超时，误判不健康 | 单个检查超时设置 1-3 秒 |
| 忘记返回 JSON | 探测失败 | 确保 Content-Type: application/json |
| 没有缓存健康状态 | 每次请求都查所有依赖 | 缓存结果，定期刷新 |

## 练习题

### 🟢 基础
1. 实现 `/health/live` 和 `/health/ready` 两个端点，验证 Nacos 能正确标记健康状态。
2. 停止数据库，观察 Nacos 是否将实例标记为不健康。

### 🟡 进阶
3. 实现一个自定义的依赖健康检查器（如第三方 API），支持重试和超时配置。

### 🔴 开放
4. 设计一个自我保护机制：当连续 3 次健康检查失败时，主动从 Nacos 注销并停止接收流量。

## 知识点总结

```
健康检查
├── 检查类型
│   ├── Liveness — 进程是否运行
│   ├── Readiness — 依赖是否就绪
│   └── Startup — 启动是否完成
├── Nacos 心跳机制
│   ├── 实例定时发送心跳
│   ├── 超时未收到心跳 → 标记不健康
│   └── 长时间不健康 → 自动摘除
├── Hyperf 实现
│   ├── HealthController
│   ├── 配置 heartbeat 间隔
│   └── 自定义健康检查端点
└── Kubernetes 集成
    ├── livenessProbe
    ├── readinessProbe
    └── startupProbe
```

## 举一反三

| 场景 | 适配方式 | 关键差异 |
|------|----------|----------|
| 优雅停机 | 停机前先返回 readiness=DOWN | 停止接收新流量，处理完现有请求 |
| 滚动发布 | 新实例 readiness=UP 后才下线旧实例 | 零停机部署 |
| 故障自愈 | readiness=DOWN 自动触发重启 | 容器编排平台（K8s）支持 |

## 参考资料

- ⭐⭐⭐ [Nacos 健康检查文档](https://nacos.io/zh-cn/docs/cluster-mode-quick-start.html)
- ⭐⭐⭐ [K8s 健康检查配置](https://kubernetes.io/docs/tasks/configure-pod-container/configure-liveness-readiness-startup-probes/)
- ⭐⭐ [Hyperf 生命周期事件](https://hyperf.wiki/3.1/#/zh-cn/lifecycle)

## 代码演进

### v1 ❌ 无健康检查

```php
// ❌ No health check — Nacos doesn't know if service is down
// Instance stays in registry even when process crashed
```

### v2 ✅ 简单心跳

```php
// ✅ Nacos heartbeat marks instance unhealthy on timeout
'heartbeat' => 5000,  // Send heartbeat every 5s
// Nacos marks unhealthy if no heartbeat for 15s
```

### v3 🟢 完整健康检查体系

```php
// 🟢 Full health check with liveness/readiness/startup
#[GetMapping(path: '/ready')]
public function readiness(): array
{
    return [
        'status' => $this->checkDatabase() && $this->checkRedis() ? 'UP' : 'DOWN',
        'checks' => ['database' => ..., 'redis' => ...],
    ];
}

// K8s integration
livenessProbe:
  httpGet:
    path: /health/live
readinessProbe:
  httpGet:
    path: /health/ready
```
