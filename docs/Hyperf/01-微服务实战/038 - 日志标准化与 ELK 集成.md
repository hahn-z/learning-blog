---
title: "038 - 日志标准化与 ELK 集成"
slug: "038-log-elk"
category: "Hyperf微服务实战"
tech_stack: "Hyperf"
created_at: "2026-05-01T21:52:09.046+08:00"
updated_at: "2026-05-01T22:15:41.579+08:00"
reading_time: 6
tags: []
---

# 日志标准化与 ELK 集成

> **难度：** ⭐⭐⭐
> **前置知识：** Hyperf 日志组件、Docker 基础、JSON 格式
> **预估用时：** 45-60 分钟

## 1. 概念讲解

**ELK Stack**（Elasticsearch + Logstash + Kibana）是业界最流行的日志收集、存储和可视化方案。Elasticsearch 负责存储和搜索，Logstash 负责日志收集和转换，Kibana 负责可视化展示。

类比理解：ELK 就像一个大型图书馆系统。Logstash 是图书管理员，负责收集各处送来的书（日志）并贴标签分类；Elasticsearch 是图书馆的索引柜，可以秒级找到任何一本书；Kibana 是前台的搜索终端，输入关键词就能看到结果。

在用户服务中，我们将日志格式标准化为 JSON，通过 Filebeat 采集发送到 Elasticsearch，用 Kibana 进行搜索和可视化。

## 2. 实时脑图

```
┌──────────────────────────────────────────────┐
│         ELK Log Pipeline                      │
├──────────────────────────────────────────────┤
│                                               │
│  [Hyperf App]                                 │
│    |-- JSON log -> /var/log/user-service.log  │
│    |                                          │
│  [Filebeat]                                   │
│    |-- Read log file -> parse JSON            │
│    +-- Send to Elasticsearch                  │
│                                               │
│  [Elasticsearch]                              │
│    |-- Index: user-service-2024.01.*          │
│    +-- Full-text search + aggregation         │
│                                               │
│  [Kibana]                                     │
│    |-- Discover: search logs                  │
│    |-- Dashboard: error rate, latency         │
│    +-- Alert: spike in errors                 │
│                                               │
│  Log Fields:                                  │
│  |-- timestamp, level, message                │
│  |-- trace_id, request_id                     │
│  |-- user_id, action, duration                │
│  +-- context (extra key-value pairs)          │
└──────────────────────────────────────────────┘
```

## 3. 完整代码

### 3.1 Hyperf 日志配置 `config/autoload/logger.php`

```php
<?php
declare(strict_types=1);

return [
    'default' => [
        'handler' => [
            // ✅ JSON formatter for ELK compatibility
            'class' => Monolog\Handler\StreamHandler::class,
            'constructor' => [
                'stream' => BASE_PATH . '/runtime/logs/user-service.log',
                'level' => Monolog\Logger::DEBUG,
            ],
            'formatter' => [
                'class' => Monolog\Formatter\JsonFormatter::class, // ✅ JSON format
            ],
        ],
        'processors' => [
            // ✅ Add trace_id to every log entry
            [
                'class' => App\Log\TraceIdProcessor::class,
            ],
        ],
    ],
];
```

### 3.2 TraceId 处理器 `app/Log/TraceIdProcessor.php`

```php
<?php
declare(strict_types=1);

namespace App\Log;

use Monolog\LogRecord;
use Monolog\Processor\ProcessorInterface;
use Hyperf\Context\Context;
use Hyperf\Coroutine\Coroutine;

/**
 * ✅ Add trace_id and request context to every log entry
 */
class TraceIdProcessor implements ProcessorInterface
{
    public function __invoke(LogRecord $record): LogRecord
    {
        // ✅ Generate or reuse trace_id within request lifecycle
        $traceId = Context::get('trace_id', uniqid('tr_', true));
        Context::set('trace_id', $traceId);

        $record->extra['trace_id'] = $traceId;
        $record->extra['coroutine_id'] = Coroutine::id();

        return $record;
    }
}
```

### 3.3 结构化日志帮助类 `app/Log/StructuredLogger.php`

```php
<?php
declare(strict_types=1);

namespace App\Log;

use Psr\Log\LoggerInterface;
use Hyperf\Di\Annotation\Inject;

/**
 * ✅ Structured logger with consistent field naming for ELK
 */
class StructuredLogger
{
    #[Inject]
    private LoggerInterface $logger;

    /**
     * ✅ Log user action with structured context
     */
    public function logUserAction(string $action, int $userId, array $context = []): void
    {
        $this->logger->info("User action: {$action}", array_merge([
            'action' => $action,
            'user_id' => $userId,
            'timestamp' => date('c'),
        ], $context));
    }

    /**
     * ✅ Log saga event
     */
    public function logSagaEvent(string $sagaId, string $step, string $event, array $context = []): void
    {
        $this->logger->info("Saga {$event}: {$step}", array_merge([
            'saga_id' => $sagaId,
            'saga_step' => $step,
            'saga_event' => $event,
        ], $context));
    }

    /**
     * ✅ Log error with full context for debugging
     */
    public function logError(string $message, \Throwable $e, array $context = []): void
    {
        $this->logger->error($message, array_merge([
            'exception_class' => get_class($e),
            'exception_message' => $e->getMessage(),
            'exception_file' => $e->getFile() . ':' . $e->getLine(),
        ], $context));
    }
}
```

### 3.4 中间件：为每个请求添加 TraceId `app/Middleware/TraceIdMiddleware.php`

```php
<?php
declare(strict_types=1);

namespace App\Middleware;

use Hyperf\Context\Context;
use Psr\Http\Message\ResponseInterface;
use Psr\Http\Message\ServerRequestInterface;
use Psr\Http\Server\MiddlewareInterface;
use Psr\Http\Server\RequestHandlerInterface;

/**
 * ✅ Assign trace_id to every request for distributed tracing
 */
class TraceIdMiddleware implements MiddlewareInterface
{
    public function process(ServerRequestInterface $request, RequestHandlerInterface $handler): ResponseInterface
    {
        // ✅ Use X-Trace-Id header or generate new one
        $traceId = $request->getHeaderLine('X-Trace-Id') ?: uniqid('tr_', true);
        Context::set('trace_id', $traceId);

        $response = $handler->handle($request);

        // ✅ Return trace_id in response header for debugging
        return $response->withHeader('X-Trace-Id', $traceId);
    }
}
```

### 3.5 Filebeat 配置 `filebeat.yml`

```yaml
# ✅ Filebeat configuration for log collection
filebeat.inputs:
  - type: log
    enabled: true
    paths:
      - /var/log/user-service/*.log
    json.keys_under_root: true      # ✅ Parse JSON logs
    json.add_error_key: true
    fields:
      service: user-service          # ✅ Tag with service name
      env: ${ENV:production}

output.elasticsearch:
  hosts: ["elasticsearch:9200"]
  index: "user-service-%{+yyyy.MM.dd}"  # ✅ Daily index

logging.level: info
```

### 3.6 Docker Compose `docker-compose.yml`

```yaml
version: '3.8'
services:
  elasticsearch:
    image: elasticsearch:8.12.0
    environment:
      - discovery.type=single-node
      - xpack.security.enabled=false
      - "ES_JAVA_OPTS=-Xms512m -Xmx512m"
    ports:
      - "9200:9200"
    volumes:
      - es_data:/usr/share/elasticsearch/data

  kibana:
    image: kibana:8.12.0
    ports:
      - "5601:5601"
    environment:
      - ELASTICSEARCH_HOSTS=http://elasticsearch:9200
    depends_on:
      - elasticsearch

  filebeat:
    image: elastic/filebeat:8.12.0
    volumes:
      - ./filebeat.yml:/usr/share/filebeat/filebeat.yml:ro
      - ./runtime/logs:/var/log/user-service:ro
    depends_on:
      - elasticsearch

volumes:
  es_data:
```

## 4. 执行预览

```bash
$ docker-compose up -d elasticsearch kibana filebeat

# Log output (JSON format):
# {"message":"User action: register","context":{"action":"register","user_id":1001},"level":200,"channel":"default","datetime":"2024-01-15T10:30:00+08:00","extra":{"trace_id":"tr_662f...","coroutine_id":1}}

# Kibana query (http://localhost:5601):
# level: "ERROR" AND context.action: "register"
# Aggregation: avg(context.duration) by date histogram
```

## 5. 注意事项

| 约束 | 说明 | 违反后果 |
|------|------|----------|
| 日志必须用 JSON 格式 | ELK 需要结构化数据 | 无法解析，搜索困难 |
| 每条日志含 trace_id | 分布式追踪的基础 | 无法关联同一请求的日志 |
| 敏感数据不入日志 | 密码、token 等 | 安全泄露 |
| 日志文件做轮转 | 防止磁盘满 | 磁盘爆满服务崩溃 |
| Elasticsearch 索引按天分割 | 便于清理和查询 | 单索引过大查询慢 |

## 6. 避坑指南

| 错误 | 现象 | 正确做法 |
|------|------|----------|
| 日志用纯文本 | 无法按字段搜索 | 用 JSON Formatter |
| 不加 trace_id | 无法追踪请求链路 | 中间件注入 + Context 传递 |
| 大量 DEBUG 日志 | ES 存储暴增 | 生产用 INFO 级别 |
| Filebeat 路径错误 | 采集不到日志 | 确认容器内路径映射 |
| 不配索引生命周期 | ES 磁盘持续增长 | 配 ILM 策略保留 30 天 |

## 7. 练习题

🟢 **基础题 1：** 添加一个 Kibana 仪表盘面板，展示每小时 ERROR 日志数量。

🟢 **基础题 2：** 配置 Filebeat 多服务采集（user-service + order-service）。

🟡 **进阶题：** 实现日志的 ILM（Index Lifecycle Management）策略，自动删除 30 天前的索引。

🔴 **开放题：** 设计一个完整的日志告警方案：当 5 分钟内 ERROR 日志超过阈值时自动通知。

## 8. 知识点总结

```
Logging with ELK
├── Hyperf Logger
│   ├── JSON Formatter
│   ├── TraceIdProcessor
│   └── StructuredLogger helper
├── Middleware
│   └── TraceIdMiddleware (X-Trace-Id header)
├── ELK Stack
│   ├── Elasticsearch (storage + search)
│   ├── Logstash/Filebeat (collection)
│   └── Kibana (visualization)
├── Log Schema
│   ├── message, level, channel, datetime
│   ├── trace_id, coroutine_id
│   └── context (action, user_id, etc.)
└── Best Practices
    ├── JSON format, trace_id on every entry
    ├── No sensitive data in logs
    ├── Daily index + ILM
    └── Production: INFO level
```

## 9. 举一反三

| 场景 | 日志策略 | 搜索需求 |
|------|----------|----------|
| API 网关 | 每请求记录 method/path/status/duration | 按 status_code 过滤慢请求 |
| 支付服务 | 记录 payment_id/amount/channel/result | 按订单号查支付流水 |
| 定时任务 | 记录 job_name/start/end/duration | 查看任务执行历史和失败原因 |

## 10. 参考资料

- 🏆 [Elasticsearch 官方文档](https://www.elastic.co/guide/en/elasticsearch/reference/current/index.html)
- 🏆 [Hyperf 日志组件](https://hyperf.wiki/3.1/#/zh-cn/logger)
- ⭐ [Filebeat 配置指南](https://www.elastic.co/guide/en/beats/filebeat/current/configuring-howto-filebeat.html)
- 📖 [Monolog JSON Formatter](https://github.com/Seldaek/monolog)

## 11. 代码演进

### v1 ❌ 混乱日志
```php
error_log("User registered: " . $userId);
// Plain text, no structure, no trace_id
```

### v2 ✅ PSR-3 日志
```php
$this->logger->info('User registered', ['user_id' => $userId]);
// Structured but text format, hard for ELK to parse
```

### v3 🟢 JSON + TraceId + ELK
```php
// JSON formatted + trace_id + structured context
$this->structuredLogger->logUserAction('register', $userId, ['email' => $email]);
// Filebeat -> Elasticsearch -> Kibana: full-text search, aggregation, alerting
```
