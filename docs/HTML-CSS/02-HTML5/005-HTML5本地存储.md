---
title: "005-HTML5本地存储"
slug: "005-html5-storage"
category: "HTML5"
tech_stack: "HTML-CSS"
created_at: "2026-04-26T10:33:05.576+08:00"
updated_at: "2026-04-29T10:02:45.933+08:00"
reading_time: 65
tags: []
---

# 005-HTML5本地存储

> **难度：** 🟡 中级 | **阅读时间：** 25分钟 | **前置知识：** JavaScript基础

---

## 一、概念讲解

### 1.1 Web存储方案对比

| 特性 | Cookie | localStorage | sessionStorage | IndexedDB |
|------|--------|-------------|----------------|-----------|
| 容量 | 4KB | 5MB | 5MB | 无上限（通常数百MB+） |
| 过期 | 可设过期时间 | 永久 | 标签页关闭 | 永久 |
| 请求携带 | ✅ 每次HTTP请求 | ❌ | ❌ | ❌ |
| API | 字符串 | 同步键值对 | 同步键值对 | 异步 |
| 作用域 | 同域+路径 | 同源 | 同源+标签页 | 同源 |
| 数据类型 | 字符串 | 字符串 | 字符串 | 结构化数据 |
| Worker访问 | ❌ | ✅ | ❌ | ✅ |

### 1.2 localStorage / sessionStorage

```javascript
// Set item (auto-converts to string)
localStorage.setItem('username', '铁蛋');

// Get item
var name = localStorage.getItem('username'); // '铁蛋'

// Remove item
localStorage.removeItem('username');

// Clear all
localStorage.clear();

// Key by index
var key = localStorage.key(0);

// Length
var count = localStorage.length;
```

### 1.3 IndexedDB基础

IndexedDB是浏览器内置的NoSQL数据库，支持事务、索引和游标：

```javascript
// Open database
var request = indexedDB.open('MyDatabase', 1);

request.onupgradeneeded = function(e) {
    var db = e.target.result;
    var store = db.createObjectStore('users', { keyPath: 'id' });
    store.createIndex('name', 'name', { unique: false });
};

request.onsuccess = function(e) {
    var db = e.target.result;
    // Start transaction, add data, etc.
};
```

---

## 二、知识脑图

```
HTML5本地存储
├── Cookie（传统）
│   ├── 4KB限制
│   ├── 每次请求携带
│   ├── document.cookie操作
│   └── 适用：会话token、用户偏好
├── Web Storage
│   ├── localStorage
│   │   ├── 5MB（同源）
│   │   ├── 永久存储
│   │   └── 同源共享
│   ├── sessionStorage
│   │   ├── 5MB（同源+标签页）
│   │   ├── 标签页关闭清除
│   │   └── 独立标签页隔离
│   └── API
│       ├── setItem / getItem
│       ├── removeItem / clear
│       ├── key / length
│       └── storage事件
├── IndexedDB
│   ├── 数据库（Database）
│   ├── 对象仓库（Object Store）
│   ├── 事务（Transaction）
│   ├── 索引（Index）
│   └── 游标（Cursor）
└── 最佳实践
    ├── 存储JSON用JSON.stringify/parse
    ├── try-catch处理quota
    ├── 敏感数据不存本地
    └── 定期清理过期数据
```

---

## 三、完整代码示例

### 3.1 本地存储管理器

```html
<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HTML5本地存储实战</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }

        body {
            font-family: system-ui, sans-serif;
            background: #f0f2f5;
            color: #333;
            padding: 20px;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
        }

        h1 {
            text-align: center;
            margin-bottom: 30px;
            color: #2c3e50;
        }

        /* Tab navigation */
        .tabs {
            display: flex;
            gap: 0;
            margin-bottom: 0;
        }

        .tab-btn {
            padding: 12px 24px;
            background: #ddd;
            border: none;
            cursor: pointer;
            font-size: 15px;
            font-weight: 600;
            transition: background 0.2s;
        }

        .tab-btn:first-child { border-radius: 8px 0 0 0; }
        .tab-btn:last-child { border-radius: 0 8px 0 0; }

        .tab-btn.active {
            background: white;
            color: #3498db;
        }

        /* Tab content */
        .tab-content {
            background: white;
            border-radius: 0 8px 8px 8px;
            padding: 24px;
            box-shadow: 0 2px 12px rgba(0,0,0,0.08);
        }

        .tab-panel { display: none; }
        .tab-panel.active { display: block; }

        /* Storage viewer */
        .storage-toolbar {
            display: flex;
            gap: 10px;
            margin-bottom: 16px;
        }

        .storage-toolbar input {
            flex: 1;
            padding: 10px 14px;
            border: 2px solid #ddd;
            border-radius: 8px;
            font-size: 14px;
            outline: none;
        }

        .storage-toolbar input:focus { border-color: #3498db; }

        .btn {
            padding: 10px 20px;
            border: none;
            border-radius: 8px;
            font-size: 14px;
            font-weight: 600;
            cursor: pointer;
            transition: transform 0.2s;
        }

        .btn:hover { transform: translateY(-1px); }
        .btn-primary { background: #3498db; color: white; }
        .btn-danger { background: #e74c3c; color: white; }
        .btn-success { background: #2ecc71; color: white; }
        .btn-warning { background: #f39c12; color: white; }

        /* Data table */
        .data-table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 16px;
        }

        .data-table th {
            background: #f8f9fa;
            padding: 10px 12px;
            text-align: left;
            font-size: 13px;
            color: #666;
            border-bottom: 2px solid #eee;
        }

        .data-table td {
            padding: 10px 12px;
            border-bottom: 1px solid #f0f0f0;
            font-size: 14px;
        }

        .data-table tr:hover { background: #f8f9fa; }

        .data-table .key { font-weight: 600; color: #2c3e50; }

        .data-table .value {
            max-width: 300px;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
            font-family: monospace;
            font-size: 13px;
            color: #666;
        }

        .data-table .actions { white-space: nowrap; }

        .data-table .actions button {
            padding: 4px 8px;
            font-size: 12px;
            margin-right: 4px;
        }

        /* Stats */
        .stats {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 16px;
            margin-bottom: 20px;
        }

        .stat-card {
            background: #f8f9fa;
            border-radius: 8px;
            padding: 16px;
            text-align: center;
        }

        .stat-card .number {
            font-size: 28px;
            font-weight: bold;
            color: #3498db;
        }

        .stat-card .label {
            font-size: 13px;
            color: #95a5a6;
            margin-top: 4px;
        }

        /* Note app */
        .note-app {
            margin-top: 16px;
        }

        .note-input {
            width: 100%;
            padding: 12px;
            border: 2px solid #ddd;
            border-radius: 8px;
            font-size: 15px;
            min-height: 60px;
            resize: vertical;
            outline: none;
        }

        .note-input:focus { border-color: #3498db; }

        .notes-list {
            margin-top: 16px;
        }

        .note-item {
            background: #f8f9fa;
            border-radius: 8px;
            padding: 12px 16px;
            margin-bottom: 8px;
            display: flex;
            justify-content: space-between;
            align-items: flex-start;
        }

        .note-content { flex: 1; }

        .note-time {
            font-size: 12px;
            color: #95a5a6;
            margin-top: 4px;
        }

        .note-item button {
            background: none;
            border: none;
            color: #e74c3c;
            cursor: pointer;
            font-size: 18px;
            padding: 0 4px;
        }

        /* IndexedDB section */
        .idb-form {
            display: grid;
            grid-template-columns: 1fr 2fr auto;
            gap: 10px;
            margin-bottom: 16px;
        }

        .idb-form input {
            padding: 10px;
            border: 2px solid #ddd;
            border-radius: 8px;
            outline: none;
        }

        .idb-form input:focus { border-color: #3498db; }

        /* Empty state */
        .empty-state {
            text-align: center;
            padding: 40px;
            color: #bdc3c7;
        }

        .empty-state .icon { font-size: 48px; margin-bottom: 12px; }

        /* Storage event log */
        .event-log {
            background: #2c3e50;
            color: #ecf0f1;
            border-radius: 8px;
            padding: 12px 16px;
            font-family: monospace;
            font-size: 13px;
            max-height: 150px;
            overflow-y: auto;
            margin-top: 16px;
        }

        .event-log .log-entry {
            margin-bottom: 4px;
        }

        .event-log .log-time { color: #95a5a6; }
    </style>
</head>
<body>
    <div class="container">
        <h1>💾 HTML5本地存储管理器</h1>

        <!-- Tab navigation -->
        <div class="tabs">
            <button class="tab-btn active" onclick="switchTab('localStorage')">localStorage</button>
            <button class="tab-btn" onclick="switchTab('sessionStorage')">sessionStorage</button>
            <button class="tab-btn" onclick="switchTab('indexedDB')">IndexedDB</button>
            <button class="tab-btn" onclick="switchTab('notes')">备忘录Demo</button>
        </div>

        <div class="tab-content">
            <!-- localStorage Panel -->
            <div class="tab-panel active" id="panel-localStorage">
                <div class="stats">
                    <div class="stat-card">
                        <div class="number" id="ls-count">0</div>
                        <div class="label">条目数</div>
                    </div>
                    <div class="stat-card">
                        <div class="number" id="ls-size">0</div>
                        <div class="label">已用空间(B)</div>
                    </div>
                    <div class="stat-card">
                        <div class="number" id="ls-limit">~5MB</div>
                        <div class="label">容量限制</div>
                    </div>
                </div>

                <div class="storage-toolbar">
                    <input type="text" id="ls-key" placeholder="Key">
                    <input type="text" id="ls-value" placeholder="Value">
                    <button class="btn btn-primary" onclick="addLSItem()">添加</button>
                </div>

                <table class="data-table" id="ls-table">
                    <thead>
                        <tr>
                            <th>Key</th>
                            <th>Value</th>
                            <th>操作</th>
                        </tr>
                    </thead>
                    <tbody id="ls-body"></tbody>
                </table>

                <div style="margin-top:12px;text-align:right;">
                    <button class="btn btn-danger" onclick="clearLS()">清空全部</button>
                </div>
            </div>

            <!-- sessionStorage Panel -->
            <div class="tab-panel" id="panel-sessionStorage">
                <p style="margin-bottom:16px;color:#95a5a6;">
                    sessionStorage与localStorage API完全一致，但数据在标签页关闭后清除。
                </p>

                <div class="storage-toolbar">
                    <input type="text" id="ss-key" placeholder="Key">
                    <input type="text" id="ss-value" placeholder="Value">
                    <button class="btn btn-primary" onclick="addSSItem()">添加</button>
                </div>

                <table class="data-table">
                    <thead>
                        <tr><th>Key</th><th>Value</th><th>操作</th></tr>
                    </thead>
                    <tbody id="ss-body"></tbody>
                </table>
            </div>

            <!-- IndexedDB Panel -->
            <div class="tab-panel" id="panel-indexedDB">
                <p style="margin-bottom:16px;color:#95a5a6;">
                    IndexedDB支持结构化数据存储，无大小限制（通常数百MB）。
                </p>

                <div class="idb-form">
                    <input type="text" id="idb-name" placeholder="姓名">
                    <input type="email" id="idb-email" placeholder="邮箱">
                    <button class="btn btn-success" onclick="addIDBContact()">添加联系人</button>
                </div>

                <table class="data-table">
                    <thead>
                        <tr><th>姓名</th><th>邮箱</th><th>操作</th></tr>
                    </thead>
                    <tbody id="idb-body"></tbody>
                </table>
            </div>

            <!-- Notes Demo Panel -->
            <div class="tab-panel" id="panel-notes">
                <div class="note-app">
                    <textarea class="note-input" id="noteInput" placeholder="写点什么...自动保存到localStorage"></textarea>
                    <div style="margin-top:8px;text-align:right;">
                        <button class="btn btn-primary" onclick="addNote()">保存备忘</button>
                    </div>

                    <div class="notes-list" id="notesList"></div>
                </div>

                <div class="event-log" id="eventLog">
                    <div class="log-entry"><span class="log-time">[系统]</span> 存储事件日志（同一源的其他标签页操作会触发storage事件）</div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // ========== Tab switching ==========
        function switchTab(name) {
            document.querySelectorAll('.tab-btn').forEach(function(btn, i) {
                btn.classList.toggle('active', btn.textContent === name ||
                    (i === 0 && name === 'localStorage') ||
                    (i === 1 && name === 'sessionStorage') ||
                    (i === 2 && name === 'indexedDB') ||
                    (i === 3 && name === 'notes'));
            });

            document.querySelectorAll('.tab-btn').forEach(function(btn) {
                btn.classList.remove('active');
            });
            event.target.classList.add('active');

            document.querySelectorAll('.tab-panel').forEach(function(panel) {
                panel.classList.remove('active');
            });
            document.getElementById('panel-' + name).classList.add('active');

            // Refresh data
            if (name === 'localStorage') refreshLS();
            if (name === 'sessionStorage') refreshSS();
            if (name === 'indexedDB') refreshIDB();
            if (name === 'notes') refreshNotes();
        }

        // ========== localStorage ==========
        function refreshLS() {
            var body = document.getElementById('ls-body');
            body.innerHTML = '';
            var totalSize = 0;

            for (var i = 0; i < localStorage.length; i++) {
                var key = localStorage.key(i);
                var value = localStorage.getItem(key);
                totalSize += key.length + value.length;

                var tr = document.createElement('tr');
                tr.innerHTML = '<td class="key">' + escapeHtml(key) + '</td>' +
                    '<td class="value">' + escapeHtml(value) + '</td>' +
                    '<td class="actions"><button class="btn btn-danger" onclick="removeLS(\'' +
                    escapeHtml(key) + '\')">删除</button></td>';
                body.appendChild(tr);
            }

            document.getElementById('ls-count').textContent = localStorage.length;
            document.getElementById('ls-size').textContent = (totalSize * 2).toLocaleString(); // UTF-16
        }

        function addLSItem() {
            var key = document.getElementById('ls-key').value.trim();
            var value = document.getElementById('ls-value').value.trim();
            if (!key) return;

            try {
                localStorage.setItem(key, value);
                document.getElementById('ls-key').value = '';
                document.getElementById('ls-value').value = '';
                refreshLS();
            } catch (e) {
                alert('存储失败：可能超出容量限制 - ' + e.message);
            }
        }

        function removeLS(key) {
            localStorage.removeItem(key);
            refreshLS();
        }

        function clearLS() {
            if (confirm('确定清空所有localStorage数据？')) {
                localStorage.clear();
                refreshLS();
            }
        }

        // ========== sessionStorage ==========
        function refreshSS() {
            var body = document.getElementById('ss-body');
            body.innerHTML = '';

            for (var i = 0; i < sessionStorage.length; i++) {
                var key = sessionStorage.key(i);
                var value = sessionStorage.getItem(key);

                var tr = document.createElement('tr');
                tr.innerHTML = '<td class="key">' + escapeHtml(key) + '</td>' +
                    '<td class="value">' + escapeHtml(value) + '</td>' +
                    '<td><button class="btn btn-danger" onclick="sessionStorage.removeItem(\'' +
                    escapeHtml(key) + '\');refreshSS();">删除</button></td>';
                body.appendChild(tr);
            }
        }

        function addSSItem() {
            var key = document.getElementById('ss-key').value.trim();
            var value = document.getElementById('ss-value').value.trim();
            if (!key) return;
            sessionStorage.setItem(key, value);
            document.getElementById('ss-key').value = '';
            document.getElementById('ss-value').value = '';
            refreshSS();
        }

        // ========== IndexedDB ==========
        var idb = null;

        function initIDB() {
            var request = indexedDB.open('ContactDB', 1);

            request.onupgradeneeded = function(e) {
                var db = e.target.result;
                if (!db.objectStoreNames.contains('contacts')) {
                    var store = db.createObjectStore('contacts', { keyPath: 'id', autoIncrement: true });
                    store.createIndex('name', 'name', { unique: false });
                    store.createIndex('email', 'email', { unique: true });
                }
            };

            request.onsuccess = function(e) {
                idb = e.target.result;
                refreshIDB();
            };
        }

        function addIDBContact() {
            var name = document.getElementById('idb-name').value.trim();
            var email = document.getElementById('idb-email').value.trim();
            if (!name || !email) return;

            var tx = idb.transaction('contacts', 'readwrite');
            var store = tx.objectStore('contacts');
            var request = store.add({ name: name, email: email });

            request.onsuccess = function() {
                document.getElementById('idb-name').value = '';
                document.getElementById('idb-email').value = '';
                refreshIDB();
            };

            request.onerror = function(e) {
                alert('添加失败（邮箱可能重复）：' + e.target.error);
            };
        }

        function refreshIDB() {
            if (!idb) return;
            var body = document.getElementById('idb-body');
            body.innerHTML = '';

            var tx = idb.transaction('contacts', 'readonly');
            var store = tx.objectStore('contacts');
            var request = store.openCursor();

            request.onsuccess = function(e) {
                var cursor = e.target.result;
                if (cursor) {
                    var contact = cursor.value;
                    var tr = document.createElement('tr');
                    tr.innerHTML = '<td>' + escapeHtml(contact.name) + '</td>' +
                        '<td>' + escapeHtml(contact.email) + '</td>' +
                        '<td><button class="btn btn-danger" onclick="deleteIDBContact(' +
                        contact.id + ')">删除</button></td>';
                    body.appendChild(tr);
                    cursor.continue();
                }
            };
        }

        function deleteIDBContact(id) {
            var tx = idb.transaction('contacts', 'readwrite');
            tx.objectStore('contacts').delete(id);
            tx.oncomplete = function() { refreshIDB(); };
        }

        // ========== Notes App ==========
        function getNotes() {
            try {
                return JSON.parse(localStorage.getItem('my_notes') || '[]');
            } catch (e) {
                return [];
            }
        }

        function saveNotes(notes) {
            localStorage.setItem('my_notes', JSON.stringify(notes));
        }

        function addNote() {
            var text = document.getElementById('noteInput').value.trim();
            if (!text) return;

            var notes = getNotes();
            notes.unshift({
                id: Date.now(),
                text: text,
                time: new Date().toLocaleString('zh-CN')
            });
            saveNotes(notes);
            document.getElementById('noteInput').value = '';
            refreshNotes();
        }

        function deleteNote(id) {
            var notes = getNotes().filter(function(n) { return n.id !== id; });
            saveNotes(notes);
            refreshNotes();
        }

        function refreshNotes() {
            var notes = getNotes();
            var list = document.getElementById('notesList');

            if (notes.length === 0) {
                list.innerHTML = '<div class="empty-state"><div class="icon">📝</div><p>还没有备忘录</p></div>';
                return;
            }

            list.innerHTML = notes.map(function(note) {
                return '<div class="note-item">' +
                    '<div class="note-content">' +
                    '<div>' + escapeHtml(note.text) + '</div>' +
                    '<div class="note-time">' + note.time + '</div>' +
                    '</div>' +
                    '<button onclick="deleteNote(' + note.id + ')">×</button>' +
                    '</div>';
            }).join('');
        }

        // ========== Storage Event Listener ==========
        window.addEventListener('storage', function(e) {
            var log = document.getElementById('eventLog');
            var entry = document.createElement('div');
            entry.className = 'log-entry';
            entry.innerHTML = '<span class="log-time">[' + new Date().toLocaleTimeString() + ']</span> ' +
                e.key + ': "' + (e.oldValue || 'null') + '" → "' + (e.newValue || 'null') + '"';
            log.appendChild(entry);
            log.scrollTop = log.scrollHeight;
        });

        // ========== Utility ==========
        function escapeHtml(str) {
            var div = document.createElement('div');
            div.textContent = str;
            return div.innerHTML;
        }

        // ========== Initialize ==========
        refreshLS();
        initIDB();
        refreshNotes();
    </script>
</body>
</html>
```

### 执行预览

四标签页管理界面：
1. **localStorage** — 统计条目数/已用空间，可增删数据，展示所有键值对
2. **sessionStorage** — 同上，标签页关闭后数据消失
3. **IndexedDB** — 联系人管理（姓名+邮箱），演示数据库事务操作
4. **备忘录Demo** — 实际应用案例，自动保存到localStorage，带删除和时间戳

---

## 四、注意事项

| 注意点 | 说明 | 建议 |
|--------|------|------|
| 只存字符串 | 对象会变成"[object Object]" | 用JSON.stringify/parse |
| 容量限制 | localStorage约5MB | 大数据用IndexedDB |
| 同步阻塞 | localStorage是同步操作 | 大量数据可能阻塞主线程 |
| 安全性 | 用户可在DevTools中查看修改 | 敏感数据不要存本地 |
| 隐私模式 | Safari隐私模式写入会报错 | 用try-catch包裹 |
| storage事件 | 只在同源的其他标签页触发 | 当前标签页不触发 |

---

## 五、避坑指南

### ❌ 直接存对象
```javascript
localStorage.setItem('user', { name: '铁蛋' });
// Actual stored: "[object Object]"
```
### ✅ JSON序列化
```javascript
localStorage.setItem('user', JSON.stringify({ name: '铁蛋' }));
var user = JSON.parse(localStorage.getItem('user'));
```

### ❌ 不处理写入失败
```javascript
localStorage.setItem('data', hugeString); // May throw QuotaExceededError
```
### ✅ try-catch保护
```javascript
try {
    localStorage.setItem('data', hugeString);
} catch (e) {
    console.error('存储失败:', e);
    // Fallback: clear old data or use IndexedDB
}
```

### ❌ IndexedDB不用事务
```javascript
var store = db.objectStore('contacts'); // Error!
```
### ✅ 通过事务操作
```javascript
var tx = db.transaction('contacts', 'readwrite');
var store = tx.objectStore('contacts');
store.add(data);
```

---

## 六、练习题

### 🟢 入门
1. 用localStorage实现一个简单的计数器（刷新后保留）。
2. 存储一个对象数组到localStorage，并在页面加载时恢复。

### 🟡 进阶
3. 实现localStorage的封装工具类：支持设过期时间、自动JSON转换。
4. 用IndexedDB实现一个简单的待办事项应用（增删改查）。

### 🔴 挑战
5. 实现离线缓存策略：网络可用时从API获取数据，离线时从IndexedDB读取。
6. 实现跨标签页通信：通过storage事件同步多标签页状态。

---

## 七、知识点总结

```
HTML5本地存储
├── Web Storage
│   ├── localStorage（永久，同源共享）
│   ├── sessionStorage（标签页隔离）
│   ├── API：setItem/getItem/removeItem/clear
│   └── storage事件（跨标签页）
├── IndexedDB
│   ├── Database → Object Store
│   ├── Transaction（事务）
│   ├── Index（索引）
│   ├── Cursor（游标遍历）
│   └── 异步API（request/result模式）
├── 对比选择
│   ├── Cookie：会话token（4KB）
│   ├── localStorage：用户偏好（5MB）
│   └── IndexedDB：大量数据（无限制）
└── 最佳实践
    ├── JSON序列化
    ├── try-catch
    ├── 过期清理
    └── 敏感数据不存本地
```

---

## 八、举一反三

| 场景 | 推荐方案 | 说明 |
|------|----------|------|
| 用户偏好（主题/语言） | localStorage | 小数据、持久化 |
| 表单草稿自动保存 | sessionStorage | 标签页内临时 |
| 离线数据缓存 | IndexedDB | 大量结构化数据 |
| 购物车（未登录） | localStorage | 跨会话保留 |
| 多标签页同步 | localStorage + storage事件 | 跨标签页通知 |
| 文件/图片缓存 | IndexedDB + Blob | 二进制数据 |

---

## 九、参考资料

- [MDN - Web Storage API](https://developer.mozilla.org/zh-CN/docs/Web/API/Web_Storage_API)
- [MDN - IndexedDB API](https://developer.mozilla.org/zh-CN/docs/Web/API/IndexedDB_API)
- [HTML5离线存储](https://developer.mozilla.org/zh-CN/docs/Web/API/Service_Worker_API)

---

## 十、代码演进

### v1 — 简单localStorage
```javascript
// Save user preference
localStorage.setItem('theme', 'dark');
var theme = localStorage.getItem('theme');
```

### v2 — 带封装的存储工具
```javascript
var Store = {
    get: function(key) {
        try { return JSON.parse(localStorage.getItem(key)); }
        catch(e) { return null; }
    },
    set: function(key, value) {
        try { localStorage.setItem(key, JSON.stringify(value)); }
        catch(e) { console.error('Storage full:', e); }
    }
};
```

### v3 — 完整存储方案
```javascript
// localStorage + sessionStorage + IndexedDB unified API
// Auto-expire support, JSON serialization
// Cross-tab sync via storage event
// Offline-first with IndexedDB cache
// Privacy mode detection with try-catch
```

> **演进要点：** 裸API调用 → 工具封装 → 完整存储方案（多存储引擎、过期机制、跨标签同步、离线优先）。