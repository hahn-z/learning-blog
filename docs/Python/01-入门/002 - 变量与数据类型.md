---
title: "002 - 变量与数据类型"
slug: "002-variables-types"
category: "Python入门"
tech_stack: "Python"
created_at: "2026-04-29T06:16:59.214+08:00"
updated_at: "2026-04-29T10:02:47.238+08:00"
reading_time: 27
tags: []
---

## 📊 难度标注

> **难度：** ⭐☆☆☆☆ 入门级
> **前置知识：** Python环境搭建（001）
> **预计用时：** 40-50分钟
> **学习目标：** 理解变量概念，熟练使用6种基本数据类型，掌握类型转换

---

## 📖 概念讲解

### 什么是变量？

变量是存储数据的**容器**。你可以把它想象成一个贴了标签的盒子：

```
标签：name     标签：age     标签：is_student
 ┌──────────┐  ┌──────────┐  ┌──────────┐
 │ "张三"   │  │   25     │  │  True    │
 └──────────┘  └──────────┘  └──────────┘
  string         int           bool
```

Python是**动态类型语言**，不需要提前声明变量类型，解释器会自动推断：

```python
name = "张三"       # Python 知道这是 str
age = 25            # Python 知道这是 int
```

### Python的6种基本数据类型

| 类型 | 关键字 | 示例 | 说明 |
|------|--------|------|------|
| 整数 | `int` | `42`, `-7`, `0` | 任意大小，没有溢出 |
| 浮点数 | `float` | `3.14`, `-0.5`, `1e3` | 64位双精度 |
| 字符串 | `str` | `"hello"`, `'世界'` | 不可变序列 |
| 布尔值 | `bool` | `True`, `False` | 逻辑值 |
| 空值 | `NoneType` | `None` | 表示"没有值" |
| 复数 | `complex` | `3+4j` | 科学计算用 |

### 动态类型 vs 静态类型

```python
# Python: Dynamic typing
x = 42        # x is int
x = "hello"   # Now x is str — totally fine!

# Compare with Java (static typing):
# int x = 42;
# x = "hello";  // ERROR! Type mismatch
```

动态类型的优势是灵活，劣势是IDE提示和错误检查不如静态语言。Python 3.5+ 引入了**类型提示**来弥补：

```python
def greet(name: str) -> str:
    return f"Hello, {name}"
```

### 数值类型详解

**整数 (int)：** Python的int没有大小限制，不会溢出

```python
big = 10 ** 100  # A googol! Works fine in Python
```

**浮点数 (float)：** 64位双精度，注意精度问题

```python
0.1 + 0.2  # = 0.30000000000000004, not 0.3!
```

**布尔值 (bool)：** True本质是1，False本质是0

```python
True + True   # = 2
False + 5     # = 5
```

---

## 🗺️ 知识脑图

```
变量与数据类型
├── 变量基础
│   ├── 赋值：x = 42
│   ├── 命名规则：字母/下划线开头
│   ├── 动态类型：同一变量可变类型
│   └── 多重赋值：a, b = 1, 2
├── 数值类型
│   ├── int → 整数，无大小限制
│   ├── float → 浮点数，64位
│   ├── complex → 复数
│   └── 运算符：+ - * / // % **
├── 文本类型
│   ├── str → 字符串，不可变
│   └── 单引号/双引号/三引号
├── 布尔与空值
│   ├── bool → True/False
│   ├── None → 空值
│   └── Falsy值：0, "", [], None, False
├── 类型操作
│   ├── type() → 查看类型
│   ├── isinstance() → 类型判断
│   └── 类型转换：int(), float(), str()
└── 进阶概念
    ├── 类型提示（Type Hints）
    └── 可变 vs 不可变
```

---

## 💻 完整Python代码

### 变量基础操作

```python
# variables.py - Variable basics

# Basic assignment
name = "Python"
version = 3.12
is_awesome = True

print(f"Name: {name}, type: {type(name)}")
print(f"Version: {version}, type: {type(version)}")
print(f"Awesome: {is_awesome}, type: {type(is_awesome)}")

# Multiple assignment
x, y, z = 1, 2, 3
print(f"x={x}, y={y}, z={z}")

# Swap without temp variable
x, y = y, x
print(f"After swap: x={x}, y={y}")

# Same value assignment
a = b = c = 0
print(f"a={a}, b={b}, c={c}")
```

### 数值类型操作

```python
# numbers.py - Numeric types in depth

# Integer operations
a = 17
b = 5

print(f"a + b = {a + b}")     # 22 - Addition
print(f"a - b = {a - b}")     # 12 - Subtraction
print(f"a * b = {a * b}")     # 85 - Multiplication
print(f"a / b = {a / b}")     # 3.4 - True division (returns float)
print(f"a // b = {a // b}")   # 3  - Floor division
print(f"a % b = {a % b}")     # 2  - Modulo (remainder)
print(f"a ** b = {a ** b}")   # 1419857 - Exponent

# Integer has no limit
big_number = 2 ** 1000
print(f"\n2^1000 has {len(str(big_number))} digits")

# Float precision issue
print(f"\n0.1 + 0.2 = {0.1 + 0.2}")         # 0.30000000000000004
print(f"0.1 + 0.2 == 0.3 = {0.1 + 0.2 == 0.3}")  # False!

# Solution: use round() or Decimal
print(f"round(0.1 + 0.2, 1) = {round(0.1 + 0.2, 1)}")  # 0.3

# Boolean is a subclass of int
print(f"\nTrue + True = {True + True}")     # 2
print(f"False + 5 = {False + 5}")           # 5
print(f"isinstance(True, int) = {isinstance(True, int)}")  # True
```

### 类型检查与转换

```python
# type_conversion.py - Type checking and conversion

# Check types
x = 42
print(f"type(x) = {type(x)}")                        # <class 'int'>
print(f"isinstance(x, int) = {isinstance(x, int)}")  # True
print(f"isinstance(x, (int, float)) = {isinstance(x, (int, float))}")  # True

# Type conversion
# String to number
s = "123"
n = int(s)
f = float("3.14")
print(f"int('123') = {n}, type: {type(n)}")
print(f"float('3.14') = {f}, type: {type(f)}")

# Number to string
print(f"str(42) = '{str(42)}'")
print(f"str(3.14) = '{str(3.14)}'")

# Float to int (truncates, not rounds!)
print(f"int(3.9) = {int(3.9)}")    # 3, not 4!
print(f"int(-3.9) = {int(-3.9)}")  # -3, not -4!

# Boolean conversion
print(f"\nbool(1) = {bool(1)}")       # True
print(f"bool(0) = {bool(0)}")       # False
print(f"bool('') = {bool('')}")      # False
print(f"bool('hello') = {bool('hello')}")  # True
print(f"bool([]) = {bool([])}")      # False
print(f"bool([1]) = {bool([1])}")     # True
print(f"bool(None) = {bool(None)}")   # False
```

### 实用示例：单位转换器

```python
# converter.py - Practical type conversion example

def celsius_to_fahrenheit(celsius: float) -> float:
    # Convert Celsius to Fahrenheit
    return celsius * 9 / 5 + 32

def fahrenheit_to_celsius(fahrenheit: float) -> float:
    # Convert Fahrenheit to Celsius
    return (fahrenheit - 32) * 5 / 9

# Get user input and convert
temp_str = input("Enter temperature: ")
unit = input("Unit (C/F): ").upper()

try:
    temp = float(temp_str)

    if unit == "C":
        result = celsius_to_fahrenheit(temp)
        print(f"{temp}°C = {result:.1f}°F")
    elif unit == "F":
        result = fahrenheit_to_celsius(temp)
        print(f"{temp}°F = {result:.1f}°C")
    else:
        print("Please enter C or F")
except ValueError:
    print(f"'{temp_str}' is not a valid number")
```

---

## 👀 执行预览

**运行 variables.py：**

```
$ python variables.py
Name: Python, type: <class 'str'>
Version: 3.12, type: <class 'float'>
Awesome: True, type: <class 'bool'>
x=1, y=2, z=3
After swap: x=2, y=1
a=0, b=0, c=0
```

**运行 numbers.py：**

```
$ python numbers.py
a + b = 22
a - b = 12
a * b = 85
a / b = 3.4
a // b = 3
a % b = 2
a ** b = 1419857

2^1000 has 302 digits

0.1 + 0.2 = 0.30000000000000004
0.1 + 0.2 == 0.3 = False
round(0.1 + 0.2, 1) = 0.3

True + True = 2
False + 5 = 5
isinstance(True, int) = True
```

**运行 converter.py：**

```
$ python converter.py
Enter temperature: 100
Unit (C/F): C
100.0°C = 212.0°F

$ python converter.py
Enter temperature: abc
Unit (C/F): C
'abc' is not a valid number
```

---

## ⚠️ 注意事项

| 项目 | 说明 | 建议 |
|------|------|------|
| 变量命名 | 区分大小写，`Name` ≠ `name` | 使用 snake_case 风格 |
| 关键字冲突 | `class = 5` 会报错 | 避免使用Python关键字 |
| 浮点精度 | `0.1 + 0.2 ≠ 0.3` | 涉及金钱用 `decimal` 模块 |
| 整除方向 | `-7 // 2 = -4`（向下取整） | 注意负数的整除行为 |
| 类型转换 | `int(3.9) = 3`（截断不是四舍五入） | 需要四舍五入用 `round()` |
| 空值判断 | `x == None` 可以但不够Pythonic | 使用 `x is None` |

### 变量命名规范

```python
# Good naming
user_name = "Alice"
max_count = 100
is_valid = True
PI = 3.14159        # Constants use UPPER_CASE

# Bad naming
x = "Alice"          # Meaningless
userName = "Alice"   # Not Python style (that's camelCase)
2nd_place = "Bob"    # Can't start with number
class = "math"       # Keyword conflict
```

---

## 🚫 避坑指南

### ❌ 浮点数直接比较

```python
if 0.1 + 0.2 == 0.3:
    print("equal")  # Never executes!
```

✅ **正确做法：** 使用 `round()` 或 `math.isclose()`

```python
import math
if math.isclose(0.1 + 0.2, 0.3):
    print("equal")  # Works!

if round(0.1 + 0.2, 10) == round(0.3, 10):
    print("equal")  # Also works
```

### ❌ 用 == 判断 None

```python
x = None
if x == None:  # Works but not recommended
    print("is None")
```

✅ **正确做法：** 使用 `is`

```python
if x is None:   # Pythonic way
    print("is None")
```

### ❌ 忘记转换 input() 的类型

```python
age = input("Age: ")  # Always returns str!
# age * 2 would be "2525" if input was "25", not 50
```

✅ **正确做法：** 显式转换

```python
age = int(input("Age: "))
double = age * 2  # Now it's 50
```

### ❌ 修改变量时忘记重新赋值

```python
x = 10
x + 5   # x is still 10, result discarded!
x = x + 5  # Now x is 15
```

✅ **正确做法：** 使用增强赋值运算符

```python
x = 10
x += 5  # x is now 15
```

### ❌ 链式比较的误解

```python
x = 5
# This is NOT (x > 3) or (x > 10)
# It IS 3 < x and x < 10
result = 3 < x < 10  # True — Python supports chained comparison!
```

---

## 🏋️ 练习题

### 🟢 入门题

**1. 变量交换**

不用临时变量，交换两个变量 `a = 10` 和 `b = 20` 的值。

**2. 类型判断**

编写函数，接收任意值，返回它的类型名称字符串。

```python
def get_type_name(value):
    # Your code here
    pass

print(get_type_name(42))       # "int"
print(get_type_name("hello"))  # "str"
print(get_type_name(True))     # "bool"
```

### 🟡 进阶题

**3. 进制转换器**

编写程序，输入一个十进制整数，输出其二进制、八进制、十六进制表示。

```python
num = 42
# Expected output:
# Binary:      0b101010
# Octal:       0o52
# Hexadecimal: 0x2a
```

**4. Falsy值检测**

编写函数，判断一个值在布尔上下文中是Truthy还是Falsy，覆盖所有Falsy情况。

### 🔴 挑战题

**5. 简易表达式求值器**

不使用 `eval()`，实现一个支持两个操作数的四则运算：

```python
def calc(expression: str) -> float:
    # "3 + 5" -> 8.0
    # "10 / 3" -> 3.333...
    # "-2 * 4" -> -8.0
    pass
```

---

## 🌳 知识点总结

```
变量与数据类型
├── 变量
│   ├── 赋值：=, +=, -=, *=, /=
│   ├── 命名：snake_case, UPPER_CASE
│   ├── 动态类型：type() 查看类型
│   └── 多重赋值：a, b = b, a
├── int（整数）
│   ├── 无大小限制
│   ├── 进制：0b二进制, 0o八进制, 0x十六进制
│   └── 运算：+ - * / // % **
├── float（浮点数）
│   ├── 64位双精度
│   ├── 精度问题：0.1 + 0.2 ≠ 0.3
│   └── 解决：round() 或 decimal 模块
├── bool（布尔值）
│   ├── True = 1, False = 0
│   ├── Falsy: 0, 0.0, "", [], {}, None, False
│   └── Truthy: everything else
├── None（空值）
│   ├── 表示"没有值"
│   ├── 用 is 判断，不用 ==
│   └── 函数默认返回 None
├── 类型操作
│   ├── type(obj) → 获取类型
│   ├── isinstance(obj, type) → 类型判断
│   └── int(), float(), str(), bool() → 类型转换
└── 进阶
    ├── Type Hints: x: int = 42
    └── 可变性：int/float/str/bool 不可变
```

---

## 🔄 举一反三

| 你学到的 | 可以应用到 | 例子 |
|----------|-----------|------|
| 变量赋值 | 存储用户输入 | `user_input = input()` |
| 类型转换 | 解析配置文件 | `port = int(config["port"])` |
| 布尔判断 | 条件分支 | `if is_valid:` |
| 浮点精度 | 金融计算 | `Decimal("0.1") + Decimal("0.2")` |
| 多重赋值 | 遍历字典 | `for k, v in dict.items()` |
| isinstance() | 函数参数校验 | `if isinstance(x, (int, float))` |

---

## 📚 参考资料

- [Python数据模型](https://docs.python.org/3/reference/datamodel.html)
- [内置类型文档](https://docs.python.org/3/library/stdtypes.html)
- [Type Hints指南](https://docs.python.org/3/library/typing.html)
- [IEEE 754浮点数](https://en.wikipedia.org/wiki/IEEE_754)

---

## 🔬 代码演进

### v1：基本变量

```python
# v1: Simple variables
name = "Python"
age = 33
print(name, age)
```

### v2：类型检查 + 运算

```python
# v2: Type-aware with operations
name = "Python"
age = 33
print(f"{name} is {age} years old")
print(f"name type: {type(name)}, age type: {type(age)}")

# Type conversion
age_str = str(age)
print(f"age as string: '{age_str}'")
```

### v3：完整的类型系统探索

```python
# v3: Complete type system explorer
def explore_value(value, label=""):
    # Display detailed info about any value
    print(f"\n--- {label or 'Value'} ---")
    print(f"  Value:    {repr(value)}")
    print(f"  Type:     {type(value).__name__}")
    print(f"  Bool:     {bool(value)}")
    if isinstance(value, (int, float)):
        print(f"  +1:       {value + 1}")
        print(f"  *2:       {value * 2}")

# Explore different types
explore_value(42, "Integer")
explore_value(3.14, "Float")
explore_value(True, "Boolean")
explore_value(None, "None")
explore_value("", "Empty String")
explore_value("Hello", "String")
explore_value(2 ** 100, "Big Integer")
```

> **下一步：** 掌握了变量和数据类型后，下一篇文章我们将深入学习 [字符串操作详解](/posts/003-strings)，解锁Python强大的文本处理能力！
