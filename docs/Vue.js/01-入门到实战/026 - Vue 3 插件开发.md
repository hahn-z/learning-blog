---
title: "026 - Vue 3 插件开发"
slug: "026-plugins"
category: "Vue.js入门到实战"
tech_stack: "Vue.js"
created_at: "2026-04-26T17:51:05.389+08:00"
updated_at: "2026-04-29T10:02:46.714+08:00"
reading_time: 23
tags: ["Vue.js", "Vue 3"]
---

## 难度标注

> 🟡 **中高级** — 需要 Vue 3 基础、了解 app 实例和 provide/inject

## 概念讲解

### 什么是 Vue 插件？

插件是**自包含的功能模块**，通过 `app.use()` 注册时向 Vue 应用添加全局功能。插件可以：

- 注册全局组件
- 注册全局指令
- 提供 provide/inject 服务
- 添加全局属性
- 执行初始化逻辑

### 插件的结构

```javascript
// Plugin pattern
const myPlugin = {
  install(app, options) {
    // app: Vue app instance
    // options: user-provided config
  }
}

// Usage: app.use(myPlugin, { theme: 'dark' })
```

### 插件 vs 组件库

| 概念 | 范围 | 例子 |
|------|------|------|
| 插件 | 添加功能（组件+指令+服务） | vuetify, vue-router |
| 组件库 | 主要是 UI 组件 | element-plus |
| 指令库 | 主要是指令 | @vueuse/integrations |

## 脑图

```
Vue 3 插件开发
├── 基本结构
│   ├── install(app, options)
│   └── app.use(plugin, config)
├── 插件能力
│   ├── app.component() — 注册组件
│   ├── app.directive() — 注册指令
│   ├── app.provide() — 全局服务
│   ├── app.config.globalProperties — 全局属性
│   └── app.mixin() — 混入（不推荐）
├── 设计模式
│   ├── 配置驱动
│   ├── 工厂函数
│   └── 组合式 API 封装
├── 打包分发
│   ├── Vite Library Mode
│   ├── peerDependencies: vue
│   └── Tree-shaking 友好
└── 实战
    ├── Toast 通知插件
    ├── Loading 加载插件
    └── 权限插件
```

## 完整代码

```javascript
// plugins/toast/index.js — Toast notification plugin
import ToastContainer from './ToastContainer.vue'
import { createApp, ref } from 'vue'

// Plugin state (shared across all toast instances)
const toasts = ref([])
let toastId = 0

function addToast(message, options = {}) {
  const id = toastId++
  const toast = {
    id,
    message,
    type: options.type || 'info',
    duration: options.duration || 3000
  }
  toasts.value.push(toast)

  // Auto-remove after duration
  if (toast.duration > 0) {
    setTimeout(() => removeToast(id), toast.duration)
  }

  return id
}

function removeToast(id) {
  toasts.value = toasts.value.filter(t => t.id !== id)
}

// Exported toast service
export function useToast() {
  return {
    success: (msg, opts) => addToast(msg, { ...opts, type: 'success' }),
    error: (msg, opts) => addToast(msg, { ...opts, type: 'error' }),
    info: (msg, opts) => addToast(msg, { ...opts, type: 'info' }),
    warning: (msg, opts) => addToast(msg, { ...opts, type: 'warning' }),
    remove: removeToast
  }
}

// Plugin install function
export default {
  install(app, options = {}) {
    // 1. Register toast container component
    app.component('ToastContainer', ToastContainer)

    // 2. Provide toast service globally
    app.provide('toast', useToast())

    // 3. Global property (for Options API)
    app.config.globalProperties.$toast = useToast()
  }
}
```

```vue
<!-- plugins/toast/ToastContainer.vue -->
<script setup>
import { toasts, removeToast } from './index'
</script>

<template>
  <Teleport to="body">
    <div class="toast-container">
      <TransitionGroup name="toast">
        <div
          v-for="toast in toasts"
          :key="toast.id"
          :class="['toast', `toast-${toast.type}`]"
          @click="removeToast(toast.id)"
        >
          <span class="toast-icon">
            {{ toast.type === 'success' ? '✓' : toast.type === 'error' ? '✕' : 'ℹ' }}
          </span>
          <span>{{ toast.message }}</span>
        </div>
      </TransitionGroup>
    </div>
  </Teleport>
</template>

<style scoped>
.toast-container {
  position: fixed;
  top: 20px;
  right: 20px;
  z-index: 9999;
  display: flex;
  flex-direction: column;
  gap: 8px;
}
.toast {
  padding: 12px 20px;
  border-radius: 8px;
  color: white;
  display: flex;
  align-items: center;
  gap: 8px;
  cursor: pointer;
  min-width: 200px;
}
.toast-success { background: #42b883; }
.toast-error { background: #e74c3c; }
.toast-info { background: #3498db; }
.toast-warning { background: #f39c12; }
.toast-enter-active, .toast-leave-active { transition: all 0.3s ease; }
.toast-enter-from { opacity: 0; transform: translateX(100px); }
.toast-leave-to { opacity: 0; transform: translateX(100px); }
</style>
```

```javascript
// plugins/loading/index.js — Loading directive plugin
export default {
  install(app) {
    app.directive('loading', {
      mounted(el, binding) {
        // Create loading overlay
        const overlay = document.createElement('div')
        overlay.className = 'v-loading-overlay'
        overlay.innerHTML = '<div class="spinner"></div>'
        el.style.position = 'relative'
        el._loadingOverlay = overlay

        if (binding.value) {
          el.appendChild(overlay)
        }
      },

      updated(el, binding) {
        if (binding.value && !el.contains(el._loadingOverlay)) {
          el.appendChild(el._loadingOverlay)
        } else if (!binding.value && el.contains(el._loadingOverlay)) {
          el.removeChild(el._loadingOverlay)
        }
      },

      unmounted(el) {
        el._loadingOverlay = null
      }
    })
  }
}
```

```javascript
// plugins/i18n/index.js — Simple i18n plugin
import { ref, computed } from 'vue'

const locale = ref('zh')
const messages = ref({})

export function useI18n() {
  const t = (key) => {
    return messages.value[locale.value]?.[key] || key
  }
  const setLocale = (newLocale) => {
    locale.value = newLocale
  }
  return { t, locale: computed(() => locale.value), setLocale }
}

export default {
  install(app, options = {}) {
    // Set initial messages
    if (options.messages) {
      messages.value = options.messages
    }
    if (options.locale) {
      locale.value = options.locale
    }

    // Provide globally
    app.provide('i18n', useI18n())
    app.config.globalProperties.$t = (key) =>
      messages.value[locale.value]?.[key] || key
  }
}
```

```javascript
// main.js — Using all plugins
import { createApp } from 'vue'
import App from './App.vue'

// Plugins
import ToastPlugin from './plugins/toast'
import LoadingPlugin from './plugins/loading'
import I18nPlugin from './plugins/i18n'

const app = createApp(App)

app.use(ToastPlugin)
app.use(LoadingPlugin)
app.use(I18nPlugin, {
  locale: 'zh',
  messages: {
    zh: { hello: '你好', goodbye: '再见' },
    en: { hello: 'Hello', goodbye: 'Goodbye' }
  }
})

app.mount('#app')
```

```vue
<!-- App.vue — Using plugins in components -->
<script setup>
import { useToast } from './plugins/toast'
import { useI18n } from './plugins/i18n'
import { ref } from 'vue'

const toast = useToast()
const { t, setLocale } = useI18n()
const loading = ref(false)

async function saveData() {
  loading.value = true
  try {
    await new Promise(r => setTimeout(r, 2000))
    toast.success('保存成功！')
  } catch {
    toast.error('保存失败')
  } finally {
    loading.value = false
  }
}
</script>

<template>
  <div v-loading="loading">
    <h1>{{ t('hello') }}</h1>
    <button @click="setLocale('en')">English</button>
    <button @click="setLocale('zh')">中文</button>
    <button @click="saveData">Save</button>
    <button @click="toast.info('Just a message')">Show Toast</button>
  </div>
  <ToastContainer />
</template>
```

## 执行预览

```
app.use(ToastPlugin):
  → 注册 ToastContainer 组件
  → 提供 useToast() 服务

点击 "Save":
  → loading = true → v-loading 显示加载遮罩
  → 2秒后 loading = false → 遮罩消失
  → toast.success('保存成功！') → 右上角弹出绿色通知
  → 3秒后通知自动消失

切换语言:
  → setLocale('en') → "你好" → "Hello"
  → setLocale('zh') → "Hello" → "你好"
```

## 注意事项

| 注意点 | 说明 |
|--------|------|
| install 必须有 | 插件必须有 `install` 方法才能用 `app.use()` |
| app.use 自动防重 | 同一插件多次 use 只执行一次 install |
| provide/inject 优先 | 比 globalProperties 更适合 Composition API |
| Tree-shaking | 导出单独的 composable（如 useToast）便于按需引入 |
| peerDependencies | 插件包的 vue 应设为 peerDependency，不打包进产物 |

## 避坑指南

| ❌ 错误做法 | ✅ 正确做法 |
|------------|-----------|
| 所有功能塞进一个插件 | 按功能拆分多个插件，按需注册 |
| 用 globalProperties 挂载所有东西 | provide/inject + composable 更类型安全 |
| 插件中硬编码配置 | install(app, options) 接受用户配置 |
| 不清理副作用 | 提供卸载机制或文档说明 |
| 打包时包含 vue | vue 设为 peerDependency，external 处理 |

## 练习题

### 🟢 基础
1. 创建一个简单的插件，注册一个全局组件 `<MyButton>`
2. 创建一个插件，通过 provide 提供一个全局配置对象

### 🟡 进阶
3. 实现一个完整的 Toast 通知插件（如本文代码）
4. 实现一个 v-loading 指令插件

### 🔴 挑战
5. 将你的插件打包为 npm 包：配置 Vite Library Mode，发布到 npm

## 知识点总结

```
Vue 3 插件开发
├── 基本结构
│   ├── { install(app, options) {} }
│   └── app.use(plugin, config)
├── 插件能力
│   ├── app.component() — 全局组件
│   ├── app.directive() — 全局指令
│   ├── app.provide() — 全局服务
│   └── globalProperties — 全局属性
├── Composable 导出
│   ├── export function useXxx()
│   └── 便于 Tree-shaking
└── 打包
    ├── vite.config.js lib mode
    ├── peerDependencies: vue
    └── external: ['vue']
```

## 举一反三

| 场景 | 插件设计 | 关键 API |
|------|---------|----------|
| 通知系统 | Toast plugin | provide + Teleport |
| 加载状态 | v-loading 指令 | app.directive() |
| 国际化 | i18n plugin | provide + reactive |
| 主题系统 | Theme plugin | CSS variables + provide |
| 权限系统 | Auth plugin | directive + provide |
| 数据埋点 | Analytics plugin | 全局 mixin 替代方案 |

## 参考资料

- [Vue 3 官方文档 - Plugins](https://vuejs.org/guide/reusability/plugins.html)
- [Vue 3 API - app.use()](https://vuejs.org/api/application.html#app-use)
- [Vite Library Mode](https://vitejs.dev/guide/build.html#library-mode)

## 代码演进

### v1 — 最简插件
```javascript
export default {
  install(app) {
    app.component('MyBtn', MyButton)
  }
}
```

### v2 — 带配置和服务
```javascript
export default {
  install(app, options) {
    app.provide('config', options)
    app.directive('loading', LoadingDirective)
  }
}
export function useService() { /* composable */ }
```

### v3 — 完整方案（本文代码）
- Toast 通知系统
- Loading 指令
- i18n 国际化
- Composable 导出
- 打包配置
