---
title: "022 - 模板引用ref"
slug: "022-template-refs"
category: "Vue.js入门到实战"
tech_stack: "Vue.js"
created_at: "2026-04-26T17:44:16.838+08:00"
updated_at: "2026-04-29T10:02:46.674+08:00"
reading_time: 16
tags: ["Vue.js", "Vue 3"]
---

## 难度标注

> 🟢 **初级** — Vue 3 基础即可

## 概念讲解

### 什么是模板引用？

模板引用（Template Ref）是 Vue 中**直接访问 DOM 元素或子组件实例**的方式。通过 `ref` attribute 和 `ref()` 函数配合使用。

### 何时使用？

- 需要直接操作 DOM（聚焦、滚动、测量尺寸）
- 需要调用子组件的方法
- 使用第三方 DOM 库（图表、地图等）
- v-for 场景获取元素列表

### 核心规则

> 模板引用是**逃生舱**，优先使用声明式的 Vue 方式（v-bind、v-on），只在必要时使用 ref。

## 脑图

```
模板引用 ref
├── DOM 引用
│   ├── <input ref="inputRef">
│   └── const inputRef = ref(null)
├── 组件引用
│   ├── <Child ref="childRef">
│   └── childRef.value.childMethod()
├── v-for 中的 ref
│   ├── <div v-for ref="itemRefs">
│   └── itemRefs.value = [] (自动填充)
├── 函数 ref
│   ├── <div :ref="el => node = el">
│   └── 动态绑定
└── 注意事项
    ├── onMounted 后才能访问
    ├── defineExpose 暴露组件方法
    └── v-if 导致 ref 为 null
```

## 完整代码

```vue
<!-- components/TemplateRefDemo.vue — Basic DOM ref -->
<script setup>
import { ref, onMounted } from 'vue'

// Declare a ref to hold the DOM element
const inputRef = ref(null)

onMounted(() => {
  // Auto-focus the input after mount
  inputRef.value?.focus()
})

function clearInput() {
  inputRef.value.value = ''
  inputRef.value.focus()
}
</script>

<template>
  <div>
    <input ref="inputRef" placeholder="Auto-focused on mount" />
    <button @click="clearInput">Clear</button>
  </div>
</template>
```

```vue
<!-- components/MeasureBox.vue — Measuring DOM dimensions -->
<script setup>
import { ref, onMounted } from 'vue'

const boxRef = ref(null)
const dimensions = ref({ width: 0, height: 0 })

onMounted(() => {
  updateDimensions()
  // Observe size changes
  const observer = new ResizeObserver(() => updateDimensions())
  observer.observe(boxRef.value)
})

function updateDimensions() {
  if (boxRef.value) {
    const rect = boxRef.value.getBoundingClientRect()
    dimensions.value = {
      width: Math.round(rect.width),
      height: Math.round(rect.height)
    }
  }
}
</script>

<template>
  <div
    ref="boxRef"
    class="resize-box"
    :style="{ width: '50%', minHeight: '100px' }"
  >
    <p>Width: {{ dimensions.width }}px</p>
    <p>Height: {{ dimensions.height }}px</p>
    <p>Try resizing the window!</p>
  </div>
</template>

<style scoped>
.resize-box {
  border: 2px dashed #42b883;
  padding: 20px;
  resize: both;
  overflow: auto;
}
</style>
```

```vue
<!-- components/ChildWithExpose.vue — Component ref with defineExpose -->
<script setup>
import { ref } from 'vue'

const count = ref(0)

// Internal state — not exposed by default
const secret = ref('hidden')

// Expose specific methods and state to parent
defineExpose({
  count,
  increment() { count.value++ },
  reset() { count.value = 0 }
})
</script>

<template>
  <div>Child Count: {{ count }}</div>
</template>
```

```vue
<!-- components/ParentComponent.vue — Using child component ref -->
<script setup>
import { ref } from 'vue'
import ChildWithExpose from './ChildWithExpose.vue'

const childRef = ref(null)

function callChildMethod() {
  // Access exposed child methods
  childRef.value?.increment()
}

function resetChild() {
  childRef.value?.reset()
}
</script>

<template>
  <div>
    <ChildWithExpose ref="childRef" />
    <button @click="callChildMethod">Call Child.increment()</button>
    <button @click="resetChild">Reset Child</button>
  </div>
</template>
```

```vue
<!-- components/VForRefs.vue — Refs in v-for -->
<script setup>
import { ref, onMounted } from 'vue'

const items = ref(['Apple', 'Banana', 'Cherry'])
const itemRefs = ref([])

// itemRefs is automatically populated after render
onMounted(() => {
  console.log('All items:', itemRefs.value)
  // Scroll to the second item
  itemRefs.value[1]?.scrollIntoView({ behavior: 'smooth' })
})

function scrollToItem(index) {
  itemRefs.value[index]?.scrollIntoView({ behavior: 'smooth' })
}
</script>

<template>
  <div class="list-container">
    <div
      v-for="(item, index) in items"
      :key="item"
      :ref="el => { if (el) itemRefs[index] = el }"
      class="list-item"
    >
      {{ item }}
      <button @click="scrollToItem(index)">Scroll here</button>
    </div>
  </div>
</template>

<style scoped>
.list-container { height: 200px; overflow-y: auto; }
.list-item { padding: 40px; border: 1px solid #eee; }
</style>
```

## 执行预览

```
页面加载:
  → inputRef.value.focus() 自动聚焦输入框
  → MeasureBox 显示初始尺寸: 400x100

用户调整窗口:
  → ResizeObserver 触发
  → dimensions 更新为新尺寸

点击 "Call Child.increment()":
  → childRef.value.increment() 调用子组件方法
  → 子组件 count: 0 → 1

v-for refs:
  → itemRefs = [div, div, div]
  → scrollToItem(1) 平滑滚动到 Banana
```

## 注意事项

| 注意点 | 说明 |
|--------|------|
| 初始值 null | ref 挂载前为 null，使用可选链 `?.` |
| onMounted 访问 | DOM ref 只在 onMounted 后可用 |
| defineExpose | `<script setup>` 组件默认不暴露任何内容，需显式 defineExpose |
| v-if 影响 | v-if 为 false 时 ref 为 null，注意空值判断 |
| 函数 ref 更安全 | v-for 中推荐使用函数 ref `:ref="el => ..."` |
| 不滥用 | 优先用 v-model、emit 等声明式方式，ref 是逃生舱 |

## 避坑指南

| ❌ 错误做法 | ✅ 正确做法 |
|------------|-----------|
| `const el = ref(null); console.log(el.value)` 在 setup 中 | 在 onMounted 中访问 |
| 直接访问子组件私有状态 | 使用 defineExpose 暴露公共接口 |
| v-for 中 `ref="items"` 期望数组 | 使用函数 ref `:ref="el => list[i] = el"` |
| 不检查 null 直接 `.focus()` | 用可选链 `inputRef.value?.focus()` |
| 用 ref 替代 v-model | 能用 v-model 就不用 ref 操作 DOM |

## 练习题

### 🟢 基础
1. 创建一个自动聚焦的 input 组件
2. 创建一个子组件，通过 ref 从父组件调用其方法

### 🟡 进阶
3. 实现一个 useElementSize composable，自动获取元素尺寸并响应窗口变化
4. 在 v-for 列表中实现"滚动到指定项"功能

### 🔴 挑战
5. 实现一个点击外部关闭的下拉菜单组件，使用 ref 获取容器元素判断点击是否在外部

## 知识点总结

```
模板引用
├── DOM ref
│   ├── <input ref="el">
│   ├── const el = ref(null)
│   └── onMounted → el.value 可用
├── 组件 ref
│   ├── <Child ref="child">
│   ├── defineExpose({ method })
│   └── child.value.method()
├── v-for ref
│   ├── :ref="el => list[i] = el"
│   └── 函数 ref 模式
└── 原则
    ├── 声明式优先
    ├── ref 是逃生舱
    └── 必须检查 null
```

## 举一反三

| 场景 | ref 类型 | 关键点 |
|------|---------|--------|
| 自动聚焦输入框 | DOM ref | onMounted + focus() |
| 调用子组件方法 | 组件 ref | defineExpose |
| 图表初始化 | DOM ref | new Chart(el) |
| 测量元素尺寸 | DOM ref | getBoundingClientRect() |
| 列表滚动定位 | v-for ref | scrollIntoView() |
| 点击外部关闭 | DOM ref | contains(event.target) |

## 参考资料

- [Vue 3 官方文档 - Template Refs](https://vuejs.org/guide/essentials/template-refs.html)
- [Vue 3 API - defineExpose](https://vuejs.org/api/sfc-macros.html#defineexpose)

## 代码演进

### v1 — 基础 DOM ref
```vue
<input ref="input" />
<script setup>const input = ref(null)</script>
```

### v2 — 组件 ref + defineExpose
```vue
<Child ref="child" />
<script setup>
  // Child: defineExpose({ method })
  // Parent: child.value.method()
</script>
```

### v3 — 完整方案（本文代码）
- 函数 ref
- ResizeObserver
- v-for ref
- composable 模式
