---
title: "022 - Gin 日志管理"
slug: "022-gin-log"
category: "Gin框架"
tech_stack: "Golang"
created_at: "2026-04-25T23:13:51.243+08:00"
updated_at: "2026-04-29T10:02:45.756+08:00"
reading_time: 29
tags: []
---

# 022 - Gin 日志管理

> **难度：** ⭐⭐⭐（较高）
> **前置知识：** Gin 中间件、Go io 接口、结构化日志概念
> **预计阅读时间：** 25 分钟

---

## 一、概念讲解

日志是生产环境的"黑匣子"。Gin 默认的 `gin.Logger()` 只输出文本格式日志，在生产环境中远远不够。我们需要结构化日志、请求追踪、日志轮转和性能分级。

**日志层次体系：**

| 层级 | 用途 | 示例 |
|------|------|------|
| 请求日志 | 每个 HTTP 请求的进出记录 | method, path, status, latency |
| 业务日志 | 业务逻辑关键节点 | order_created, payment_success |
| 错误日志 | 异常和错误堆栈 | db_connection_failed |
| 审计日志 | 安全敏感操作 | user_login, permission_change |

**技术选型对比：**

| 方案 | 性能 | 结构化 | 适用场景 |
|------|------|--------|---------|
| `log/slog` (Go 1.21+) | 中 | ✅ 原生 | 标准库优先、新项目推荐 |
| `uber-go/zap` | 极高 | ✅ | 高并发、高性能要求 |
| `gin.Logger()` | 低 | ❌ | 仅开发环境 |

---

## 二、脑图

```
日志管理
├── slog 结构化日志
│   ├── JSON Handler
│   ├── 替换 Gin 默认日志
│   └── Context 传递
├── 请求日志中间件
│   ├── method/path/status/latency
│   ├── 请求体记录(谨慎)
│   └── 响应体包装
├── zap 高性能日志
│   ├── Logger(强类型) vs Sugar(灵活)
│   ├── Development/Production 预设
│   └── 自定义 Encoder
├── 日志轮转
│   ├── lumberjack 按大小切割
│   ├── 按日期归档
│   └── 自动清理策略
├── 请求 ID 追踪
│   ├── UUID 生成
│   ├── Context 传递
│   └── 全链路串联
└── 慢请求日志
    ├── 阈值配置
    ├── 自动告警
    └── 与 APM 集成
```

---

## 三、完整代码

### v1：slog 结构化日志 + 请求 ID

```go
// main.go - v1 slog integration with request ID
package main

import (
	"log/slog"
	"net/http"
	"os"
	"time"

	"github.com/gin-gonic/gin"
	"github.com/google/uuid"
)

func main() {
	// Configure structured JSON logger
	logger := slog.New(slog.NewJSONHandler(os.Stdout, &slog.HandlerOptions{
		Level: slog.LevelInfo,
	}))
	slog.SetDefault(logger)

	r := gin.New() // no default middleware
	r.Use(gin.Recovery())
	r.Use(requestIDMiddleware())
	r.Use(slogRequestMiddleware())

	r.GET("/ping", func(c *gin.Context) {
		slog.InfoContext(c.Request.Context(), "ping received",
			"path", c.Request.URL.Path)
		c.JSON(http.StatusOK, gin.H{"message": "pong"})
	})

	r.GET("/users/:id", func(c *gin.Context) {
		slog.InfoContext(c.Request.Context(), "fetching user",
			"user_id", c.Param("id"))
		c.JSON(http.StatusOK, gin.H{
			"id": c.Param("id"), "name": "Alice",
		})
	})

	r.Run(":8080")
}

// requestIDMiddleware generates and propagates a request ID
func requestIDMiddleware() gin.HandlerFunc {
	return func(c *gin.Context) {
		requestID := c.GetHeader("X-Request-ID")
		if requestID == "" {
			requestID = uuid.New().String()
		}
		c.Set("request_id", requestID)
		c.Header("X-Request-ID", requestID)
		c.Next()
	}
}

// slogRequestMiddleware logs every request with slog
func slogRequestMiddleware() gin.HandlerFunc {
	return func(c *gin.Context) {
		start := time.Now()
		path := c.Request.URL.Path
		query := c.Request.URL.RawQuery

		c.Next()

		latency := time.Since(start)
		slog.Info("request",
			"request_id", c.GetString("request_id"),
			"status", c.Writer.Status(),
			"method", c.Request.Method,
			"path", path,
			"query", query,
			"latency_ms", latency.Milliseconds(),
			"client_ip", c.ClientIP(),
			"user_agent", c.Request.UserAgent(),
			"body_size", c.Writer.Size(),
			"errors", c.Errors.ByType(gin.ErrorTypePrivate).String(),
		)
	}
}
```

### v2：zap + lumberjack 日志轮转

```go
// main.go - v2 zap + lumberjack with rotation
package main

import (
	"net/http"
	"time"

	"github.com/gin-gonic/gin"
	"github.com/google/uuid"
	"go.uber.org/zap"
	"go.uber.org/zap/zapcore"
	"gopkg.in/natefinch/lumberjack.v2"
)

var logger *zap.Logger

func main() {
	initLogger()
	defer logger.Sync()

	r := gin.New()
	r.Use(gin.Recovery())
	r.Use(requestIDMiddleware())
	r.Use(zapRequestMiddleware())
	r.Use(slowRequestMiddleware(500*time.Millisecond))

	r.GET("/ping", func(c *gin.Context) {
		logger.Info("ping",
			zap.String("request_id", c.GetString("request_id")))
		c.JSON(http.StatusOK, gin.H{"message": "pong"})
	})

	r.GET("/heavy", func(c *gin.Context) {
		time.Sleep(800 * time.Millisecond) // simulate slow
		c.JSON(http.StatusOK, gin.H{"done": true})
	})

	r.Run(":8080")
}

// initLogger sets up zap with lumberjack rotation
func initLogger() {
	// Lumberjack for log rotation
	writer := &lumberjack.Logger{
		Filename:   "logs/app.log",
		MaxSize:    100, // MB per file
		MaxBackups: 10,  // keep 10 old files
		MaxAge:     30,  // days
		Compress:   true,
	}

	core := zapcore.NewCore(
		zapcore.NewJSONEncoder(zap.NewProductionEncoderConfig()),
		zapcore.AddSync(writer),
		zapcore.InfoLevel,
	)

	logger = zap.New(core, zap.AddCaller(), zap.AddStacktrace(zap.ErrorLevel))
}

func requestIDMiddleware() gin.HandlerFunc {
	return func(c *gin.Context) {
		requestID := c.GetHeader("X-Request-ID")
		if requestID == "" {
			requestID = uuid.New().String()
		}
		c.Set("request_id", requestID)
		c.Header("X-Request-ID", requestID)
		c.Next()
	}
}

func zapRequestMiddleware() gin.HandlerFunc {
	return func(c *gin.Context) {
		start := time.Now()
		c.Next()
		latency := time.Since(start)

		logger.Info("request",
			zap.String("request_id", c.GetString("request_id")),
			zap.String("method", c.Request.Method),
			zap.String("path", c.Request.URL.Path),
			zap.Int("status", c.Writer.Status()),
			zap.Duration("latency", latency),
			zap.String("client_ip", c.ClientIP()),
		)
	}
}

// slowRequestMiddleware logs warnings for requests exceeding threshold
func slowRequestMiddleware(threshold time.Duration) gin.HandlerFunc {
	return func(c *gin.Context) {
		start := time.Now()
		c.Next()
		latency := time.Since(start)

		if latency > threshold {
			logger.Warn("slow request detected",
				zap.String("request_id", c.GetString("request_id")),
				zap.String("method", c.Request.Method),
				zap.String("path", c.Request.URL.Path),
				zap.Duration("latency", latency),
				zap.Duration("threshold", threshold),
			)
		}
	}
}
```

### v3：slog + zap 双轨 + 分级输出

```go
// main.go - v3 dual logger with level-based output
package main

import (
	"io"
	"log/slog"
	"net/http"
	"os"
	"time"

	"github.com/gin-gonic/gin"
	"github.com/google/uuid"
	"go.uber.org/zap"
	"go.uber.org/zap/zapcore"
	"gopkg.in/natefinch/lumberjack.v2"
)

var zapLogger *zap.Logger

func main() {
	// Setup slog to stdout for development visibility
	slog.SetDefault(slog.New(slog.NewJSONHandler(os.Stdout, &slog.HandlerOptions{
		Level: slog.LevelDebug,
	})))

	// Setup zap to file for production persistence
	initZapLogger()
	defer zapLogger.Sync()

	r := gin.New()
	r.Use(gin.Recovery())
	r.Use(requestIDMiddleware())
	r.Use(dualLogMiddleware())
	r.Use(slowRequestMiddleware(500*time.Millisecond))

	r.GET("/ping", func(c *gin.Context) {
		slog.InfoContext(c.Request.Context(), "ping")
		c.JSON(http.StatusOK, gin.H{"message": "pong"})
	})

	r.GET("/error", func(c *gin.Context) {
		slog.ErrorContext(c.Request.Context(), "simulated error",
			"detail", "something went wrong")
		c.JSON(http.StatusInternalServerError, gin.H{"error": "internal"})
	})

	r.Run(":8080")
}

func initZapLogger() {
	infoWriter := &lumberjack.Logger{
		Filename: "logs/info.log", MaxSize: 100, MaxBackups: 5, Compress: true,
	}
	errorWriter := &lumberjack.Logger{
		Filename: "logs/error.log", MaxSize: 50, MaxBackups: 10, Compress: true,
	}

	// Info and above → info.log, Error and above → error.log
	infoCore := zapcore.NewCore(
		zapcore.NewJSONEncoder(zap.NewProductionEncoderConfig()),
		zapcore.AddSync(infoWriter),
		zapcore.InfoLevel,
	)
	errorCore := zapcore.NewCore(
		zapcore.NewJSONEncoder(zap.NewProductionEncoderConfig()),
		zapcore.AddSync(errorWriter),
		zapcore.ErrorLevel,
	)

	zapLogger = zap.New(zapcore.NewTee(infoCore, errorCore),
		zap.AddCaller(), zap.AddStacktrace(zap.ErrorLevel))
}

func requestIDMiddleware() gin.HandlerFunc {
	return func(c *gin.Context) {
		rid := c.GetHeader("X-Request-ID")
		if rid == "" {
			rid = uuid.New().String()
		}
		c.Set("request_id", rid)
		c.Header("X-Request-ID", rid)
		c.Next()
	}
}

func dualLogMiddleware() gin.HandlerFunc {
	return func(c *gin.Context) {
		start := time.Now()
		c.Next()
		latency := time.Since(start)
		rid := c.GetString("request_id")

		// slog to stdout
		slog.Info("request", "request_id", rid, "status", c.Writer.Status(),
			"latency_ms", latency.Milliseconds(), "path", c.Request.URL.Path)

		// zap to file
		zapLogger.Info("request",
			zap.String("request_id", rid),
			zap.String("method", c.Request.Method),
			zap.String("path", c.Request.URL.Path),
			zap.Int("status", c.Writer.Status()),
			zap.Duration("latency", latency),
			zap.String("client_ip", c.ClientIP()),
		)
	}
}

func slowRequestMiddleware(threshold time.Duration) gin.HandlerFunc {
	return func(c *gin.Context) {
		start := time.Now()
		c.Next()
		if d := time.Since(start); d > threshold {
			slog.Warn("slow request", "path", c.Request.URL.Path, "latency", d)
			zapLogger.Warn("slow request",
				zap.String("path", c.Request.URL.Path),
				zap.Duration("latency", d))
		}
	}
}
```

---

## 四、执行预览

```bash
# v1: slog 基础
$ go run main.go
# Output (JSON):
# {"time":"2024-01-15T10:30:00Z","level":"INFO","msg":"request","request_id":"abc-123","status":200,"method":"GET","path":"/ping","latency_ms":1}

# v2: zap + rotation
$ mkdir -p logs && go run main.go
# logs/app.log grows until 100MB, then rotates to app-1234567890.log.gz

# v3: dual output
$ go run main.go
# stdout → colored JSON for development
# logs/info.log → all info+ messages
# logs/error.log → only errors and panics

# Slow request warning:
# {"level":"WARN","msg":"slow request","path":"/heavy","latency":"800ms","threshold":"500ms"}
```

---

## 五、注意事项

| 事项 | 说明 |
|------|------|
| 日志不要记敏感信息 | 密码、Token、身份证号必须脱敏或排除 |
| 请求体日志要谨慎 | 大文件上传、JSON 请求体可能很大，设上限 |
| zap Logger vs Sugar | Logger 性能更高（强类型），Sugar 更灵活，按需选择 |
| lumberjack 并发安全 | 内部有 mutex，多 goroutine 写入没问题 |
| 请求 ID 传播 | 微服务场景需在 HTTP/gRPC 调用间透传 X-Request-ID |
| slog 最低版本 | 需要 Go 1.21+，老项目用 zap |

---

## 六、避坑指南

| ❌ 错误做法 | ✅ 正确做法 |
|------------|-----------|
| 用 `fmt.Println` 做日志 | 用 slog/zap 结构化输出，方便日志平台检索 |
| 日志打到 stdout 不落盘 | 配合 lumberjack 写文件，或发送到日志平台 |
| 每个请求打印完整请求体 | 设置大小上限，超过只记录前 N 字节 |
| zap 不调用 `Sync()` | 程序退出前 `defer logger.Sync()` 确保刷盘 |
| 错误日志不含堆栈 | `zap.AddStacktrace(zap.ErrorLevel)` 自动附加 |
| 日志文件不轮转 | lumberjack 自动按大小/时间切割，避免磁盘爆满 |

---

## 七、练习题

### 🟢 基础题
1. 用 slog 实现一个中间件，记录请求的 User-Agent 和 Referer
2. 配置 lumberjack，设置日志文件超过 50MB 自动切割，保留最近 5 个备份

### 🟡 进阶题
3. 实现请求 ID 在 HTTP 调用链中透传（A 服务调 B 服务时自动携带）
4. 实现基于响应状态码的分级日志：2xx → Info、4xx → Warn、5xx → Error

### 🔴 挑战题
5. 实现 zap + slog 双轨日志，并添加 Prometheus metrics 统计各状态码的请求量

---

## 八、知识点总结

```
Gin 日志管理
├── 1. slog (Go 1.21+)
│   ├── NewJSONHandler → JSON 格式
│   ├── InfoContext/ErrorContext → context 传递
│   └── SetDefault → 全局默认
├── 2. zap (uber-go)
│   ├── NewProduction/NewDevelopment → 预设
│   ├── Core + Encoder + Writer → 自定义
│   ├── AddCaller → 记录调用位置
│   └── AddStacktrace → 错误堆栈
├── 3. lumberjack 轮转
│   ├── MaxSize → 文件大小上限
│   ├── MaxBackups → 保留备份数
│   ├── MaxAge → 保留天数
│   └── Compress → gzip 压缩
├── 4. 请求 ID
│   ├── UUID 生成
│   ├── Header 透传
│   └── Context 存储
└── 5. 慢请求检测
    ├── 阈值配置
    ├── Warn 级别记录
    └── APM 集成
```

---

## 九、举一反三

| 场景 | 方案 |
|------|------|
| ELK 日志平台 | zap JSON → Filebeat → Logstash → Elasticsearch → Kibana |
| 链路追踪 | OpenTelemetry + Jaeger，request_id 映射为 trace_id |
| 实时日志告警 | 日志写入 Kafka → Flink/Consumer → 告警规则 |
| 多服务统一日志 | 统一日志格式 + 分布式 ID + 日志聚合平台 |
| 性能分析 | 慢请求日志 → pprof 分析 → 火焰图定位 |

---

## 十、参考资料

- [Go slog 官方文档](https://pkg.go.dev/log/slog)
- [uber-go/zap GitHub](https://github.com/uber-go/zap)
- [lumberjack 日志轮转](https://github.com/natefinch/lumberjack)
- [Gin 自定义日志](https://gin-gonic.com/docs/examples/custom-log-format/)

---

## 十一、代码演进

| 版本 | 变化 |
|------|------|
| **v1** | slog 结构化日志 + 请求 ID 追踪 |
| **v2** | zap 高性能日志 + lumberjack 轮转 + 慢请求检测 |
| **v3** | 双轨日志（slog→stdout, zap→file）+ 分级文件输出 |

> **下一步建议：** 集成 OpenTelemetry，将日志与分布式追踪串联，实现全链路可观测性。
