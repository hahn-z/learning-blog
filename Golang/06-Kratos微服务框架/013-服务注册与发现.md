---
title: "013-服务注册与发现"
slug: "013-kratos-registry"
category: "Kratos框架"
tech_stack: "Golang"
created_at: "2026-04-25T18:37:02.892+08:00"
updated_at: "2026-04-29T10:02:45.377+08:00"
reading_time: 26
tags: []
---


# 013-服务注册与发现

> **难度：** ⭐⭐⭐⭐ | **适合：** 需要搭建多服务协作的微服务开发者

## 一、概念讲解

微服务架构中，服务实例动态变化（扩容、重启、迁移），调用方不能硬编码地址。**服务注册与发现**解决这个问题：

1. **注册**：服务启动时将自己的地址信息写入注册中心
2. **发现**：调用方从注册中心获取目标服务的可用实例列表
3. **心跳/健康检查**：注册中心定期检查实例是否存活，剔除不健康实例

### 核心角色

```
┌──────────┐    注册    ┌──────────────┐    查询    ┌──────────┐
│ Service A │ ────────→ │  Registry    │ ←──────── │ Service B │
│ (Provider)│           │ (Consul/Etcd)│           │(Consumer) │
└──────────┘           └──────────────┘           └──────────┘
     ↑                       │                         │
     └── 心跳保活 ───────────┘                         │
     └── 健康检查 ──────────┘                         │
                                         负载均衡选择实例 ↓
```

### Kratos Registry 接口

Kratos抽象了统一的注册中心接口，适配多种实现：

```go
// registry/registry.go
type Registrar interface {
    Register(ctx context.Context, service *ServiceInstance) error
    Deregister(ctx context.Context, service *ServiceInstance) error
}

type Discovery interface {
    GetService(ctx context.Context, serviceName string) ([]*ServiceInstance, error)
    Watch(ctx context.Context, serviceName string) (Watcher, error)
}
```

## 二、脑图

```
服务注册与发现
├── 注册中心选型
│   ├── Consul (推荐入门)
│   ├── Etcd (K8s生态)
│   ├── Nacos (阿里开源)
│   ├── Zookeeper (老牌)
│   └── Polaris (腾讯云)
├── Kratos集成
│   ├──Registrar接口
│   ├── Discovery接口
│   └── ServiceInstance结构
├── 配置方式
│   ├── 代码配置
│   └── 配置文件
├── 健康检查
│   ├── 客户端心跳
│   └── 服务端主动检查
└── 负载均衡
    ├── Round Robin
    ├── Random
    ├── Weighted
    └── 自定义策略
```

## 三、完整Go代码

### v1: Consul注册基础

```go
// go.mod dependencies
// require (
//     github.com/go-kratos/kratos/v2 latest
//     github.com/go-kratos/kratos/contrib/registry/consul/v2 latest
//     github.com/hashicorp/consul/api latest
// )
```

```go
// cmd/server/main.go - Service with Consul registration
package main

import (
    "log"

    "github.com/go-kratos/kratos/contrib/registry/consul/v2"
    "github.com/go-kratos/kratos/v2"
    "github.com/go-kratos/kratos/v2/transport/grpc"
    "github.com/go-kratos/kratos/v2/transport/http"
    "github.com/hashicorp/consul/api"
)

func main() {
    // Create Consul client
    consulClient, err := api.NewClient(api.DefaultConfig())
    if err != nil {
        log.Fatal(err)
    }

    // Create Kratos Consul registrar
    r := consul.New(consulClient)

    // Create servers with registrar
    httpSrv := http.NewServer(
        http.Address(":8000"),
    )
    grpcSrv := grpc.NewServer(
        grpc.Address(":9000"),
    )

    app := kratos.New(
        kratos.Name("helloworld"),        // Service name for registration
        kratos.Server(httpSrv, grpcSrv),
        kratos.Registrar(r),              // Registrar for service registration
    )

    if err := app.Run(); err != nil {
        log.Fatal(err)
    }
}
```

### v2: 完整配置 + 服务发现消费方

```yaml
# internal/conf/conf.proto (protobuf config)
# Server config with registry settings
```

```go
// internal/server/server.go - Configurable registry
package server

import (
    "log"

    "github.com/go-kratos/kratos/contrib/registry/consul/v2"
    "github.com/go-kratos/kratos/v2"
    "github.com/go-kratos/kratos/v2/transport/grpc"
    "github.com/go-kratos/kratos/v2/transport/http"
    "github.com/hashicorp/consul/api"
)

// NewApp create kratos application with registry
func NewApp(httpSrv *http.Server, grpcSrv *grpc.Server) *kratos.App {
    consulClient, err := api.NewClient(&api.Config{
        Address:    "127.0.0.1:8500",   // Consul address
        Scheme:     "http",
        Token:      "",                  // ACL token if needed
    })
    if err != nil {
        log.Fatalf("failed to create consul client: %v", err)
    }

    r := consul.New(consulClient)

    return kratos.New(
        kratos.Name("helloworld"),
        kratos.Version("v1.0.0"),
        kratos.Metadata(map[string]string{
            "env":     "production",
            "version": "v1.0.0",
        }),
        kratos.Server(httpSrv, grpcSrv),
        kratos.Registrar(r),
    )
}
```

```go
// internal/data/data.go - Service discovery for consumers
package data

import (
    "context"

    "github.com/go-kratos/kratos/contrib/registry/consul/v2"
    "github.com/go-kratos/kratos/v2/registry"
    "github.com/go-kratos/kratos/v2/transport/grpc"
    "github.com/hashicorp/consul/api"
    "google.golang.org/grpc/credentials/insecure"
)

// NewDiscovery create service discovery from Consul
func NewDiscovery() registry.Discovery {
    consulClient, _ := api.NewClient(api.DefaultConfig())
    return consul.New(consulClient)
}

// NewGreeterClient create gRPC client with service discovery
func NewGreeterClient(r registry.Discovery) pb.GreeterClient {
    conn, err := grpc.DialInsecure(
        context.Background(),
        grpc.WithEndpoint("discovery:///helloworld"),   // Use discovery scheme
        grpc.WithDiscovery(r),                           // Inject discovery
    )
    if err != nil {
        panic(err)
    }
    return pb.NewGreeterClient(conn)
}
```

### v3: Etcd注册 + 健康检查 + 负载均衡

```go
// internal/server/server.go - Etcd registry variant
package server

import (
    "log"
    "time"

    etcdreg "github.com/go-kratos/kratos/contrib/registry/etcd/v2"
    "github.com/go-kratos/kratos/v2"
    "github.com/go-kratos/kratos/v2/transport/grpc"
    "github.com/go-kratos/kratos/v2/transport/http"
    clientv3 "go.etcd.io/etcd/client/v3"
)

// NewAppWithEtcd use Etcd as registry
func NewAppWithEtcd(httpSrv *http.Server, grpcSrv *grpc.Server) *kratos.App {
    etcdClient, err := clientv3.New(clientv3.Config{
        Endpoints:   []string{"127.0.0.1:2379"},
        DialTimeout: 5 * time.Second,
    })
    if err != nil {
        log.Fatal(err)
    }

    r := etcdreg.New(etcdClient)

    return kratos.New(
        kratos.Name("order-service"),
        kratos.Version("v1.0.0"),
        kratos.Server(httpSrv, grpcSrv),
        kratos.Registrar(r),
    )
}
```

```go
// internal/data/client.go - Consumer with load balancing
package data

import (
    "context"
    "time"

    "github.com/go-kratos/kratos/v2/selector"
    "github.com/go-kratos/kratos/v2/selector/p2c"
    "github.com/go-kratos/kratos/v2/transport/grpc"
)

// NewUserClient create client with P2C load balancing
func NewUserClient(r registry.Discovery) pb.UserClient {
    conn, err := grpc.DialInsecure(
        context.Background(),
        grpc.WithEndpoint("discovery:///user-service"),
        grpc.WithDiscovery(r),
        grpc.WithSelector(
            selector.SetGlobalSelector(p2c.New()),  // P2C load balancing
        ),
        grpc.WithTimeout(5*time.Second),
    )
    if err != nil {
        panic(err)
    }
    return pb.NewUserClient(conn)
}
```

```go
// internal/server/health.go - Health check implementation
package server

import (
    "context"

    "google.golang.org/grpc/health/grpc_health_v1"
)

// HealthServer implements gRPC health checking
type HealthServer struct {
    grpc_health_v1.UnimplementedHealthServer
}

// Check implements health check
func (h *HealthServer) Check(ctx context.Context, req *grpc_health_v1.HealthCheckRequest) (*grpc_health_v1.HealthCheckResponse, error) {
    return &grpc_health_v1.HealthCheckResponse{
        Status: grpc_health_v1.HealthCheckResponse_SERVING,
    }, nil
}

// Watch implements health watch
func (h *HealthServer) Watch(req *grpc_health_v1.HealthCheckRequest, stream grpc_health_v1.Health_WatchServer) error {
    return stream.Send(&grpc_health_v1.HealthCheckResponse{
        Status: grpc_health_v1.HealthCheckResponse_SERVING,
    })
}
```

## 四、执行预览

```bash
# Start Consul (Docker)
$ docker run -d --name consul -p 8500:8500 hashicorp/consul agent -dev

# Start provider service (multiple instances)
$ go run cmd/server/main.go --addr :8001
$ go run cmd/server/main.go --addr :8002
$ go run cmd/server/main.go --addr :8003

# Check Consul UI
$ open http://localhost:8500/ui
# → See "helloworld" service with 3 instances

# Start consumer - requests distributed across instances
$ go run cmd/client/main.go
# → Calling helloworld via :8001
# → Calling helloworld via :8002
# → Calling helloworld via :8003

# Kill one instance
$ kill <pid-8002>
# → Consul auto-deregisters, traffic routes to :8001 and :8003
```

## 五、注意事项

| 注意点 | 说明 | 建议 |
|--------|------|------|
| 注册中心可用性 | 注册中心宕机导致全链路故障 | 集群部署 + 本地缓存 |
| 心跳间隔 | 太短浪费资源，太长切换慢 | 建议10-30秒 |
| 优雅下线 | kill -9会导致残留节点 | 使用kratos.New的Stop钩子 |
| 服务名规范 | 名称冲突导致路由错误 | 使用 `{team}.{service}.{version}` 格式 |
| 多注册中心 | 不同环境用不同注册中心 | 通过配置文件切换 |
| 实例元数据 | 版本/权重/环境信息 | 用于灰度发布和流量控制 |

## 六、避坑指南

| ❌ 错误做法 | ✅ 正确做法 |
|-------------|------------|
| 硬编码服务地址 | 使用 `discovery:///service-name` 方案 |
| 注册中心单节点 | 至少3节点集群部署 |
| 忽略服务deregister | 实现优雅关闭，调用Deregister |
| 心跳超时设太长 | 根据业务容忍度设置（通常15-30秒） |
| 不做健康检查 | 实现gRPC health check协议 |
| 所有环境共用注册中心 | dev/staging/prod 隔离 |
| 忽略DNS缓存 | 客户端设置合理的刷新间隔 |

## 七、练习题

### 🟢 基础题
1. 用Docker启动Consul，注册一个HTTP服务并验证发现
2. 修改服务名和版本号，观察Consul UI变化

### 🟡 进阶题
3. 从Consul切换到Etcd，只需修改注册中心配置
4. 启动3个服务实例，用P2C负载均衡观察请求分布

### 🔴 挑战题
5. 实现基于元数据的灰度发布：只调用带有 `version=canary` 标签的实例
6. 实现自定义负载均衡策略：优先调用同机房实例

## 八、知识点总结

```
服务注册与发现
├── 核心概念
│   ├── 服务注册 Register
│   ├── 服务发现 Discovery/Watch
│   └── 健康检查 Health Check
├── Kratos接口
│   ├── registry.Registrar
│   ├── registry.Discovery
│   └── registry.ServiceInstance
├── 注册中心实现
│   ├── Consul (HTTP-based, easy setup)
│   ├── Etcd (KV-based, K8s friendly)
│   ├── Nacos (Alibaba, Java friendly)
│   └── Polaris (Tencent, feature rich)
├── 负载均衡
│   ├── Round Robin (default)
│   ├── P2C (Power of Two Choices)
│   ├── Weighted Round Robin
│   └── Custom Selector
└── 生命周期
    ├── 启动 → Register
    ├── 运行 → Heartbeat/HealthCheck
    └── 关闭 → Deregister
```

## 九、举一反三

| 场景 | 方案 | 技术 |
|------|------|------|
| 多环境隔离 | Consul Namespace / Etcd Prefix | 环境变量+配置 |
| 灰度发布 | 元数据标签 + 自定义Selector | version/canary标签 |
| 同机房优先 | 自定义Selector优先本zone | metadata zone匹配 |
| 故障转移 | Watch变更 + 自动摘除 | Discovery.Watch |
| 服务熔断 | selector + circuit breaker | kratos circuit breaker |
| 蓝绿部署 | 双版本注册 + 流量切换 | version metadata |

## 十、参考资料

- [Kratos官方文档 - Registry](https://go-kratos.dev/docs/component/registry)
- [Consul Service Discovery](https://developer.hashicorp.com/consul/docs/concepts/service-discovery)
- [Etcd as Service Discovery](https://etcd.io/docs/latest/learning/)
- [Kratos Registry Contrib](https://github.com/go-kratos/kratos/tree/main/contrib/registry)

## 十一、代码演进

### v1 → v2 的变化
- 从硬编码Consul地址到可配置
- 新增消费者端服务发现（Discovery）
- 添加服务元数据（版本、环境）
- 使用 `discovery:///` URI方案

### v2 → v3 的变化
- 支持Etcd注册中心（展示接口统一性）
- 添加负载均衡策略（P2C）
- 实现gRPC健康检查
- 完整的服务生命周期管理

### 核心演进路径
```
单节点Consul注册 → 配置化 + 服务发现 + 消费方 → 多注册中心 + 负载均衡 + 健康检查
```
