---
title: "010-模块与包管理"
slug: "010-modules"
category: "Python入门"
tech_stack: "Python"
created_at: "2026-04-29T06:16:59.255+08:00"
updated_at: "2026-04-29T10:02:47.307+08:00"
reading_time: 19
tags: []
---

# 010 - 模块与包管理

> **难度：** ⭐⭐（初级偏上）
> **预计阅读时间：** 20 分钟
> **前置知识：** 变量、函数、基础语法

---

## 一、概念讲解

### 什么是模块（Module）？

模块就是一个 `.py` 文件。没错，就这么简单。你写的每一个 Python 文件，都是一个模块。

```python
# my_module.py
def greet(name):
    return f"Hello, {name}!"

PI = 3.14159
```

当你在另一个文件里 `import my_module`，就能用 `my_module.greet("Alice")` 了。

### 什么是包（Package）？

包就是一个**文件夹**，里面有个 `__init__.py` 文件（可以为空）。文件夹里放多个模块，就组成了一个包。

```
my_package/
├── __init__.py
├── math_tools.py
└── string_tools.py
```

### 为什么要用模块和包？

| 理由 | 说明 |
|------|------|
| **代码复用** | 写一次，到处 import |
| **命名空间隔离** | 避免变量名冲突 |
| **可维护性** | 代码分门别类，好找好改 |
| **团队协作** | 各人负责不同模块，互不干扰 |
| **标准库扩展** | Python 自带 200+ 模块，按需加载 |

---

## 二、脑图（ASCII）

```
                    模块与包管理
                        │
        ┌───────────────┼───────────────┐
        │               │               │
     导入方式        自定义模块       包管理
        │               │               │
   ┌────┼────┐     ┌────┼────┐     ┌────┼────┐
   │    │    │     │    │    │     │    │    │
 import  from  as  单文件  文件夹  pip  requirements
   │    │                      │          │
   │  选择性导入            __init__.py 虚拟环境
   │
 math / os / sys
 json / datetime
 collections / itertools
```

---

## 三、完整代码示例

### 3.1 基础导入方式

```python
# === Basic import styles ===

# Style 1: Import entire module
import math
print(math.sqrt(16))       # 4.0
print(math.pi)             # 3.141592653589793

# Style 2: Import specific items
from math import sqrt, pi
print(sqrt(25))            # 5.0
print(pi)                  # 3.141592653589793

# Style 3: Import with alias
import datetime as dt
now = dt.datetime.now()
print(now.strftime("%Y-%m-%d %H:%M:%S"))

# Style 4: Import all (avoid in production!)
from math import *
print(pow(2, 10))          # 1024.0
```

### 3.2 自定义模块

```python
# === File: string_utils.py ===

def truncate(text, max_length=50, suffix="..."):
    """Truncate text to max_length, appending suffix if needed."""
    if len(text) <= max_length:
        return text
    return text[:max_length - len(suffix)] + suffix

def count_words(text):
    """Count words in a text string."""
    return len(text.split())

def reverse_words(text):
    """Reverse the order of words in a text."""
    words = text.split()
    return " ".join(reversed(words))

# Only runs when executed directly, not when imported
if __name__ == "__main__":
    sample = "Python modules are powerful tools for code organization"
    print(truncate(sample, 30))    # Python modules are powerfu...
    print(count_words(sample))     # 8
    print(reverse_words(sample))   # organization code for tools...
```

### 3.3 自定义包

```python
# === Package structure ===
# my_tools/
#   __init__.py
#   math_helpers.py
#   text_helpers.py

# --- my_tools/__init__.py ---
from .math_helpers import add, multiply
from .text_helpers import shout, whisper

# --- my_tools/math_helpers.py ---
def add(a, b):
    return a + b

def multiply(a, b):
    return a * b

# --- my_tools/text_helpers.py ---
def shout(text):
    return text.upper() + "!!!"

def whisper(text):
    return text.lower() + "..."

# === Usage ===
# from my_tools import add, shout
# print(add(3, 5))       # 8
# print(shout("hello"))  # HELLO!!!
```

### 3.4 pip 包管理实战

```python
# === Package management with pip (run in terminal) ===

# Install a package
# pip install requests

# Install specific version
# pip install requests==2.31.0

# Install from requirements.txt
# pip install -r requirements.txt

# --- requirements.txt example ---
# requests>=2.28.0
# flask>=3.0.0
# python-dotenv>=1.0.0

# List installed packages
# pip list

# Show package info
# pip show requests

# Uninstall
# pip uninstall requests
```

### 3.5 相对导入与绝对导入

```python
# === Absolute vs Relative imports ===

# Absolute import (recommended)
from my_tools.math_helpers import add

# Relative import (use inside packages)
from .math_helpers import add       # Same package
from ..sibling_pkg.module import fn  # Parent's sibling package

# __all__ controls what "from module import *" exports
__all__ = ["add", "multiply"]  # Explicitly exported names
```

---

## 四、执行预览

```
$ python string_utils.py
Python modules are powerfu...
8
organization code for tools powerful are modules Python

$ python -c "from my_tools import add, shout; print(add(2,3)); print(shout('hi'))"
5
HI!!!

$ pip install requests
Successfully installed requests-2.31.0

$ pip list | grep requests
requests    2.31.0
```

---

## 五、注意事项

| 事项 | 说明 | 建议 |
|------|------|------|
| `__init__.py` | Python 3.3+ 可以省略（命名空间包） | 建议始终保留，保持兼容 |
| `import *` | 污染命名空间，可能覆盖同名变量 | 生产代码禁止使用 |
| 循环导入 | A 导入 B，B 又导入 A | 重构代码，提取公共部分到 C |
| 搜索路径 | Python 按 `sys.path` 顺序查找模块 | 添加路径用 `sys.path.insert(0, path)` |
| 缓存机制 | `__pycache__/` 存编译后的字节码 | 开发时修改模块可能需要重启 |
| 相对导入 | 只能在包内部使用 | 入口脚本用绝对导入 |

---

## 六、避坑指南

### ❌ 坑1：模块名与标准库冲突

```python
# ❌ 文件命名为 random.py
import random
print(random.randint(1, 10))  # 可能导入自己而不是标准库！

# ✅ 使用有意义的模块名
import my_random_utils
```

### ❌ 坑2：循环导入

```python
# ❌ a.py imports b.py, b.py imports a.py -> ImportError

# ✅ 延迟导入或提取公共模块
def func_a():
    from b import func_b  # Import inside function
    return func_b()
```

### ❌ 坑3：直接修改 sys.path

```python
# ❌ 硬编码路径
sys.path.append("/home/user/my-project/src")

# ✅ 使用项目结构和相对路径
import os
sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))
```

---

## 七、练习题

### 🟢 入门题

1. 创建一个模块 `calculator.py`，包含 `add`、`subtract`、`multiply`、`divide` 四个函数，然后在另一个文件中导入并使用。

2. 使用 `from math import ceil, floor`，对数字 `4.7` 分别向上和向下取整。

### 🟡 进阶题

3. 创建一个包 `geometry`，包含 `circle.py`（计算圆面积、周长）和 `rectangle.py`（计算矩形面积、周长），在 `__init__.py` 中统一导出。

4. 编写代码检查某个包是否已安装（提示：`importlib.util.find_spec`），如果没安装就提示用户安装。

### 🔴 挑战题

5. 实现一个简单的插件系统：程序启动时自动扫描 `plugins/` 目录下的所有 `.py` 文件，动态导入并注册每个插件模块。

---

## 八、知识点总结

```
模块与包管理
├── 模块基础
│   ├── 本质：一个 .py 文件
│   ├── 导入方式：import / from...import / as
│   └── __name__ == "__main__" 惯用法
├── 包结构
│   ├── 目录 + __init__.py
│   ├── __all__ 控制导出
│   └── 相对导入 vs 绝对导入
├── 包管理工具
│   ├── pip install / uninstall / list
│   ├── requirements.txt
│   └── 虚拟环境（venv）
└── 最佳实践
    ├── 避免循环导入
    ├── 命名不与标准库冲突
    └── 优先使用绝对导入
```

---

## 九、举一反三

| 场景 | 模块/工具 | 用法示例 |
|------|-----------|----------|
| 处理 JSON | `json` | `json.loads()`, `json.dumps()` |
| 操作系统路径 | `os.path` / `pathlib` | `Path("dir/file.txt").read_text()` |
| 日期时间 | `datetime` | `datetime.now()`, `timedelta(days=7)` |
| 随机数 | `random` | `random.choice(list)`, `random.randint()` |
| 正则表达式 | `re` | `re.findall(r'\d+', text)` |
| 集合工具 | `collections` | `Counter()`, `defaultdict()` |
| 文件匹配 | `glob` | `glob.glob("*.py")` |
| 第三方包管理 | `pip` | `pip install -r requirements.txt` |

---

## 十、参考资料

- [Python 官方文档 - Modules](https://docs.python.org/3/tutorial/modules.html)
- [Python 官方文档 - Packages](https://docs.python.org/3/reference/import.html)
- [pip 用户指南](https://pip.pypa.io/en/stable/user_guide/)
- [Real Python - Python Modules and Packages](https://realpython.com/python-modules-packages/)

---

## 十一、代码演进

### v1：最简模块

```python
# utils.py - Everything in one file
def add(a, b):
    return a + b

def greet(name):
    return f"Hello, {name}!"
```

### v2：拆分为包

```python
# myapp/__init__.py
from .math_utils import add
from .text_utils import greet

# myapp/math_utils.py
def add(a, b): return a + b

# myapp/text_utils.py
def greet(name): return f"Hello, {name}!"

# Usage:
# from myapp import add, greet
```

### v3：专业级项目结构

```
# Project structure with proper packaging
myproject/
├── pyproject.toml          # Modern Python packaging config
├── src/
│   └── myapp/
│       ├── __init__.py
│       ├── math_utils.py
│       └── text_utils.py
├── tests/
│   ├── test_math.py
│   └── test_text.py
└── README.md

# pyproject.toml
# [project]
# name = "myapp"
# version = "0.1.0"
# dependencies = ["requests>=2.28"]
#
# Install in development mode:
# pip install -e .
```

---

> **下一篇：** [011 - 文件读写操作](/posts/011-file-io)
> **上一篇：** [009 - 字典与集合](/posts/009-dict-set)
