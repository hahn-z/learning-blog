---
title: "003 - 字符串操作详解"
slug: "003-strings"
category: "Python入门"
tech_stack: "Python"
created_at: "2026-04-29T06:16:59.219+08:00"
updated_at: "2026-04-29T10:02:47.247+08:00"
reading_time: 37
tags: []
---

## 📊 难度标注

> **难度：** ⭐⭐☆☆☆ 基础级
> **前置知识：** 变量与数据类型（002）
> **预计用时：** 50-60分钟
> **学习目标：** 熟练使用字符串创建、切片、格式化、常用方法

---

## 📖 概念讲解

### 字符串的本质

Python中，字符串是**不可变的Unicode字符序列**。三个关键点：

1. **不可变**：创建后不能修改，每次"修改"实际是创建新字符串
2. **序列**：有序、可迭代、可通过索引访问
3. **Unicode**：原生支持中文、emoji等所有字符

```python
s = "Hello"
# s[0] = "h"  # ERROR! Strings are immutable
s = "hello"    # This creates a NEW string, doesn't modify the old one
```

### 字符串的四种创建方式

```python
# Single quotes
s1 = 'Hello'

# Double quotes (identical to single)
s2 = "Hello"

# Triple quotes (multi-line)
s3 = """Line 1
Line 2
Line 3"""

# Raw strings (no escape processing)
s4 = r"C:\new\test"  # \n and \t are literal, not escape sequences
```

单引号和双引号完全等价，选择标准是：**字符串内容包含哪种引号就用另一种**。

### 字符串格式化演进

Python字符串格式化经历了三代：

| 方式 | 语法 | Python版本 | 推荐度 |
|------|------|-----------|--------|
| % 格式化 | `"Hello %s" % name` | 所有版本 | ⭐⭐ |
| .format() | `"Hello {}".format(name)` | 2.6+ | ⭐⭐⭐ |
| f-string | `f"Hello {name}"` | 3.6+ | ⭐⭐⭐⭐⭐ |

**f-string 是现代Python的标准写法**，性能最好、语法最简洁。

### 切片（Slicing）

切片是Python最优雅的特性之一，用于提取子序列：

```
字符串:  P  y  t  h  o  n
索引:    0  1  2  3  4  5
负索引: -6 -5 -4 -3 -2 -1

语法: s[start:stop:step]
```

---

## 🗺️ 知识脑图

```
字符串操作
├── 创建
│   ├── 单/双引号：'hello', "world"
│   ├── 三引号：多行字符串
│   ├── 原始字符串：r"raw"
│   └── 转义字符：\n, \t, \\
├── 基础操作
│   ├── 拼接：+ 和 join()
│   ├── 重复：*
│   ├── 索引：s[0], s[-1]
│   ├── 切片：s[1:4], s[::-1]
│   └── 长度：len(s)
├── 格式化
│   ├── f-string（推荐）
│   ├── .format()
│   ├── % 格式化
│   └── 格式规范：:.2f, :>10, :^20
├── 常用方法
│   ├── 查找：find(), index(), count()
│   ├── 判断：startswith(), isdigit()
│   ├── 变换：upper(), lower(), strip()
│   ├── 替换：replace()
│   └── 分割：split(), join()
└── 高级技巧
    ├── 字符串对齐
    ├── 编码解码：encode(), decode()
    └── 正则表达式入门
```

---

## 💻 完整Python代码

### 字符串创建与基础操作

```python
# string_basics.py - String creation and basic operations

# Four ways to create strings
s1 = 'Single quotes'
s2 = "Double quotes"
s3 = """Triple quotes
can span
multiple lines"""
s4 = r"Raw string: \n is not newline"

# Concatenation
greeting = "Hello" + " " + "World"
print(greeting)  # Hello World

# Repetition
separator = "-" * 30
print(separator)  # ------------------------------

# Length
print(f"Length of '{s1}': {len(s1)}")  # 14

# Membership test
print(f"'quotes' in s1: {'quotes' in s1}")  # True
print(f"'double' in s1: {'double' in s1}")  # False
```

### 索引与切片

```python
# slicing.py - String indexing and slicing

s = "Python"

# Indexing
print(f"s[0] = '{s[0]}'")    # P
print(f"s[-1] = '{s[-1]}'")  # n
print(f"s[3] = '{s[3]}'")    # h

# Basic slicing: s[start:stop] (stop is exclusive)
print(f"s[0:3] = '{s[0:3]}'")   # Pyt
print(f"s[2:5] = '{s[2:5]}'")   # tho
print(f"s[:3] = '{s[:3]}'")     # Pyt (omit start = 0)
print(f"s[3:] = '{s[3:]}'")     # hon (omit end = len)

# Negative index slicing
print(f"s[-3:] = '{s[-3:]}'")   # hon (last 3 chars)
print(f"s[:-2] = '{s[:-2]}'")   # Pyth (all but last 2)

# Step slicing: s[start:stop:step]
print(f"s[::2] = '{s[::2]}'")     # Pto (every 2nd char)
print(f"s[1::2] = '{s[1::2]}'")   # yhn (every 2nd, starting from index 1)
print(f"s[::-1] = '{s[::-1]}'")   # nohtyP (reverse!)
print(f"s[::-2] = '{s[::-2]}'")   # nhy (reverse every 2nd)

# Practical examples
email = "user@example.com"
domain = email.split("@")[1]  # example.com
username = email.split("@")[0]  # user
print(f"\nUsername: {username}, Domain: {domain}")

# Reverse a string
text = "ABBA"
print(f"'{text}' reversed: '{text[::-1]}'")
print(f"Is palindrome: {text == text[::-1]}")  # True
```

### f-string 格式化大全

```python
# fstring_demo.py - f-string formatting complete guide

name = "Python"
version = 3.12
pi = 3.14159265

# Basic f-string
print(f"Language: {name} {version}")

# Expressions inside f-string
print(f"Upper: {name.upper()}")
print(f"2 + 3 = {2 + 3}")
print(f"Version check: {'modern' if version >= 3.6 else 'old'}")

# Number formatting
print(f"\n--- Number Formatting ---")
print(f"Pi: {pi}")                  # 3.14159265
print(f"Pi (2 decimal): {pi:.2f}")  # 3.14
print(f"Pi (4 decimal): {pi:.4f}")  # 3.1416
print(f"Percent: {0.856:.1%}")      # 85.6%
print(f"Scientific: {pi:.2e}")      # 3.14e+00
print(f"Thousand sep: {1234567:,}") # 1,234,567
print(f"Binary: {42:b}")            # 101010
print(f"Hex: {255:x}")              # ff
print(f"Octal: {8:o}")              # 10
print(f"Padding: {42:05d}")         # 00042

# Alignment
print(f"\n--- Alignment ---")
print(f"{'Left':<20}|")    # Left                 |
print(f"{'Center':^20}|")  #        Center        |
print(f"{'Right':>20}|")   #                 Right|
print(f"{'Fill':*^20}|")   # ********Fill********|

# Dictionary in f-string
data = {"name": "Alice", "score": 95}
print(f"\nStudent: {data['name']}, Score: {data['score']}")

nums = [1, 2, 3]
print(f"List: {nums}")
```

### 常用字符串方法

```python
# string_methods.py - Essential string methods

# --- Case methods ---
s = "Hello World"
print(f"upper():      '{s.upper()}'")       # HELLO WORLD
print(f"lower():      '{s.lower()}'")       # hello world
print(f"title():      '{s.title()}'")       # Hello World
print(f"capitalize():  '{s.capitalize()}'") # Hello world
print(f"swapcase():   '{s.swapcase()}'")    # hELLO wORLD

# --- Search methods ---
text = "Python is awesome. Python is fun."
print(f"\nfind('Python'):    {text.find('Python')}")     # 0
print(f"find('Java'):      {text.find('Java')}")        # -1 (not found)
print(f"rfind('Python'):   {text.rfind('Python')}")     # 19
print(f"index('Python'):   {text.index('Python')}")     # 0
print(f"count('Python'):   {text.count('Python')}")     # 2
print(f"startswith('Py'):  {text.startswith('Py')}")    # True
print(f"endswith('fun.'):  {text.endswith('fun.')}")    # True

# --- Strip methods ---
messy = "  Hello World  "
print(f"\nstrip():    '{messy.strip()}'")     # 'Hello World'
print(f"lstrip():   '{messy.lstrip()}'")    # 'Hello World  '
print(f"rstrip():   '{messy.rstrip()}'")    # '  Hello World'

# Strip specific characters
code = "xxHelloxx"
print(f"strip('x'): '{code.strip('x')}")   # 'Hello'

# --- Replace and split ---
greeting = "Hello World"
print(f"\nreplace('World', 'Python'): '{greeting.replace('World', 'Python')}'")
print(f"replace('l', 'L', 1): '{greeting.replace('l', 'L', 1)}'")  # Only first

csv_line = "name,age,city"
parts = csv_line.split(",")
print(f"split(','): {parts}")  # ['name', 'age', 'city']

words = "Hello World"
print(f"split(): {words.split()}")  # ['Hello', 'World']

# Join (reverse of split)
fruits = ["apple", "banana", "cherry"]
print(f"' | '.join(fruits): '{' | '.join(fruits)}'")

# --- Check methods ---
print(f"\n'123'.isdigit(): {'123'.isdigit()}")        # True
print(f"'abc'.isalpha(): {'abc'.isalpha()}")          # True
print(f"'abc123'.isalnum(): {'abc123'.isalnum()}")    # True
print(f"'   '.isspace(): {'   '.isspace()}")          # True
print(f"'Hello'.islower(): {'Hello'.islower()}")      # False
print(f"'HELLO'.isupper(): {'HELLO'.isupper()}")       # True
```

### 实用字符串处理示例

```python
# practical_strings.py - Real-world string processing

def clean_user_input(text: str) -> str:
    # Clean and normalize user input
    return text.strip().lower()

def format_phone(phone: str) -> str:
    # Format phone number: 13812345678 -> 138-1234-5678
    phone = phone.replace("-", "").replace(" ", "")
    if len(phone) == 11 and phone.isdigit():
        return f"{phone[:3]}-{phone[3:7]}-{phone[7:]}"
    return phone

def mask_email(email: str) -> str:
    # Mask email: alice@example.com -> a***e@example.com
    if "@" not in email:
        return email
    user, domain = email.split("@")
    if len(user) <= 2:
        masked = user[0] + "***"
    else:
        masked = user[0] + "***" + user[-1]
    return f"{masked}@{domain}"

def truncate(text: str, max_len: int = 20, suffix: str = "...") -> str:
    # Truncate text with ellipsis
    if len(text) <= max_len:
        return text
    return text[:max_len - len(suffix)] + suffix

# Demo
print("--- User Input Cleaning ---")
inputs = ["  Hello  ", "WORLD", "  Python  "]
for raw in inputs:
    print(f"'{raw}' -> '{clean_user_input(raw)}'")

print("\n--- Phone Formatting ---")
phones = ["13812345678", "138-1234-5678", "123"]
for p in phones:
    print(f"{p} -> {format_phone(p)}")

print("\n--- Email Masking ---")
emails = ["alice@example.com", "bob@test.org", "x@y.com"]
for e in emails:
    print(f"{e} -> {mask_email(e)}")

print("\n--- Text Truncation ---")
texts = ["Hello World", "This is a very long sentence that needs truncation"]
for t in texts:
    print(f"'{t}' -> '{truncate(t, 20)}'")
```

---

## 👀 执行预览

**运行 slicing.py：**

```
$ python slicing.py
s[0] = 'P'
s[-1] = 'n'
s[3] = 'h'
s[0:3] = 'Pyt'
s[2:5] = 'tho'
s[:3] = 'Pyt'
s[3:] = 'hon'
s[-3:] = 'hon'
s[:-2] = 'Pyth'
s[::2] = 'Pto'
s[1::2] = 'yhn'
s[::-1] = 'nohtyP'
s[::-2] = 'nhy'

Username: user, Domain: example.com

'ABBA' reversed: 'ABBA'
Is palindrome: True
```

**运行 fstring_demo.py（部分输出）：**

```
Language: Python 3.12
Upper: PYTHON
2 + 3 = 5
Version check: modern

--- Number Formatting ---
Pi: 3.14159265
Pi (2 decimal): 3.14
Percent: 85.6%
Thousand sep: 1,234,567
Binary: 101010
Padding: 00042

--- Alignment ---
Left                |
       Center       |
               Right|
********Fill********|
```

**运行 practical_strings.py：**

```
--- User Input Cleaning ---
'  Hello  ' -> 'hello'
'WORLD' -> 'world'
'  Python  ' -> 'python'

--- Phone Formatting ---
13812345678 -> 138-1234-5678
138-1234-5678 -> 138-1234-5678
123 -> 123

--- Email Masking ---
alice@example.com -> a***e@example.com
bob@test.org -> b***b@test.org
x@y.com -> x***@y.com

--- Text Truncation ---
'Hello World' -> 'Hello World'
'This is a very long sentence...' -> 'This is a very lo...'
```

---

## ⚠️ 注意事项

| 项目 | 说明 | 建议 |
|------|------|------|
| 不可变性 | 字符串操作返回新字符串 | `s = s.upper()` 而不是 `s.upper()` |
| 编码 | Python 3默认UTF-8 | 涉及文件读写时指定 `encoding='utf-8'` |
| 原始字符串 | `r""` 不处理转义 | 正则表达式和Windows路径用 `r""` |
| join vs + | `+` 拼接大量字符串效率低 | 批量拼接用 `"".join(list)` |
| index vs find | `index()` 找不到会抛异常 | 不确定是否存在时用 `find()` |
| 切片越界 | 不会报错，返回空串 | `s[100:]` 返回 `""` 而非报错 |
| 中文长度 | 一个中文算一个字符 | `len("你好")` = 2 |

---

## 🚫 避坑指南

### ❌ 拼接大量字符串用 +

```python
# Inefficient! Creates a new string each iteration
result = ""
for i in range(10000):
    result += str(i)  # O(n^2) time complexity
```

✅ **正确做法：** 使用 `join()`

```python
# Efficient! O(n) time complexity
parts = [str(i) for i in range(10000)]
result = "".join(parts)
```

### ❌ 忘记字符串不可变

```python
s = "hello"
s.upper()  # Returns "HELLO" but s is still "hello"!
print(s)   # hello
```

✅ **正确做法：** 重新赋值

```python
s = "hello"
s = s.upper()  # s is now "HELLO"
```

### ❌ 判断包含用 find() >= 0

```python
if s.find("hello") >= 0:  # Unnecessary
    print("found")
```

✅ **正确做法：** 使用 `in` 运算符

```python
if "hello" in s:  # Pythonic!
    print("found")
```

### ❌ 格式化浮点数用字符串截断

```python
pi_str = str(3.14159)[:4]  # "3.14" — fragile and wrong for larger numbers
```

✅ **正确做法：** 使用格式化

```python
pi_str = f"{3.14159:.2f}"  # "3.14" — correct and clear
```

### ❌ Windows路径转义问题

```python
path = "C:\new\test"  # \n and \t are escape sequences!
print(path)  # C:
# ew    est
```

✅ **正确做法：** 使用原始字符串

```python
path = r"C:\new\test"  # Raw string, no escape
# Or use forward slashes (works on Windows too!)
path = "C:/new/test"
```

---

## 🏋️ 练习题

### 🟢 入门题

**1. 回文检测**

编写函数，判断一个字符串是否是回文（忽略大小写和空格）。

```python
def is_palindrome(s: str) -> bool:
    pass

print(is_palindrome("racecar"))     # True
```

**2. 字符串反转**

用三种不同方法反转字符串 "Hello World"。

### 🟡 进阶题

**3. 密码强度检测**

编写函数，检查密码强度：
- 至少8位
- 包含大小写字母
- 包含数字
- 包含特殊字符
- 返回强度等级（弱/中/强）

**4. 文本统计器**

输入一段文字，统计：
- 总字符数
- 单词数
- 行数
- 各字符出现频率（Top 10）

### 🔴 挑战题

**5. 简易模板引擎**

实现一个模板替换函数：

```python
template = "Dear {name}, welcome to {city}!"
data = {"name": "Alice", "city": "Beijing"}

def render(template: str, data: dict) -> str:
    # "Dear Alice, welcome to Beijing!"
    pass

# Bonus: support nested placeholders and default values
# "Hello {user.name|Guest}"
```

---

## 🌳 知识点总结

```
字符串操作
├── 创建方式
│   ├── 'str' / "str" / multi-line
│   ├── r"raw string"
│   └── 转义字符：\n \t \\ \"
├── 索引与切片
│   ├── s[i] / s[-i]
│   ├── s[start:stop:step]
│   ├── s[::-1] → 反转
│   └── 切片越界不报错
├── 格式化
│   ├── f-string → f"{var}"（首选）
│   ├── .format() → "{}".format(v)
│   ├── % 格式化 → "%s" % v
│   └── 格式码：.2f >10 <20 ^20 , b x o
├── 查找与判断
│   ├── find() / rfind() → 返回索引/-1
│   ├── index() / rindex() → 返回索引/异常
│   ├── count() → 出现次数
│   ├── in → 包含判断
│   ├── startswith() / endswith()
│   └── isdigit() / isalpha() / isalnum()
├── 变换
│   ├── upper() / lower() / title()
│   ├── strip() / lstrip() / rstrip()
│   ├── replace(old, new[, count])
│   └── split(sep) / join(iterable)
└── 性能
    ├── join > + （批量拼接）
    ├── f-string > .format() > %
    └── 字符串不可变 → 每次操作创建新对象
```

---

## 🔄 举一反三

| 你学到的 | 可以应用到 | 例子 |
|----------|-----------|------|
| 切片 | 提取文件扩展名 | `name[name.rfind('.'):]` |
| split/join | CSV数据处理 | `",".join(row)` |
| strip() | 清洗用户输入 | `input().strip()` |
| f-string | 日志和报告输出 | `f"[{time}] {level}: {msg}"` |
| replace() | 文本批量替换 | 模板变量替换 |
| 正则+字符串 | 数据清洗提取 | 提取网页中的URL |
| 格式化 | 数据对齐显示 | 表格化输出 |
| 编码 | 网络通信 | `data.encode('utf-8')` |

---

## 📚 参考资料

- [Python字符串方法文档](https://docs.python.org/3/library/stdtypes.html#string-methods)
- [f-string官方文档](https://docs.python.org/3/reference/lexical_analysis.html#f-strings)
- [Unicode入门](https://docs.python.org/3/howto/unicode.html)
- [Python字符串格式化指南](https://realpython.com/python-string-formatting/)

---

## 🔬 代码演进

### v1：基础字符串拼接

```python
# v1: Basic string concatenation
first = "Hello"
last = "World"
message = first + " " + last
print(message)
```

### v2：使用字符串方法

```python
# v2: Using string methods
name = "  Python  "
print(name.strip().upper())
print(name.strip().lower())
print(f"Length: {len(name.strip())}")

# Split and join
words = "apple,banana,cherry".split(",")
print(" | ".join(words))
```

### v3：完整的文本处理器

```python
# v3: Text processor with multiple utilities
import re
from collections import Counter

class TextProcessor:
    # A simple text processing toolkit

    def __init__(self, text: str):
        self.text = text

    def word_count(self) -> int:
        return len(self.text.split())

    def char_count(self, ignore_spaces: bool = True) -> int:
        if ignore_spaces:
            return len(self.text.replace(" ", ""))
        return len(self.text)

    def top_chars(self, n: int = 5) -> list:
        chars = [c.lower() for c in self.text if c.isalpha()]
        return Counter(chars).most_common(n)

    def reverse_words(self) -> str:
        return " ".join(self.text.split()[::-1])

    def extract_numbers(self) -> list:
        return [int(n) for n in re.findall(r'\d+', self.text)]

    def summary(self) -> str:
        lines = [
            f"Characters: {self.char_count()}",
            f"Words: {self.word_count()}",
            f"Top chars: {self.top_chars(3)}",
            f"Reversed: {self.reverse_words()}",
        ]
        return "\n".join(lines)

# Usage
tp = TextProcessor("Hello World 123 Python 456")
print(tp.summary())
```

> **下一步：** 掌握字符串操作后，你已经具备了文本处理的基础能力。后续将学习条件判断和循环控制，让程序具备逻辑判断能力！
